-- =============================================
-- 澳小遊 Trip of Macau - 數據庫初始化腳本
-- 版本: 1.0
-- 日期: 2026-04-06
-- =============================================

CREATE DATABASE IF NOT EXISTS aoxiaoyou DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
USE aoxiaoyou;

-- 用戶表
CREATE TABLE IF NOT EXISTS users (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '主鍵ID',
    open_id VARCHAR(64) UNIQUE NOT NULL COMMENT '微信OpenID',
    nickname VARCHAR(64) COMMENT '昵稱',
    avatar_url VARCHAR(512) COMMENT '頭像URL',
    language_preference VARCHAR(10) DEFAULT 'zh_CN' COMMENT '語言偏好',
    level INT DEFAULT 1 COMMENT '等級',
    title VARCHAR(32) DEFAULT '探索新手' COMMENT '頭銜',
    total_stamps INT DEFAULT 0 COMMENT '總印章數',
    interface_mode ENUM('standard', 'elderly') DEFAULT 'standard' COMMENT '界面模式',
    font_scale DECIMAL(2,1) DEFAULT 1.0 COMMENT '字體縮放',
    high_contrast BOOLEAN DEFAULT FALSE COMMENT '高對比度',
    voice_guide_enabled BOOLEAN DEFAULT FALSE COMMENT '語音導覽',
    simplified_mode BOOLEAN DEFAULT FALSE COMMENT '簡化模式',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '創建時間',
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新時間',
    deleted TINYINT DEFAULT 0 COMMENT '邏輯刪除'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='用戶表';

-- POI景點表
CREATE TABLE IF NOT EXISTS pois (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '主鍵ID',
    name_zh VARCHAR(128) NOT NULL COMMENT '中文名稱',
    name_en VARCHAR(128) COMMENT '英文名稱',
    name_zht VARCHAR(128) COMMENT '繁體名稱',
    latitude DECIMAL(10,8) NOT NULL COMMENT '緯度',
    longitude DECIMAL(11,8) NOT NULL COMMENT '經度',
    address VARCHAR(256) COMMENT '地址',
    category_id BIGINT COMMENT '分類ID',
    trigger_radius INT DEFAULT 30 COMMENT '觸發半徑(米)',
    importance ENUM('normal', 'important', 'very_important') DEFAULT 'normal' COMMENT '重要性',
    story_line_id BIGINT COMMENT '關聯故事線ID',
    stamp_type VARCHAR(32) COMMENT '印章類型',
    description TEXT COMMENT '描述',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    deleted TINYINT DEFAULT 0
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='POI景點表';

-- POI分類表
CREATE TABLE IF NOT EXISTS poi_categories (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(64) NOT NULL COMMENT '分類名稱',
    icon VARCHAR(128) COMMENT '圖標',
    sort_order INT DEFAULT 0,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    deleted TINYINT DEFAULT 0
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 印章記錄表
CREATE TABLE IF NOT EXISTS user_stamps (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT NOT NULL COMMENT '用戶ID',
    stamp_type ENUM('location', 'story', 'mission', 'secret', 'team') NOT NULL COMMENT '印章類型',
    source_id BIGINT NOT NULL COMMENT '來源ID',
    checkin_type ENUM('gps', 'wifi', 'manual') DEFAULT 'gps' COMMENT '簽到方式',
    latitude DECIMAL(10,8) COMMENT '簽到緯度',
    longitude DECIMAL(11,8) COMMENT '簽到經度',
    collected_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 觸發日誌表
CREATE TABLE IF NOT EXISTS trigger_logs (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT NOT NULL,
    poi_id BIGINT NOT NULL,
    trigger_type ENUM('auto', 'manual') NOT NULL,
    distance DECIMAL(10,2) COMMENT '觸發時距離',
    gps_accuracy DECIMAL(10,2) COMMENT 'GPS精度',
    wifi_used BOOLEAN DEFAULT FALSE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_user_poi (user_id, poi_id),
    INDEX idx_created (created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 故事線表
CREATE TABLE IF NOT EXISTS story_lines (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    code VARCHAR(32) UNIQUE NOT NULL COMMENT '故事線編碼',
    name_zh VARCHAR(64) NOT NULL COMMENT '中文名稱',
    name_en VARCHAR(64) COMMENT '英文名稱',
    description TEXT COMMENT '描述',
    cover_url VARCHAR(512) COMMENT '封面URL',
    total_chapters INT DEFAULT 0 COMMENT '總章節數',
    status ENUM('draft', 'published', 'archived') DEFAULT 'draft',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    deleted TINYINT DEFAULT 0
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 章節表
CREATE TABLE IF NOT EXISTS story_chapters (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    story_line_id BIGINT NOT NULL,
    chapter_order INT NOT NULL COMMENT '章節順序',
    title_zh VARCHAR(128) NOT NULL,
    media_type VARCHAR(32) NOT NULL COMMENT '視頻/音頻/圖文',
    media_url VARCHAR(256) NOT NULL,
    script_zh TEXT COMMENT '中文劇本',
    script_en TEXT,
    script_zht TEXT,
    unlock_type VARCHAR(32) NOT NULL COMMENT '解鎖條件類型',
    unlock_param VARCHAR(128) COMMENT '解鎖條件參數',
    duration INT COMMENT '時長(秒)',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (story_line_id) REFERENCES story_lines(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 用戶故事進度表
CREATE TABLE IF NOT EXISTS user_story_progress (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT NOT NULL,
    story_line_id BIGINT NOT NULL,
    current_chapter INT DEFAULT 1,
    completed_chapters JSON COMMENT '已完成章節ID列表',
    completed_at TIMESTAMP,
    UNIQUE KEY uk_user_story (user_id, story_line_id),
    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (story_line_id) REFERENCES story_lines(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 獎勵表
CREATE TABLE IF NOT EXISTS rewards (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    name_zh VARCHAR(64) NOT NULL,
    description TEXT,
    stamps_required INT COMMENT '所需印章數',
    total_quantity INT COMMENT '總數量',
    redeemed_count INT DEFAULT 0 COMMENT '已兌換數量',
    start_time TIMESTAMP,
    end_time TIMESTAMP,
    status ENUM('active', 'inactive') DEFAULT 'active',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    deleted TINYINT DEFAULT 0
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 用戶獎勵領取表
CREATE TABLE IF NOT EXISTS user_rewards (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT NOT NULL,
    reward_id BIGINT NOT NULL,
    qr_code VARCHAR(256) COMMENT '二維碼',
    redeemed BOOLEAN DEFAULT FALSE,
    redeemed_at TIMESTAMP,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (reward_id) REFERENCES rewards(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 地圖瓦片配置表
CREATE TABLE IF NOT EXISTS map_tile_configs (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    map_id VARCHAR(32) UNIQUE NOT NULL,
    style VARCHAR(32) DEFAULT 'cartoon',
    cdn_base VARCHAR(256) NOT NULL,
    control_points_url VARCHAR(256) COMMENT '控制點JSON URL',
    pois_url VARCHAR(256) COMMENT 'POI數據URL',
    zoom_levels VARCHAR(128) DEFAULT '[1,2,3,4]',
    status ENUM('active', 'inactive') DEFAULT 'active',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    deleted TINYINT DEFAULT 0
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- =============================================
-- 後台管理系統表結構
-- =============================================

CREATE DATABASE IF NOT EXISTS aoxiaoyou_admin DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
USE aoxiaoyou_admin;

-- 管理員表
CREATE TABLE IF NOT EXISTS sys_admin (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    username VARCHAR(64) UNIQUE NOT NULL,
    password VARCHAR(256) NOT NULL,
    nickname VARCHAR(64),
    email VARCHAR(128),
    phone VARCHAR(32),
    avatar_url VARCHAR(512),
    status ENUM('active', 'disabled') DEFAULT 'active',
    last_login_at TIMESTAMP,
    last_login_ip VARCHAR(64),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    deleted TINYINT DEFAULT 0
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 角色表
CREATE TABLE IF NOT EXISTS sys_role (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    role_code VARCHAR(32) UNIQUE NOT NULL,
    role_name VARCHAR(64) NOT NULL,
    description VARCHAR(256),
    status ENUM('active', 'disabled') DEFAULT 'active',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    deleted TINYINT DEFAULT 0
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 權限表
CREATE TABLE IF NOT EXISTS sys_permission (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    parent_id BIGINT DEFAULT 0,
    name VARCHAR(64) NOT NULL,
    permission_key VARCHAR(128) UNIQUE NOT NULL,
    permission_type ENUM('menu', 'button', 'api') DEFAULT 'menu',
    path VARCHAR(128),
    icon VARCHAR(64),
    sort_order INT DEFAULT 0,
    status ENUM('active', 'disabled') DEFAULT 'active',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    deleted TINYINT DEFAULT 0
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 管理員-角色關聯表
CREATE TABLE IF NOT EXISTS sys_admin_role (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    admin_id BIGINT NOT NULL,
    role_id BIGINT NOT NULL,
    UNIQUE KEY uk_admin_role (admin_id, role_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 角色-權限關聯表
CREATE TABLE IF NOT EXISTS sys_role_permission (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    role_id BIGINT NOT NULL,
    permission_id BIGINT NOT NULL,
    UNIQUE KEY uk_role_permission (role_id, permission_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 登錄日誌表
CREATE TABLE IF NOT EXISTS sys_login_log (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    admin_id BIGINT,
    username VARCHAR(64),
    ip VARCHAR(64),
    user_agent VARCHAR(256),
    login_status ENUM('success', 'failed') DEFAULT 'success',
    fail_reason VARCHAR(256),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_admin (admin_id),
    INDEX idx_created (created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 操作日誌表
CREATE TABLE IF NOT EXISTS sys_operation_log (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    admin_id BIGINT,
    admin_username VARCHAR(64),
    module VARCHAR(64),
    operation VARCHAR(128),
    request_method VARCHAR(16),
    request_url VARCHAR(256),
    request_params TEXT,
    response_data TEXT,
    ip VARCHAR(64),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_admin (admin_id),
    INDEX idx_created (created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 測試賬號標記表
CREATE TABLE IF NOT EXISTS test_accounts (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT NOT NULL UNIQUE COMMENT '關聯小程序用戶ID',
    test_group VARCHAR(32) COMMENT '測試分組',
    mock_latitude DECIMAL(10,8) COMMENT '模擬緯度',
    mock_longitude DECIMAL(11,8) COMMENT '模擬經度',
    mock_enabled BOOLEAN DEFAULT FALSE COMMENT '是否啟用模擬定位',
    mock_poi_id BIGINT COMMENT '模擬觸發的POI',
    notes TEXT COMMENT '備註',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    deleted TINYINT DEFAULT 0,
    INDEX idx_user (user_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 測試操作日誌表
CREATE TABLE IF NOT EXISTS test_operation_logs (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    test_account_id BIGINT NOT NULL,
    operator_id BIGINT NOT NULL COMMENT '操作人管理員ID',
    operation_type VARCHAR(64) NOT NULL COMMENT '操作類型',
    before_data JSON COMMENT '操作前數據',
    after_data JSON COMMENT '操作後數據',
    request_params JSON COMMENT '請求參數',
    ip VARCHAR(64),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_test_account (test_account_id),
    INDEX idx_created (created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 系統配置表
CREATE TABLE IF NOT EXISTS sys_config (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    config_key VARCHAR(128) UNIQUE NOT NULL,
    config_value TEXT,
    config_type VARCHAR(32) DEFAULT 'string',
    description VARCHAR(256),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    deleted TINYINT DEFAULT 0
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 活動表
CREATE TABLE IF NOT EXISTS activities (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    code VARCHAR(32) UNIQUE NOT NULL,
    title VARCHAR(128) NOT NULL,
    description TEXT,
    cover_url VARCHAR(512),
    start_time TIMESTAMP,
    end_time TIMESTAMP,
    status ENUM('draft', 'published', 'ended', 'cancelled') DEFAULT 'draft',
    participation_count INT DEFAULT 0,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    deleted TINYINT DEFAULT 0
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- =============================================
-- 初始數據
-- =============================================

-- 插入默認超級管理員 (密碼: admin123, BCrypt加密)
INSERT INTO sys_admin (username, password, nickname, email, status) VALUES
('admin', '$2a$10$N.zmdr9k7uOCQb376NoUnuTJ8iAt6Z5EHsM8lE9lBOsl7iKTVKIUi', '超級管理員', 'admin@tripofmacau.com', 'active');

-- 插入角色
INSERT INTO sys_role (role_code, role_name, description) VALUES
('super_admin', '超級管理員', '擁有所有權限'),
('content_admin', '內容運營', '內容管理、故事線管理'),
('operation_admin', '運營人員', '活動管理、獎勵配置'),
('tester', '測試人員', '測試工具、數據調整'),
('data_analyst', '數據分析', '數據查看、導出');

-- 故事線初始數據
INSERT INTO story_lines (code, name_zh, name_en, description, status) VALUES
('silk_road', '海上絲路', 'Maritime Silk Road', '探索澳門在海上絲綢之路的歷史角色', 'published'),
('east_west', '東西方戰事', 'East-West Encounter', '澳門四百年的東西方交流史', 'published');

-- 插入默認地圖配置
INSERT INTO map_tile_configs (map_id, style, cdn_base, status) VALUES
('macau_peninsula', 'cartoon', 'https://cdn.tripofmacau.com/maps/macau', 'active');

-- 系統配置初始數據
INSERT INTO sys_config (config_key, config_value, config_type, description) VALUES
('trigger.outdoor.radius.high', '30', 'int', 'GPS精度<10米時觸發半徑(米)'),
('trigger.outdoor.radius.medium', '50', 'int', 'GPS精度10-20米時觸發半徑(米)'),
('trigger.outdoor.radius.low', '80', 'int', 'GPS精度>20米時觸發半徑(米)'),
('trigger.cooldown.time', '1800000', 'int', '觸發冷卻時間(毫秒),默認30分鐘'),
('trigger.debounce.delay', '2000', 'int', '觸發防抖延遲(毫秒)'),
('gps.interval', '2000', 'int', 'GPS採樣間隔(毫秒)'),
('manual.checkin.radius', '200', 'int', '手動補簽有效半徑(米)');
