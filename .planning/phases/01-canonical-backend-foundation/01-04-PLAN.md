---
phase: 01-canonical-backend-foundation
plan: 04
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - docker-compose.local.yml
  - packages/server/src/main/resources/application-local.yml
  - packages/admin/aoxiaoyou-admin-backend/src/main/resources/application-local.yml
  - scripts/local/start-public-backend.cmd
  - scripts/local/start-admin-backend.cmd
  - scripts/local/smoke-phase-01-foundation.ps1
  - README.md
autonomous: true
requirements: [OPS-01]
user_setup: []
must_haves:
  truths:
    - "Local developers can boot MySQL, MongoDB, the public backend, and the admin backend with one documented local-profile flow."
    - "Phase 1 smoke checks probe real public and admin health endpoints and fail fast on datastore or profile misconfiguration."
    - "Local public-backend verification is not blocked by the repo's current lack of a Redis container."
  artifacts:
    - path: "packages/server/src/main/resources/application-local.yml"
      provides: "Local public-backend profile aligned with compose credentials and no-Redis health behavior"
      contains: "management:"
    - path: "scripts/local/start-public-backend.cmd"
      provides: "One-command local launcher for the public backend"
      contains: "SPRING_PROFILES_ACTIVE=local"
    - path: "scripts/local/smoke-phase-01-foundation.ps1"
      provides: "Phase 1 local smoke harness for public/admin foundation checks"
      contains: "/api/v1/health"
    - path: "README.md"
      provides: "Documented local backend foundation flow"
      contains: "smoke-phase-01-foundation.ps1"
  key_links:
    - from: "scripts/local/start-public-backend.cmd"
      to: "packages/server/src/main/resources/application-local.yml"
      via: "shared local profile and environment contract"
      pattern: "SPRING_PROFILES_ACTIVE=local|SERVER_PORT=8080"
    - from: "scripts/local/smoke-phase-01-foundation.ps1"
      to: "packages/server/src/main/java/com/aoxiaoyou/tripofmacau/controller/HealthController.java"
      via: "public health endpoint probe"
      pattern: "8080/api/v1/health"
    - from: "scripts/local/smoke-phase-01-foundation.ps1"
      to: "packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/controller/HealthController.java"
      via: "admin health endpoint probe"
      pattern: "8081/api/v1/health"
---

<objective>
Wire the local runtime and smoke harness for public/admin backend foundation checks.

Purpose: Make Phase 1 executable against real local services instead of leaving runtime verification implicit or blocked by environment drift.
Output: Corrected local compose/profile wiring, a public-backend startup helper, a repeatable Phase 1 smoke script, and README instructions for the shared local flow.
</objective>

<execution_context>
@D:/Archive/trip-of-macau/.codex/get-shit-done/workflows/execute-plan.md
@D:/Archive/trip-of-macau/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-canonical-backend-foundation/01-CONTEXT.md
@.planning/phases/01-canonical-backend-foundation/01-RESEARCH.md
@.planning/phases/01-canonical-backend-foundation/01-VALIDATION.md
@docker-compose.local.yml
@packages/server/src/main/resources/application.yml
@packages/server/src/main/resources/application-dev.yml
@packages/admin/aoxiaoyou-admin-backend/src/main/resources/application.yml
@packages/admin/aoxiaoyou-admin-backend/src/main/resources/application-local.yml
@packages/server/src/main/java/com/aoxiaoyou/tripofmacau/controller/HealthController.java
@packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/controller/HealthController.java
@scripts/local/start-admin-backend.cmd
@README.md
</context>

<threat_model>
<trust_boundaries>
  <boundary>
    <name>Local operator to compose-backed datastores</name>
    <description>Local scripts must connect to the same MySQL and MongoDB instances started by `docker-compose.local.yml`.</description>
    <data>database credentials, Mongo auth settings, health probes</data>
  </boundary>
  <boundary>
    <name>Local operator to backend processes</name>
    <description>Public and admin backend startup helpers define the runtime contract used by Phase 1 smoke verification.</description>
    <data>Spring profile selection, ports, health endpoints, optional dependencies</data>
  </boundary>
</trust_boundaries>
<threats>
  <threat>
    <threat_id>T-01-10</threat_id>
    <category>Denial of Service</category>
    <component>local compose and profile drift</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Fix mismatched local credentials and healthchecks, and make the public/admin start helpers explicitly target the same compose-backed services.</mitigation_plan>
  </threat>
  <threat>
    <threat_id>T-01-11</threat_id>
    <category>Misconfiguration</category>
    <component>public local runtime profile</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Introduce a dedicated public `application-local.yml` that disables Redis health as a Phase 1 blocker while keeping MySQL-backed startup and actuator health verifiable.</mitigation_plan>
  </threat>
  <threat>
    <threat_id>T-01-12</threat_id>
    <category>Repudiation</category>
    <component>local smoke verification</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Use a deterministic PowerShell smoke script that checks exact endpoints and response fields instead of relying on manual "it seems up" verification.</mitigation_plan>
  </threat>
</threats>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Align local compose and profile wiring with the real datastores</name>
  <files>docker-compose.local.yml, packages/server/src/main/resources/application-local.yml, packages/admin/aoxiaoyou-admin-backend/src/main/resources/application-local.yml, scripts/local/start-admin-backend.cmd</files>
  <read_first>docker-compose.local.yml, packages/server/src/main/resources/application.yml, packages/server/src/main/resources/application-dev.yml, packages/admin/aoxiaoyou-admin-backend/src/main/resources/application.yml, packages/admin/aoxiaoyou-admin-backend/src/main/resources/application-local.yml, scripts/local/start-admin-backend.cmd</read_first>
  <action>Create `packages/server/src/main/resources/application-local.yml` with `spring.config.import: optional:file:.env.local[.properties]`, local MySQL defaults matching compose (`jdbc:mysql://127.0.0.1:3306/aoxiaoyou?...connectTimeout=3000&socketTimeout=10000&tcpKeepAlive=true`, username `root`, password `Abc123456`), `server.port: ${SERVER_PORT:8080}`, and `management.health.redis.enabled: false` so `/actuator/health` can report healthy without a local Redis container. Update `packages/admin/aoxiaoyou-admin-backend/src/main/resources/application-local.yml` and `scripts/local/start-admin-backend.cmd` so `MONGODB_URI` defaults to `mongodb://root:root@127.0.0.1:27017/aoxiaoyou_doc?authSource=admin`, matching the compose Mongo root credentials. Fix `docker-compose.local.yml` so the MySQL healthcheck uses the actual root password `Abc123456` instead of `root`, and keep the local compose scope limited to MySQL and MongoDB for Phase 1.</action>
  <verify>powershell -Command "Select-String -Path 'docker-compose.local.yml','packages/server/src/main/resources/application-local.yml','packages/admin/aoxiaoyou-admin-backend/src/main/resources/application-local.yml','scripts/local/start-admin-backend.cmd' -Pattern 'Abc123456|management.health.redis.enabled|mongodb://root:root@127.0.0.1:27017/aoxiaoyou_doc\\?authSource=admin'"</verify>
  <acceptance_criteria>
    - `packages/server/src/main/resources/application-local.yml` exists and contains `management.health.redis.enabled: false`
    - `packages/server/src/main/resources/application-local.yml` contains `jdbc:mysql://127.0.0.1:3306/aoxiaoyou`
    - `packages/admin/aoxiaoyou-admin-backend/src/main/resources/application-local.yml` contains `mongodb://root:root@127.0.0.1:27017/aoxiaoyou_doc?authSource=admin`
    - `scripts/local/start-admin-backend.cmd` contains `MONGODB_URI=mongodb://root:root@127.0.0.1:27017/aoxiaoyou_doc?authSource=admin`
    - `docker-compose.local.yml` contains `-pAbc123456`
  </acceptance_criteria>
  <done>The local profiles, compose healthcheck, and admin start script all point at the same real local credentials and no-Redis Phase 1 behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Add a dedicated public-backend local start helper</name>
  <files>scripts/local/start-public-backend.cmd</files>
  <read_first>scripts/local/start-admin-backend.cmd, packages/server/src/main/resources/application.yml, packages/server/src/main/resources/application-dev.yml, packages/server/src/main/resources/application-local.yml, packages/server/pom.xml</read_first>
  <action>Create `scripts/local/start-public-backend.cmd` mirroring the admin helper style: set `JAVA_HOME=C:\\Program Files\\Java\\jdk-17`, prepend `%JAVA_HOME%\\bin` to `PATH`, export `DB_URL=jdbc:mysql://127.0.0.1:3306/aoxiaoyou?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true&connectTimeout=3000&socketTimeout=10000&tcpKeepAlive=true`, `DB_USERNAME=root`, `DB_PASSWORD=Abc123456`, `SERVER_PORT=8080`, `SPRING_PROFILES_ACTIVE=local`, `JWT_SECRET=please-change-this-secret-in-production`, then `cd /d %~dp0..\\..\\packages\\server` and run `mvn -DskipTests spring-boot:run`. Keep the script single-purpose: it starts the public backend against the local MySQL profile and does not attempt to bootstrap Docker itself.</action>
  <verify>powershell -Command "Select-String -Path 'scripts/local/start-public-backend.cmd' -Pattern 'SPRING_PROFILES_ACTIVE=local|SERVER_PORT=8080|DB_PASSWORD=Abc123456|spring-boot:run'"</verify>
  <acceptance_criteria>
    - `scripts/local/start-public-backend.cmd` exists and contains `SPRING_PROFILES_ACTIVE=local`
    - `scripts/local/start-public-backend.cmd` contains `SERVER_PORT=8080`
    - `scripts/local/start-public-backend.cmd` contains `DB_PASSWORD=Abc123456`
    - `scripts/local/start-public-backend.cmd` contains `mvn -DskipTests spring-boot:run`
  </acceptance_criteria>
  <done>The repo now has a one-command local launcher for the public backend that matches the documented local Phase 1 runtime contract.</done>
</task>

<task type="auto">
  <name>Task 3: Add the Phase 1 smoke script and document the local backend flow</name>
  <files>scripts/local/smoke-phase-01-foundation.ps1, README.md</files>
  <read_first>docker-compose.local.yml, scripts/local/start-public-backend.cmd, scripts/local/start-admin-backend.cmd, packages/server/src/main/java/com/aoxiaoyou/tripofmacau/controller/HealthController.java, packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/controller/HealthController.java, README.md</read_first>
  <action>Create `scripts/local/smoke-phase-01-foundation.ps1` that first runs `docker compose -f docker-compose.local.yml up -d mysql mongodb`, waits for the `trip-of-macau-mysql` and `trip-of-macau-mongo` containers to become healthy or running, then probes these exact URLs with `Invoke-RestMethod`: `http://127.0.0.1:8080/api/v1/health`, `http://127.0.0.1:8080/actuator/health`, and `http://127.0.0.1:8081/api/v1/health`. Fail with non-zero exit if any endpoint is unreachable or if the returned JSON does not report `trip-of-macau-server`, `aoxiaoyou-admin-backend`, or `UP` where expected. Update `README.md` to add a dedicated local backend foundation section that lists this exact sequence: `docker compose -f docker-compose.local.yml up -d mysql mongodb`, `cmd /c scripts\\local\\start-public-backend.cmd`, `cmd /c scripts\\local\\start-admin-backend.cmd`, and `powershell -ExecutionPolicy Bypass -File scripts/local/smoke-phase-01-foundation.ps1`. Explicitly note in the README that Redis is not required for the Phase 1 smoke path and that local Mongo access uses `root:root` with `authSource=admin`.</action>
  <verify>powershell -Command "Select-String -Path 'scripts/local/smoke-phase-01-foundation.ps1','README.md' -Pattern '8080/api/v1/health|8080/actuator/health|8081/api/v1/health|smoke-phase-01-foundation.ps1|authSource=admin|Redis is not required|Phase 1 does not require Redis'"</verify>
  <acceptance_criteria>
    - `scripts/local/smoke-phase-01-foundation.ps1` contains `http://127.0.0.1:8080/api/v1/health`
    - `scripts/local/smoke-phase-01-foundation.ps1` contains `http://127.0.0.1:8080/actuator/health`
    - `scripts/local/smoke-phase-01-foundation.ps1` contains `http://127.0.0.1:8081/api/v1/health`
    - `scripts/local/smoke-phase-01-foundation.ps1` contains `trip-of-macau-server` and `aoxiaoyou-admin-backend`
    - `README.md` contains `smoke-phase-01-foundation.ps1`
    - `README.md` contains either `authSource=admin` or an equivalent explicit Mongo auth note
  </acceptance_criteria>
  <done>The project has a repeatable Phase 1 smoke harness plus documentation for bringing up local stores and both backend services with the same contract.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Local compose/profile wiring reflects the actual MySQL and Mongo credentials used by the repo
- [ ] Public and admin backend start helpers both target `SPRING_PROFILES_ACTIVE=local`
- [ ] `powershell -ExecutionPolicy Bypass -File scripts/local/smoke-phase-01-foundation.ps1` exits 0 after both backend processes are started
</verification>

<success_criteria>

- All tasks completed
- Local backend foundation flow is reproducible and documented
- Public `/api/v1/health`, public `/actuator/health`, and admin `/api/v1/health` are all covered by the Phase 1 smoke harness
- Local Phase 1 verification no longer depends on an unprovisioned Redis container
  </success_criteria>

<output>
After completion, create `.planning/phases/01-canonical-backend-foundation/01-04-SUMMARY.md`
</output>
