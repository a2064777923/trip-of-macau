---
phase: 01-canonical-backend-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - docs/database/live-backend-schema-mapping.md
  - scripts/local/mysql/init/02-live-backend-foundation.sql
  - scripts/local/mysql/init/03-live-backend-seed-scaffold.sql
autonomous: true
requirements: [DATA-01]
user_setup: []
must_haves:
  truths:
    - "The repo contains a repeatable SQL foundation for the live mini-program data model in MySQL."
    - "Schema artifacts cover runtime settings, content, assets, and user-state tables required by later API cutover phases."
  artifacts:
    - path: "scripts/local/mysql/init/02-live-backend-foundation.sql"
      provides: "Core MySQL schema bootstrap for live backend domains"
      min_lines: 150
      contains: "CREATE TABLE IF NOT EXISTS app_runtime_settings"
    - path: "scripts/local/mysql/init/03-live-backend-seed-scaffold.sql"
      provides: "Deterministic seed/bootstrap scaffold"
      contains: "INSERT INTO"
    - path: "docs/database/live-backend-schema-mapping.md"
      provides: "Mapping between current mocks/admin/public contract and SQL tables"
      min_lines: 50
      contains: "## Table Coverage"
  key_links:
    - from: "docs/database/live-backend-schema-mapping.md"
      to: "scripts/local/mysql/init/02-live-backend-foundation.sql"
      via: "documented table/column mapping"
      pattern: "app_runtime_settings|content_assets|user_progress"
---

<objective>
Add the MySQL schema foundation and migration-safe bootstrap scripts.

Purpose: Give later public/admin API phases a real database contract to build against.
Output: One schema-mapping document plus versioned SQL bootstrap and seed-scaffold scripts under `scripts/local/mysql/init`.
</objective>

<execution_context>
@D:/Archive/trip-of-macau/.codex/get-shit-done/workflows/execute-plan.md
@D:/Archive/trip-of-macau/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-canonical-backend-foundation/01-CONTEXT.md
@.planning/phases/01-canonical-backend-foundation/01-RESEARCH.md
@docs/数据库设计-v2.0.md
@docs/cloudbase_mysql_init.sql
@scripts/local/mysql/init/01-init.sql
@packages/client/src/services/gameMock.ts
</context>

<threat_model>
<trust_boundaries>
  <boundary>
    <name>Bootstrap SQL to local MySQL</name>
    <description>Schema bootstrap scripts initialize the data model used by public and admin services.</description>
    <data>table definitions, indexes, baseline seed metadata</data>
  </boundary>
  <boundary>
    <name>Seed scaffold to later migration phases</name>
    <description>Seed scaffolding must support repeatable mock-data import without corrupting production-like state.</description>
    <data>deterministic identifiers, seed metadata, asset references</data>
  </boundary>
</trust_boundaries>
<threats>
  <threat>
    <threat_id>T-01-04</threat_id>
    <category>Tampering</category>
    <component>schema bootstrap scripts</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Use `CREATE TABLE IF NOT EXISTS`, explicit primary keys, and named indexes so repeated local bootstrap runs do not silently diverge.</mitigation_plan>
  </threat>
  <threat>
    <threat_id>T-01-05</threat_id>
    <category>Denial of Service</category>
    <component>table/index design</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Add indexes for publish-state, sort-order, foreign-key lookup, and user-progress paths instead of leaving later phases to query unindexed tables.</mitigation_plan>
  </threat>
  <threat>
    <threat_id>T-01-06</threat_id>
    <category>Repudiation</category>
    <component>seed/migration provenance</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Include bootstrap/seed metadata tables or markers so later migrations can prove what foundational SQL has already run.</mitigation_plan>
  </threat>
</threats>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Author the canonical schema mapping document</name>
  <files>docs/database/live-backend-schema-mapping.md</files>
  <read_first>docs/数据库设计-v2.0.md, docs/cloudbase_mysql_init.sql, scripts/local/mysql/init/01-init.sql, packages/client/src/services/gameMock.ts, docs/integration/miniapp-admin-public-contract.md</read_first>
  <action>Create `docs/database/live-backend-schema-mapping.md` with exact sections `## Table Coverage`, `## Entity-to-Table Mapping`, `## Publish and Locale Columns`, `## Asset Link Strategy`, and `## Deferred Migration Notes`. In `## Table Coverage`, enumerate these exact target tables: `app_runtime_settings`, `content_assets`, `cities`, `pois`, `storylines`, `story_chapters`, `tip_articles`, `rewards`, `stamps`, `notifications`, `user_profiles`, `user_progress`, `user_checkins`, `user_preferences`, and `reward_redemptions`. For each table, state the primary key, key foreign keys, publish/sort columns if applicable, and which mini-program surface it supports.</action>
  <verify>powershell -Command "Select-String -Path 'docs/database/live-backend-schema-mapping.md' -Pattern '## Table Coverage|app_runtime_settings|content_assets|user_progress|reward_redemptions'"</verify>
  <acceptance_criteria>
    - `docs/database/live-backend-schema-mapping.md` contains `## Table Coverage`
    - `docs/database/live-backend-schema-mapping.md` contains all listed target table names
    - `docs/database/live-backend-schema-mapping.md` contains `## Deferred Migration Notes`
  </acceptance_criteria>
  <done>The schema mapping document clearly translates current product/domain needs into target MySQL tables.</done>
</task>

<task type="auto">
  <name>Task 2: Create the live-backend foundation SQL bootstrap</name>
  <files>scripts/local/mysql/init/02-live-backend-foundation.sql</files>
  <read_first>docs/database/live-backend-schema-mapping.md, scripts/local/mysql/init/01-init.sql, packages/server/src/main/resources/application.yml, packages/admin/aoxiaoyou-admin-backend/src/main/resources/application-local.yml</read_first>
  <action>Create `scripts/local/mysql/init/02-live-backend-foundation.sql` with `CREATE TABLE IF NOT EXISTS` statements for the exact tables listed in the schema mapping document. Include these minimum columns: status columns on content tables using `draft/published/archived`, `sort_order` integers on ordered content tables, multilingual `*_zh`, `*_en`, `*_zht` fields on human-facing content tables, `asset_id` or equivalent foreign keys for media-backed records, `created_at` and `updated_at` timestamps, and indexes for `(status, sort_order)`, `city_id`, `storyline_id`, `user_id`, and `setting_group`. Use foreign keys where the current brownfield schema already safely allows them; where it does not, document the relationship in comments and still add lookup indexes.</action>
  <verify>powershell -Command "Select-String -Path 'scripts/local/mysql/init/02-live-backend-foundation.sql' -Pattern 'CREATE TABLE IF NOT EXISTS app_runtime_settings|CREATE TABLE IF NOT EXISTS content_assets|CREATE TABLE IF NOT EXISTS cities|CREATE TABLE IF NOT EXISTS user_progress|INDEX|sort_order'"</verify>
  <acceptance_criteria>
    - `scripts/local/mysql/init/02-live-backend-foundation.sql` contains `CREATE TABLE IF NOT EXISTS app_runtime_settings`
    - `scripts/local/mysql/init/02-live-backend-foundation.sql` contains `CREATE TABLE IF NOT EXISTS content_assets`
    - `scripts/local/mysql/init/02-live-backend-foundation.sql` contains `CREATE TABLE IF NOT EXISTS user_progress`
    - `scripts/local/mysql/init/02-live-backend-foundation.sql` contains `sort_order`
  </acceptance_criteria>
  <done>The core MySQL bootstrap script exists and explicitly covers the canonical live-backend tables and indexing rules.</done>
</task>

<task type="auto">
  <name>Task 3: Add deterministic seed scaffold and bootstrap verification path</name>
  <files>scripts/local/mysql/init/03-live-backend-seed-scaffold.sql</files>
  <read_first>scripts/local/mysql/init/02-live-backend-foundation.sql, packages/client/src/services/gameMock.ts, README.md</read_first>
  <action>Create `scripts/local/mysql/init/03-live-backend-seed-scaffold.sql` that inserts baseline records needed before the full Phase 6 migration: runtime-setting groups for `home`, `discover`, `map`, `tips`, `profile`, and `settings`; asset-kind lookup rows if the schema uses lookup tables; and a bootstrap/seed provenance record structure such as `seed_runs` or equivalent metadata rows. Do not import the full mock dataset yet. Add SQL comments that Phase 6 owns full content backfill. Use verification guidance compatible with `docker compose -f docker-compose.local.yml up -d mysql` and a `docker exec trip-of-macau-mysql mysql -uroot -proot aoxiaoyou -e` style table existence check.</action>
  <verify>powershell -Command "Select-String -Path 'scripts/local/mysql/init/03-live-backend-seed-scaffold.sql' -Pattern 'home|discover|map|tips|profile|settings|seed'"</verify>
  <acceptance_criteria>
    - `scripts/local/mysql/init/03-live-backend-seed-scaffold.sql` contains the setting groups `home`, `discover`, `map`, `tips`, `profile`, and `settings`
    - `scripts/local/mysql/init/03-live-backend-seed-scaffold.sql` contains `seed` provenance language or table usage
    - The seed scaffold explicitly notes that full mock-data migration is deferred to Phase 6
  </acceptance_criteria>
  <done>The schema foundation includes deterministic bootstrap scaffolding without prematurely performing full content migration.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] The schema mapping document covers the exact Phase 1 target tables
- [ ] SQL bootstrap script exists with the required tables and indexing conventions
- [ ] Seed scaffold exists and is explicit about deferred full migration
</verification>

<success_criteria>

- All tasks completed
- MySQL foundation artifacts exist for later public/admin domain implementation
- Schema coverage is explicit and tied to current mini-program surfaces
- Bootstrap SQL remains deterministic and migration-safe for local reruns
  </success_criteria>

<output>
After completion, create `.planning/phases/01-canonical-backend-foundation/01-02-SUMMARY.md`
</output>
