---
phase: 29-poi-default-experience-workbench
plan: 03
type: execute
wave: 3
depends_on:
  - 29-01
  - 29-02
files_modified:
  - scripts/local/mysql/init/40-phase-29-poi-default-experience.sql
  - scripts/local/smoke-phase-29-poi-experience.ps1
  - .planning/phases/29-poi-default-experience-workbench/29-HANDOFF.md
  - .planning/phases/29-poi-default-experience-workbench/29-VERIFICATION.md
autonomous: true
requirements_addressed:
  - STORY-01
  - LINK-01
requirements:
  - STORY-01
  - LINK-01
must_haves:
  truths:
    - A-Ma Temple has a meaningful seeded POI default experience that exercises the workbench.
    - The seed uses UTF-8/utf8mb4 and preserves Traditional Chinese text without PowerShell inline Chinese writes.
    - The live smoke proves admin facade, reusable-template save, and public runtime alignment.
    - Verification evidence distinguishes Phase 29 claims from Phase 30-34 deferred work.
  artifacts:
    - scripts/local/mysql/init/40-phase-29-poi-default-experience.sql
    - scripts/local/smoke-phase-29-poi-experience.ps1
    - .planning/phases/29-poi-default-experience-workbench/29-HANDOFF.md
    - .planning/phases/29-poi-default-experience-workbench/29-VERIFICATION.md
  key_links:
    - SQL seed -> A-Ma Temple default POI flow and templates
    - Smoke script -> admin POI facade and public runtime endpoint
    - Handoff -> Phase 30 storyline override workbench can inherit POI flows
---

<objective>
Seed and verify the A-Ma Temple POI default experience so Phase 29 ends with runnable evidence that the dedicated workbench can author the required POI flow and that the public runtime contract still consumes it correctly.
</objective>

<threat_model>
| Risk ID | ASVS L1 risk | Surface | Mitigation |
| --- | --- | --- | --- |
| T29-03-01 | Encoding corruption in Chinese seed data | SQL and smoke payloads | Use UTF-8 SQL files with `SET NAMES utf8mb4`; use .NET `HttpClient` UTF-8 body handling in PowerShell smoke. |
| T29-03-02 | False-positive frontend-only verification | Phase closeout | Smoke real admin and public endpoints, not just build output. |
| T29-03-03 | Public draft leakage | Public runtime smoke | Assert public runtime steps do not include admin-only `status` fields. |
| T29-03-04 | Seed conflicts with prior Phase 28 rows | SQL idempotency | Use `code` lookups and idempotent upserts/deletes scoped to Phase 29 canonical codes. |
| T29-03-05 | Secrets in scripts | Smoke auth | Read admin/public tokens or credentials from env/local ignored files only; do not hardcode secrets. |
</threat_model>

<tasks>

<task id="29-03-01" type="auto">
<read_first>
- .planning/phases/29-poi-default-experience-workbench/29-CONTEXT.md
- scripts/local/mysql/init/39-phase-28-experience-orchestration.sql
- scripts/local/mysql/init/24-phase-23-reward-seed.sql
- packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/service/impl/AdminPoiExperienceServiceImpl.java
</read_first>
<action>
Create `scripts/local/mysql/init/40-phase-29-poi-default-experience.sql`.

The SQL file must:

- Begin with comments warning that all text is UTF-8/utf8mb4.
- Use `USE aoxiaoyou;` and `SET NAMES utf8mb4;`.
- Resolve `@poi_ama_id` from `pois.code = 'ama_temple'`.
- Resolve or create a default flow with `code='poi_ama_default_walk_in'`, `flow_type='default_poi'`, `mode='walk_in'`, `status='published'`.
- Ensure one binding exists for `owner_type='poi'`, `owner_id=@poi_ama_id`, `binding_role='default_experience_flow'`, `inherit_policy='inherit'`, and that flow ID.
- Seed or update reusable templates for:
  - `tpl_poi_intro_modal`
  - `tpl_poi_route_guidance`
  - `tpl_proximity_fullscreen_media`
  - `tpl_poi_checkin_task_release`
  - `tpl_poi_pickup_side_clues`
  - `tpl_poi_hidden_dwell_achievement`
  - `tpl_poi_completion_reward_title`
- Seed or update these seven steps with `status='published'`, ordered `sort_order` values, meaningful Traditional Chinese names/descriptions, and versioned trigger/condition/effect JSON:
  - `tap_intro`
  - `start_route_guidance`
  - `arrival_intro_media`
  - `release_checkin_tasks`
  - `pickup_side_clues`
  - `hidden_dwell_achievement`
  - `completion_reward_title`
- Use semantic exploration weights, not fixed percent strings:
  - `tap_intro` -> `tiny`
  - `start_route_guidance` -> `tiny`
  - `arrival_intro_media` -> `small`
  - `release_checkin_tasks` -> `medium`
  - `pickup_side_clues` -> `large`
  - `hidden_dwell_achievement` -> `core`
  - `completion_reward_title` -> `core`
- Seed exploration elements for at least `ama_poi_arrival_intro`, `ama_checkin_tasks_released`, `ama_side_clues_collected`, `ama_hidden_dwell_achievement`, and `ama_completion_reward_title`.

Make the SQL idempotent. Do not use inline PowerShell to write Chinese data.
</action>
<verify>
<grep>powershell -NoProfile -Command "Select-String -Path 'scripts/local/mysql/init/40-phase-29-poi-default-experience.sql' -Pattern 'SET NAMES utf8mb4|poi_ama_default_walk_in|default_experience_flow|tpl_poi_intro_modal|tpl_poi_route_guidance|tpl_poi_hidden_dwell_achievement|tap_intro|start_route_guidance|arrival_intro_media|release_checkin_tasks|pickup_side_clues|hidden_dwell_achievement|completion_reward_title|schemaVersion|ama_hidden_dwell_achievement|core'"</grep>
</verify>
<acceptance_criteria>
- The seed file exists and includes `SET NAMES utf8mb4`.
- The seed file includes all seven template codes and all seven step codes.
- The seed file includes `schemaVersion` in trigger, condition, and effect JSON payloads.
- The seed file includes semantic weights `tiny`, `small`, `medium`, `large`, and `core`.
- The seed file is idempotent through code-based lookups or upserts rather than blind duplicate inserts.
</acceptance_criteria>
</task>

<task id="29-03-02" type="auto">
<read_first>
- .planning/phases/29-poi-default-experience-workbench/29-VALIDATION.md
- scripts/local/smoke-phase-28-experience.ps1
- packages/admin/aoxiaoyou-admin-ui/src/services/api.ts
- packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/controller/AdminPoiExperienceController.java
- packages/server/src/main/java/com/aoxiaoyou/tripofmacau/controller/ExperienceController.java
</read_first>
<action>
Create `scripts/local/smoke-phase-29-poi-experience.ps1` by following the Phase 28 smoke script's explicit UTF-8 `HttpClient` pattern.

The script must:

- Accept optional parameters `AdminBaseUrl`, `PublicBaseUrl`, `Locale`, and `PoiId`, defaulting to `http://127.0.0.1:8081`, `http://127.0.0.1:8080`, `zh-Hant`, and `9`.
- Read admin auth from `PHASE29_ADMIN_BEARER`, `PHASE29_ADMIN_USERNAME`/`PHASE29_ADMIN_PASSWORD`, or local `tmp-admin-login.json`.
- Read public auth from `PHASE29_PUBLIC_BEARER` or use dev-bypass only when available, matching Phase 28's pattern.
- Call `GET /api/admin/v1/pois/{PoiId}/experience/default` and assert:
  - `publicRuntimePath` equals `/api/v1/experience/poi/{PoiId}` or contains that path.
  - `flow.flowType` is `default_poi`.
  - `flow.mode` is `walk_in`.
  - `binding.ownerType` is `poi`.
  - `binding.bindingRole` is `default_experience_flow`.
- Assert the admin snapshot contains all seven canonical step codes.
- Call `POST /api/admin/v1/pois/{PoiId}/experience/steps/{stepId}/save-template` for one step using a generated template code such as `phase29_smoke_saved_template`.
- Assert `GET /api/admin/v1/experience/templates?keyword=phase29_smoke_saved_template` returns that template.
- Call `GET /api/v1/experience/poi/{PoiId}?locale={Locale}`.
- Assert public runtime includes all seven canonical step codes.
- Assert public runtime does not expose admin-only `status` fields on the flow or steps.
- Print `Phase 29 POI experience smoke passed` on success.

Do not hardcode credentials or secrets in the script.
</action>
<verify>
<automated>powershell -NoProfile -ExecutionPolicy Bypass -File scripts/local/smoke-phase-29-poi-experience.ps1</automated>
<grep>powershell -NoProfile -Command "Select-String -Path 'scripts/local/smoke-phase-29-poi-experience.ps1' -Pattern 'PHASE29_ADMIN_BEARER|PHASE29_PUBLIC_BEARER|HttpClient|UTF8|/api/admin/v1/pois/\\$\\(\\$PoiId\\)/experience/default|save-template|/api/v1/experience/poi|tap_intro|start_route_guidance|arrival_intro_media|release_checkin_tasks|pickup_side_clues|hidden_dwell_achievement|completion_reward_title|Assert-NoStatusField|Phase 29 POI experience smoke passed'"</grep>
</verify>
<acceptance_criteria>
- The smoke script exists and uses explicit UTF-8 `HttpClient` request/response handling.
- The smoke script reads credentials only from environment variables or local ignored login material.
- The smoke script checks the admin POI facade, save-template behavior, and public runtime endpoint.
- The smoke script asserts all seven canonical step codes in admin and public runtime responses.
- `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/local/smoke-phase-29-poi-experience.ps1` succeeds against the local stack.
</acceptance_criteria>
</task>

<task id="29-03-03" type="auto">
<read_first>
- .planning/phases/29-poi-default-experience-workbench/29-CONTEXT.md
- .planning/phases/29-poi-default-experience-workbench/29-RESEARCH.md
- .planning/phases/29-poi-default-experience-workbench/29-VALIDATION.md
- .planning/phases/28-story-and-content-control-plane-completion/28-HANDOFF.md
- .planning/phases/28-story-and-content-control-plane-completion/28-VERIFICATION.md
- scripts/local/smoke-phase-29-poi-experience.ps1
</read_first>
<action>
After implementation and verification, write Phase 29 closeout evidence:

- Create `29-HANDOFF.md` with status `foundation-handoff`, delivered artifacts, decisions carried forward to Phase 30, and explicit non-claims.
- Create `29-VERIFICATION.md` with status `passed` only if the backend compile, UI build, seed/source checks, and smoke script pass.
- In `29-HANDOFF.md`, include a table showing:
  - `POI default flow authoring` -> Phase 30 storyline chapters can inherit it.
  - `POI step template saving` -> Phase 31 governance can aggregate usage.
  - `semantic exploration weights` -> Phase 32 progress model can calculate denominator.
  - `A-Ma Temple seeded default flow` -> Phase 33 flagship content can reuse or override it.
  - `public POI runtime path` -> Phase 34 mini-program baseline can consume it.
- In `29-VERIFICATION.md`, list exact commands run and their results.
</action>
<verify>
<grep>powershell -NoProfile -Command "Select-String -Path '.planning/phases/29-poi-default-experience-workbench/29-HANDOFF.md','.planning/phases/29-poi-default-experience-workbench/29-VERIFICATION.md' -Pattern 'POI default flow authoring|Phase 30|Phase 31|Phase 32|Phase 33|Phase 34|smoke-phase-29-poi-experience.ps1|STORY-01|LINK-01|passed'"</grep>
</verify>
<acceptance_criteria>
- `29-HANDOFF.md` exists and names the Phase 30-34 follow-on consumers.
- `29-HANDOFF.md` explicitly states Phase 29 does not implement storyline chapter overrides, cross-domain governance, full user progress UI, flagship material generation, or mini-program UX acceptance.
- `29-VERIFICATION.md` exists and lists backend compile, admin UI build, seed/source checks, and `smoke-phase-29-poi-experience.ps1`.
- `Select-String` finds `STORY-01`, `LINK-01`, `Phase 30`, `Phase 31`, `Phase 32`, `Phase 33`, and `Phase 34` in the closeout docs.
</acceptance_criteria>
</task>

</tasks>

<verification>
- Run `mvn -q -DskipTests compile -f packages/admin/aoxiaoyou-admin-backend/pom.xml`.
- Run `npm run build --prefix packages/admin/aoxiaoyou-admin-ui`.
- Run `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/local/smoke-phase-29-poi-experience.ps1`.
- Run the task-level `Select-String` checks for seed content, smoke coverage, and closeout evidence.
</verification>

<must_haves>
- A-Ma Temple default experience seed exercises all Phase 29 POI workbench categories.
- Smoke verifies admin POI facade, reusable-template save, and public runtime alignment.
- No Chinese text is written through unsafe inline PowerShell literals.
- Closeout docs make Phase 29 claims and non-claims explicit for Phase 30-34.
</must_haves>

<success_criteria>
- Seed source checks pass.
- Backend compile and admin UI build pass.
- Live smoke passes against local admin backend on 8081 and public backend on 8080.
- Handoff and verification docs are ready for `/gsd-verify-work`.
</success_criteria>
