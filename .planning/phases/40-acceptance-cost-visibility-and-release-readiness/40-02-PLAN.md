---
phase: 40
plan: 40-02
type: execute
wave: 2
depends_on:
  - 40-01
files_modified:
  - packages/admin/aoxiaoyou-admin-ui/src/pages/AiCapabilityCenter/ObservabilityPage.tsx
  - packages/admin/aoxiaoyou-admin-ui/src/services/api.ts
  - packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/controller/AdminAiController.java
  - packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/service/AdminAiService.java
  - packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/service/impl/AdminAiServiceImpl.java
  - packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/dto/response/AdminAiGenerationJobResponse.java
  - packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/dto/response/AdminAiLogResponse.java
autonomous: true
requirements:
  - ACC-03
must_haves:
  truths:
    - "Operators can inspect generation jobs and request logs with provider, model, capability, owner, status, timing, asset linkage, and cost values."
    - "Default admin views show readable Traditional Chinese summaries, not raw JSON-first logs."
    - "Prompt text, request payload JSON, provider request IDs, and other sensitive diagnostics are not exposed in the default table view."
    - "Cost values are clearly labeled as estimated, actual, or provider-unavailable."
  artifacts:
    - path: packages/admin/aoxiaoyou-admin-ui/src/pages/AiCapabilityCenter/ObservabilityPage.tsx
      provides: "Admin monitoring and cost visibility UI"
      contains: "生成作業"
    - path: packages/admin/aoxiaoyou-admin-ui/src/services/api.ts
      provides: "Typed AI generation job/log detail contracts"
      contains: "getAiGenerationJobDetail"
    - path: packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/controller/AdminAiController.java
      provides: "Admin AI history/detail endpoints"
      contains: "/generation-jobs/{jobId}"
  key_links:
    - from: "ObservabilityPage.tsx"
      to: "GET /api/admin/v1/ai/generation-jobs"
      via: "generation jobs table"
      pattern: "getAiGenerationJobs"
    - from: "ObservabilityPage.tsx"
      to: "GET /api/admin/v1/ai/logs"
      via: "request log table"
      pattern: "getAiLogs"
    - from: "AdminAiServiceImpl.java"
      to: "AiGenerationJob"
      via: "role-aware job access"
      pattern: "canAccessJob"
---

<objective>
Improve admin-visible generation job, request log, and cost visibility for v3.1 acceptance.

Purpose: operators need to inspect who generated what, through which provider/model, with what status/cost/error/asset result, without exposing API keys or raw secret-bearing diagnostics.
Output: a more usable `監控與成本` page and safe backend/frontend contracts for job/log detail display.
</objective>

<execution_context>
@D:/Archive/trip-of-macau/.codex/get-shit-done/workflows/execute-plan.md
@D:/Archive/trip-of-macau/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/40-acceptance-cost-visibility-and-release-readiness/40-CONTEXT.md
@.planning/phases/40-acceptance-cost-visibility-and-release-readiness/40-RESEARCH.md
@.planning/phases/40-acceptance-cost-visibility-and-release-readiness/40-UI-SPEC.md
@packages/admin/aoxiaoyou-admin-ui/src/pages/AiCapabilityCenter/ObservabilityPage.tsx
@packages/admin/aoxiaoyou-admin-ui/src/pages/AiCapabilityCenter/catalog.tsx
@packages/admin/aoxiaoyou-admin-ui/src/services/api.ts
@packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/controller/AdminAiController.java
@packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/service/AdminAiService.java
@packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/service/impl/AdminAiServiceImpl.java
@packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/dto/response/AdminAiGenerationJobResponse.java
@packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/dto/response/AdminAiLogResponse.java
</context>

<tasks>

<task id="40-02-01" type="auto" tdd="true">
<name>Extend admin AI history API with safe summaries and filters</name>
<files>
- packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/controller/AdminAiController.java
- packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/service/AdminAiService.java
- packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/service/impl/AdminAiServiceImpl.java
- packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/dto/response/AdminAiGenerationJobResponse.java
- packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/dto/response/AdminAiLogResponse.java
</files>
<read_first>
- packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/controller/AdminAiController.java
- packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/service/AdminAiService.java
- packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/service/impl/AdminAiServiceImpl.java
- packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/dto/response/AdminAiGenerationJobResponse.java
- packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/dto/response/AdminAiLogResponse.java
- packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/entity/AiGenerationJob.java
- packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/entity/AiRequestLog.java
</read_first>
<behavior>
- Existing role-aware generation job visibility remains: super/admin can see all jobs, non-super operators see accessible jobs.
- Request log paging can be filtered by provider, capability, success, and optionally owner/model/request type where data exists.
- Default summaries avoid showing full prompt text, full request payload JSON, API keys, bearer tokens, or COS secrets.
- Detail endpoint may keep restricted diagnostic fields if existing authorized workflows require them, but the DTO must include safe summary fields for default UI.
</behavior>
<action>
Implement backend support for the Phase 40 observability UI without removing existing endpoints.

In `AdminAiGenerationJobResponse.java`, add fields:
```java
private String costLabel;
private String costType;
private String safePromptSummary;
private String safeRequestSummary;
private Integer candidateCount;
private String latestAssetUrl;
private String latestAssetKind;
private String latestAssetName;
```

In `AdminAiLogResponse.java`, add fields:
```java
private String modelCode;
private String costLabel;
private String costType;
private String safeOutputSummary;
```

In `AdminAiController.pageLogs`, add optional request parameters:
```java
@RequestParam(required = false) String requestType,
@RequestParam(required = false) Long adminOwnerId,
@RequestParam(required = false) String inventoryCode
```

In `AdminAiService.pageLogs`, add matching parameters.

In `AdminAiServiceImpl.pageLogs`, apply filters when values are present:
```text
AiRequestLog::getRequestType == requestType
AiRequestLog::getAdminOwnerId == adminOwnerId
AiRequestLog::getInventoryCode == inventoryCode
```

In `AdminAiServiceImpl.mapLog`, set:
```text
modelCode = inventoryCode
costLabel = costUsd == null ? "供應商未返回" : "US$" + costUsd
costType = costUsd == null ? "provider_unavailable" : "estimated"
safeOutputSummary = trimTo(defaultString(outputSummary, errorMessage), 180)
```

In `AdminAiServiceImpl.mapJob`, compute safe fields:
```text
safePromptSummary = trimTo(promptTitle if present else resultSummary if present else generationType, 180)
safeRequestSummary = trimTo(resultSummary if present else errorMessage, 180)
candidateCount = candidates count for the job
latestAssetUrl/latestAssetKind/latestAssetName = values from latest candidate if available
costLabel = "供應商未返回"
costType = "provider_unavailable"
```

Do not include API key, decrypted provider credential, bearer token, or COS secret fields in any response.
</action>
<acceptance_criteria>
- `AdminAiGenerationJobResponse.java` contains `safePromptSummary`.
- `AdminAiGenerationJobResponse.java` contains `candidateCount`.
- `AdminAiGenerationJobResponse.java` contains `latestAssetUrl`.
- `AdminAiGenerationJobResponse.java` contains `costType`.
- `AdminAiLogResponse.java` contains `safeOutputSummary`.
- `AdminAiLogResponse.java` contains `modelCode`.
- `AdminAiController.java` contains `requestType`.
- `AdminAiService.java` contains `inventoryCode`.
- `AdminAiServiceImpl.java` contains `safePromptSummary`.
- `AdminAiServiceImpl.java` contains `provider_unavailable`.
- `mvn -q -DskipTests compile -f packages/admin/aoxiaoyou-admin-backend/pom.xml` exits `0`.
</acceptance_criteria>
<verify>
  <automated>mvn -q -DskipTests compile -f packages/admin/aoxiaoyou-admin-backend/pom.xml</automated>
</verify>
<done>
Backend AI history APIs expose the safe summary data needed by the Phase 40 admin UI.
</done>
</task>

<task id="40-02-02" type="auto" tdd="true">
<name>Upgrade monitoring and cost UI with jobs, logs, filters, and detail drawer</name>
<files>
- packages/admin/aoxiaoyou-admin-ui/src/pages/AiCapabilityCenter/ObservabilityPage.tsx
- packages/admin/aoxiaoyou-admin-ui/src/services/api.ts
</files>
<read_first>
- packages/admin/aoxiaoyou-admin-ui/src/pages/AiCapabilityCenter/ObservabilityPage.tsx
- packages/admin/aoxiaoyou-admin-ui/src/pages/AiCapabilityCenter/catalog.tsx
- packages/admin/aoxiaoyou-admin-ui/src/services/api.ts
- .planning/phases/40-acceptance-cost-visibility-and-release-readiness/40-UI-SPEC.md
</read_first>
<behavior>
- The page remains under `/ai/observability` and uses Traditional Chinese copy.
- Operators can view recent generation jobs and request logs on the same page.
- Long URLs, object keys, request IDs, material keys, and error messages do not overflow table cells.
- Detail drawer shows readable summaries by default and puts diagnostic JSON behind a collapsed advanced section.
- Loading states disable repeated detail refresh/open operations where feasible.
</behavior>
<action>
In `packages/admin/aoxiaoyou-admin-ui/src/services/api.ts`, update interfaces:

Add these optional fields to `AiGenerationJobItem`:
```ts
costLabel?: string;
costType?: 'estimated' | 'actual' | 'provider_unavailable' | string;
safePromptSummary?: string;
safeRequestSummary?: string;
candidateCount?: number;
latestAssetUrl?: string;
latestAssetKind?: string;
latestAssetName?: string;
```

Add these optional fields to `AiLogItem`:
```ts
modelCode?: string;
costLabel?: string;
costType?: 'estimated' | 'actual' | 'provider_unavailable' | string;
safeOutputSummary?: string;
```

Extend `getAiLogs` params with:
```ts
requestType?: string;
adminOwnerId?: number;
inventoryCode?: string;
```

In `ObservabilityPage.tsx`, implement these UI sections:
- Header card with title `監控與成本` and subtitle from `40-UI-SPEC.md`.
- Summary cards for `24h 估算成本`, `24h 請求數`, `24h 失敗`, `待處理生成作業`, `已產出素材`, `最近一次錯誤`.
- Filter panel with visible labels: `能力`, `供應商`, `模型`, `狀態`, `作業類型`, `操作人`, `只看失敗`, `只看有素材`.
- `生成作業` table using `getAiGenerationJobs`, columns `時間`, `操作人`, `能力`, `作業類型`, `供應商`, `模型`, `狀態`, `素材包 / 目標`, `輸出素材`, `耗時`, `成本`, `操作`.
- `請求日誌` table using `getAiLogs`, columns `時間`, `操作人`, `能力`, `供應商`, `模型`, `成功`, `耗時`, `成本`, `摘要`, `操作`.
- Detail drawer title `查看詳情` with sections `基本資訊`, `供應商與模型`, `輸入摘要`, `輸出摘要`, `成本與用量`, `素材與版本`, `錯誤與重試`, `安全檢查`, `進階診斷`.

Use `Typography.Text` with `ellipsis={{ tooltip: value }}` or Ant Design `Tooltip` for long values. Add `scroll={{ x: 1200 }}` or wider on both tables.

Default table/detail rendering must use:
```text
safePromptSummary
safeRequestSummary
safeOutputSummary
costLabel
costType
latestAssetName
latestAssetKind
candidateCount
```

Do not show `promptText`, `requestPayloadJson`, or `promptVariablesJson` in the default table. If advanced diagnostics are shown, place them under `Collapse` item `進階診斷`.
</action>
<acceptance_criteria>
- `ObservabilityPage.tsx` contains `監控與成本`.
- `ObservabilityPage.tsx` contains `生成作業`.
- `ObservabilityPage.tsx` contains `請求日誌`.
- `ObservabilityPage.tsx` contains `成本與用量`.
- `ObservabilityPage.tsx` contains `safePromptSummary`.
- `ObservabilityPage.tsx` contains `safeOutputSummary`.
- `ObservabilityPage.tsx` contains `進階診斷`.
- `ObservabilityPage.tsx` contains `ellipsis`.
- `ObservabilityPage.tsx` contains `scroll={{ x:`.
- `ObservabilityPage.tsx` does not render `promptText` in a table column title or default cell.
- `api.ts` contains `costLabel`.
- `api.ts` contains `safeRequestSummary`.
- `cd packages/admin/aoxiaoyou-admin-ui; npm run build` exits `0`.
</acceptance_criteria>
<verify>
  <automated>cd packages/admin/aoxiaoyou-admin-ui; npm run build</automated>
</verify>
<done>
Operators can inspect AI generation jobs, request logs, cost labels, status, errors, and asset linkage through a usable Traditional Chinese monitoring page.
</done>
</task>

<task id="40-02-03" type="auto" tdd="true">
<name>Add admin API smoke coverage for cost/history visibility</name>
<files>
- scripts/local/smoke-phase-40-release-readiness.ps1
- .planning/phases/40-acceptance-cost-visibility-and-release-readiness/40-SMOKE-REPORT.md
</files>
<read_first>
- scripts/local/smoke-phase-40-release-readiness.ps1
- packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/controller/AdminAiController.java
- packages/admin/aoxiaoyou-admin-ui/src/services/api.ts
</read_first>
<behavior>
- Phase 40 smoke verifies the AI observability API shape when admin backend is available.
- The smoke checks safe summary fields and cost labels without printing job payloads.
- Missing admin credentials are reported as `BLOCKED` or `SKIP` with exact env guidance.
</behavior>
<action>
Extend `scripts/local/smoke-phase-40-release-readiness.ps1` with an AI observability API check.

Use env/config:
```text
PHASE40_ADMIN_BASE_URL default http://127.0.0.1:8081
PHASE40_ADMIN_BEARER_TOKEN
PHASE40_ADMIN_USERNAME default admin
PHASE40_ADMIN_PASSWORD default admin123
PHASE40_SKIP_ADMIN_AUTH
```

If `PHASE40_SKIP_ADMIN_AUTH=true`, record `SKIP` for AI observability API with evidence `PHASE40_SKIP_ADMIN_AUTH=true`.

Otherwise:
1. Obtain admin bearer token from `PHASE40_ADMIN_BEARER_TOKEN` or login through `POST /api/admin/v1/auth/login`.
2. Call `GET /api/admin/v1/ai/overview`.
3. Call `GET /api/admin/v1/ai/generation-jobs?pageNum=1&pageSize=5`.
4. Call `GET /api/admin/v1/ai/logs?pageNum=1&pageSize=5`.
5. Assert responses are JSON envelopes with `code` equal to `0` or `200`.
6. If generation job rows exist, assert serialized rows contain `jobStatus`, `ownerAdminName`, and `costType` or record a warning in evidence if `costType` is absent before backend task lands.
7. If log rows exist, assert serialized rows contain `costUsd` or `costLabel` and `success`.
8. Run `Assert-NoSecretText` against serialized overview/job/log summaries before writing evidence.

Never write the admin token to console or `40-SMOKE-REPORT.md`.
</action>
<acceptance_criteria>
- `smoke-phase-40-release-readiness.ps1` contains `PHASE40_ADMIN_BASE_URL`.
- `smoke-phase-40-release-readiness.ps1` contains `/api/admin/v1/ai/overview`.
- `smoke-phase-40-release-readiness.ps1` contains `/api/admin/v1/ai/generation-jobs`.
- `smoke-phase-40-release-readiness.ps1` contains `/api/admin/v1/ai/logs`.
- `smoke-phase-40-release-readiness.ps1` contains `PHASE40_SKIP_ADMIN_AUTH`.
- `40-SMOKE-REPORT.md` does not contain `Bearer ` after smoke execution.
- `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/local/smoke-phase-40-release-readiness.ps1 -Quick` exits `0` or records a clear blocked admin service state.
</acceptance_criteria>
<verify>
  <automated>powershell -NoProfile -ExecutionPolicy Bypass -File scripts/local/smoke-phase-40-release-readiness.ps1 -Quick</automated>
</verify>
<done>
Phase 40 smoke includes admin cost/history API visibility checks.
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| admin backend -> admin UI | AI job/log records may include sensitive prompt, request, provider, and asset metadata. |
| operator browser -> detail drawer | Default UI must show safe summaries while keeping diagnostics clearly separated. |
| admin auth -> job visibility | Role-aware backend checks decide whether an operator can see all jobs or only owned jobs. |
| smoke script -> admin AI endpoints | Smoke may use admin credentials and must not print tokens or payload secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T40-06 | Information Disclosure | ObservabilityPage tables | mitigate | Default tables render safe summary fields and omit full prompt/request JSON columns. |
| T40-07 | Information Disclosure | detail drawer | mitigate | Place diagnostic JSON behind `進階診斷`; do not render API keys, bearer tokens, COS secrets, or decrypted credentials. |
| T40-08 | Elevation of Privilege | generation job history | mitigate | Preserve `canAccessJob` role checks and non-super owner filtering. |
| T40-09 | Repudiation | generation job UI | mitigate | Display owner, status, provider/model, timestamp, cost label, and error summary. |
| T40-10 | Information Disclosure | smoke report | mitigate | Sanitize admin AI API output with `Assert-NoSecretText` before writing report. |
</threat_model>

<verification>
Run:

```powershell
mvn -q -DskipTests compile -f packages/admin/aoxiaoyou-admin-backend/pom.xml
cd packages/admin/aoxiaoyou-admin-ui
npm run build
cd ..\..\..
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/local/smoke-phase-40-release-readiness.ps1 -Quick
```
</verification>

<success_criteria>
- `ACC-03` has admin UI and API evidence.
- Operators can view generation jobs and request logs with cost/status/provider/model/owner fields.
- Long values are ellipsized or moved into detail drawers.
- Secret-bearing diagnostics are not default table content.
</success_criteria>

<output>
After completion, create `.planning/phases/40-acceptance-cost-visibility-and-release-readiness/40-02-SUMMARY.md`.
</output>
