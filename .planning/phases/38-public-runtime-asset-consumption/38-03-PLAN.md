---
phase: 38
plan: 38-03
type: execute
wave: 3
depends_on:
  - 38-01
  - 38-02
files_modified:
  - scripts/local/smoke-phase-38-public-runtime-assets.ps1
  - .planning/REQUIREMENTS.md
  - .planning/phases/38-public-runtime-asset-consumption/38-VERIFICATION.md
autonomous: true
requirements:
  - RUN-01
  - RUN-02
  - RUN-03
  - RUN-04
must_haves:
  truths:
    - "A repeatable local smoke verifies flagship runtime asset chain, fallback/lifecycle filtering, banned-field absence, event idempotency, exploration, and session exit."
    - "Requirements traceability marks RUN-01 through RUN-04 complete only after local verification passes."
    - "Smoke scripts do not print or commit provider keys, COS secrets, bearer tokens, or raw sensitive provenance."
  artifacts:
    - path: scripts/local/smoke-phase-38-public-runtime-assets.ps1
      provides: "Public runtime asset/event smoke"
      contains: "Phase 38 public runtime asset smoke passed"
    - path: .planning/phases/38-public-runtime-asset-consumption/38-VERIFICATION.md
      provides: "Phase 38 verification evidence"
      contains: "RUN-01"
    - path: .planning/REQUIREMENTS.md
      provides: "Traceability updates for RUN-01 through RUN-04"
      contains: "RUN-04 | Phase 38 | Complete"
  key_links:
    - from: "smoke-phase-38-public-runtime-assets.ps1"
      to: "GET /api/v1/storylines/{id}/runtime"
      via: "flagship story runtime check"
      pattern: "/runtime?locale="
    - from: "smoke-phase-38-public-runtime-assets.ps1"
      to: "POST /api/v1/storylines/{id}/sessions/{sessionId}/events"
      via: "authenticated idempotent story event checks"
      pattern: "media_completed"
---

<objective>
Add Phase 38 smoke verification and update traceability after public runtime asset/event behavior is proven.

Purpose: Phase 38 closes only when the local public backend can load the flagship generated asset chain safely, hide unsafe fields, degrade unavailable media, and record baseline story events idempotently.
Output: a repeatable smoke script, verification report, and `RUN-01` through `RUN-04` traceability updates.
</objective>

<execution_context>
@D:/Archive/trip-of-macau/.codex/get-shit-done/workflows/execute-plan.md
@D:/Archive/trip-of-macau/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-public-runtime-asset-consumption/38-CONTEXT.md
@.planning/phases/38-public-runtime-asset-consumption/38-RESEARCH.md
@.planning/phases/38-public-runtime-asset-consumption/38-VALIDATION.md
@.planning/phases/36-material-production-pipeline-and-asset-promotion/36-VERIFICATION.md
@.planning/phases/37-material-qa-workspace-and-reuse-controls/37-VERIFICATION.md
@scripts/local/smoke-phase-34-public-runtime.ps1
@scripts/local/smoke-phase-36-material-production.ps1
@scripts/local/smoke-phase-37-material-qa.ps1
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task id="38-03-01" type="smoke">
<name>Add Phase 38 public runtime asset smoke script</name>
<files>
- scripts/local/smoke-phase-38-public-runtime-assets.ps1
</files>
<read_first>
- scripts/local/smoke-phase-34-public-runtime.ps1
- scripts/local/smoke-phase-36-material-production.ps1
- scripts/local/smoke-phase-37-material-qa.ps1
- .planning/phases/38-public-runtime-asset-consumption/38-RESEARCH.md
</read_first>
<action>
Create `scripts/local/smoke-phase-38-public-runtime-assets.ps1`.

Use UTF-8 output setup from Phase 34 smoke:
- `$ErrorActionPreference = 'Stop'`
- `[Console]::OutputEncoding = [System.Text.Encoding]::UTF8`
- `$OutputEncoding = [System.Text.Encoding]::UTF8`

Accept parameters:
- `PublicBaseUrl`
- `Locale`
- `MySqlExe`
- `MySqlHost`
- `MySqlPort`
- `MySqlDatabase`
- `MySqlUser`
- `MySqlPassword`

Resolve defaults from `PHASE38_PUBLIC_BASE_URL`, `PHASE38_MYSQL_*`, `DB_*`, `MYSQL_PWD`, and local `application-local.yml`, following the Phase 34 smoke pattern.

Import or assume these seed files in order when present:
- `scripts/local/mysql/init/47-phase-33-story-material-package-model.sql`
- `scripts/local/mysql/init/48-phase-33-flagship-material-assets.sql`
- `scripts/local/mysql/init/49-phase-33-east-west-flagship-story.sql`
- `scripts/local/mysql/init/51-phase-36-material-production-versioning.sql`

Fetch the flagship story from `GET /api/v1/story-lines?locale={Locale}` by code `east_west_war_and_coexistence`.

Fetch runtime from `GET /api/v1/storylines/{id}/runtime?locale={Locale}` and assert:
- `runtimeVersion = "v1"`
- at least five chapters exist
- at least one chapter has `compiledSteps`
- at least one content block has `primaryAsset` or `attachmentAssets`
- at least one media asset in the whole runtime has property `availability`
- at least one media asset has `runtimeKind = "image"` or `assetKind = "image"`
- at least one media asset has `runtimeKind = "audio"` or `assetKind = "audio"`
- at least one media asset has `runtimeKind = "video"` or `assetKind = "video"`
- Lottie-capable metadata is present when any `assetKind = "lottie"` or `blockType = "lottie"` exists

Serialize the runtime response and assert it does not contain banned field names:
- `promptText`
- `scriptText`
- `localPath`
- `providerApiKey`
- `apiKey`
- `secret`
- `estimatedCost`
- `actualCost`
- `qaNote`
- `cosObjectKey`

If `PHASE38_TRAVELER_BEARER_TOKEN` is set, run authenticated checks:
- Start session.
- POST `media_completed` with a stable `clientEventId`; repeat it and assert the same `eventId` or `duplicate = true`.
- POST `pickup_interacted`.
- POST `task_completed`.
- POST `reward_acquired`.
- GET `/api/v1/users/me/exploration?locale={Locale}&scopeType=storyline&scopeId={storylineId}` and assert `scopeType = "storyline"`.
- POST session exit twice and assert second exit does not fail.

If no traveler token is set, print a clear skip line: `Skipping authenticated Phase 38 event checks because PHASE38_TRAVELER_BEARER_TOKEN is not set`.

On success, print exactly `Phase 38 public runtime asset smoke passed`.
</action>
<acceptance_criteria>
- `smoke-phase-38-public-runtime-assets.ps1` contains `PHASE38_PUBLIC_BASE_URL`.
- `smoke-phase-38-public-runtime-assets.ps1` contains `east_west_war_and_coexistence`.
- `smoke-phase-38-public-runtime-assets.ps1` contains `availability`.
- `smoke-phase-38-public-runtime-assets.ps1` contains `promptText`.
- `smoke-phase-38-public-runtime-assets.ps1` contains `media_completed`.
- `smoke-phase-38-public-runtime-assets.ps1` contains `pickup_interacted`.
- `smoke-phase-38-public-runtime-assets.ps1` contains `reward_acquired`.
- `smoke-phase-38-public-runtime-assets.ps1` contains `Phase 38 public runtime asset smoke passed`.
- `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/local/smoke-phase-38-public-runtime-assets.ps1` exits `0` when local public backend and MySQL are running.
</acceptance_criteria>
<verify>
  <automated>powershell -NoProfile -ExecutionPolicy Bypass -File scripts/local/smoke-phase-38-public-runtime-assets.ps1</automated>
</verify>
<done>
Phase 38 has a repeatable smoke script for runtime asset chain, privacy, event idempotency, exploration, and session exit.
</done>
</task>

<task id="38-03-02" type="docs">
<name>Run verification and record Phase 38 evidence</name>
<files>
- .planning/phases/38-public-runtime-asset-consumption/38-VERIFICATION.md
</files>
<read_first>
- .planning/phases/38-public-runtime-asset-consumption/38-VALIDATION.md
- .planning/phases/38-public-runtime-asset-consumption/38-01-SUMMARY.md
- .planning/phases/38-public-runtime-asset-consumption/38-02-SUMMARY.md
- scripts/local/smoke-phase-38-public-runtime-assets.ps1
</read_first>
<action>
Create `38-VERIFICATION.md` after executing verification commands.

Record exact command results for:
- `mvn -q -DskipTests compile -f packages/server/pom.xml`
- `mvn -q -Dtest=PublicRuntimeAssetServiceTest test -f packages/server/pom.xml`
- `mvn -q -Dtest=PublicExperienceEventServiceTest test -f packages/server/pom.xml`
- `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/local/smoke-phase-38-public-runtime-assets.ps1`

The verification document must include sections:
- `## Scope`
- `## Automated Evidence`
- `## Runtime Privacy Checks`
- `## Event and Session Checks`
- `## Caveats`

If authenticated smoke is skipped because no bearer token is set, record that as a caveat and do not mark `RUN-03` complete until an authenticated smoke or equivalent test passes.
</action>
<acceptance_criteria>
- `38-VERIFICATION.md` contains `# Phase 38 Verification`.
- `38-VERIFICATION.md` contains `RUN-01`.
- `38-VERIFICATION.md` contains `RUN-02`.
- `38-VERIFICATION.md` contains `RUN-03`.
- `38-VERIFICATION.md` contains `RUN-04`.
- `38-VERIFICATION.md` contains `mvn -q -DskipTests compile -f packages/server/pom.xml`.
- `38-VERIFICATION.md` contains `smoke-phase-38-public-runtime-assets.ps1`.
- `38-VERIFICATION.md` contains `Runtime Privacy Checks`.
</acceptance_criteria>
<verify>
  <manual>Read `38-VERIFICATION.md` and confirm all required evidence sections exist with actual command outcomes.</manual>
</verify>
<done>
Phase 38 verification evidence records compile, tests, smoke, runtime privacy checks, event checks, and caveats.
</done>
</task>

<task id="38-03-03" type="docs">
<name>Update v3.1 requirements traceability for RUN-01 through RUN-04</name>
<files>
- .planning/REQUIREMENTS.md
</files>
<read_first>
- .planning/REQUIREMENTS.md
- .planning/phases/38-public-runtime-asset-consumption/38-VERIFICATION.md
</read_first>
<action>
After verification passes, update `.planning/REQUIREMENTS.md`.

Set these requirement checklist rows to complete:
- `RUN-01`
- `RUN-02`
- `RUN-03`
- `RUN-04`

In the traceability table, set:
- `RUN-01 | Phase 38 | Complete`
- `RUN-02 | Phase 38 | Complete`
- `RUN-03 | Phase 38 | Complete`
- `RUN-04 | Phase 38 | Complete`

Add concise evidence notes only if the file already has comparable notes for Phase 36/37. Do not claim mini-program Phase 39 behavior here.
</action>
<acceptance_criteria>
- `.planning/REQUIREMENTS.md` contains `**RUN-01**` with checked status `[x]`.
- `.planning/REQUIREMENTS.md` contains `**RUN-02**` with checked status `[x]`.
- `.planning/REQUIREMENTS.md` contains `**RUN-03**` with checked status `[x]`.
- `.planning/REQUIREMENTS.md` contains `**RUN-04**` with checked status `[x]`.
- `.planning/REQUIREMENTS.md` contains `RUN-01 | Phase 38 | Complete`.
- `.planning/REQUIREMENTS.md` contains `RUN-04 | Phase 38 | Complete`.
</acceptance_criteria>
<verify>
  <manual>Grep `.planning/REQUIREMENTS.md` for `RUN-01 | Phase 38 | Complete` through `RUN-04 | Phase 38 | Complete`.</manual>
</verify>
<done>
Requirements traceability truthfully marks Phase 38 runtime requirements complete after verification evidence exists.
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| smoke script -> local public API | Local script calls read-only and authenticated public endpoints. |
| smoke script -> MySQL | Local script may import seed files and inspect fixture state. |
| smoke output -> git/docs | Verification evidence must not leak bearer tokens or secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T38-11 | Information Disclosure | Smoke output | mitigate | Never print bearer tokens, provider keys, COS secrets, or raw sensitive provenance. |
| T38-12 | Tampering | Smoke seed import | mitigate | Import only known repo seed files and use utf8mb4; do not write arbitrary inline Chinese SQL. |
| T38-13 | Repudiation | Verification evidence | mitigate | Record exact commands and pass/fail outcomes in `38-VERIFICATION.md`. |
| T38-14 | Denial of Service | Smoke runtime calls | mitigate | Use bounded local API checks and no request-time mass URL probing. |
| T38-15 | Information Disclosure | Requirements update | mitigate | Requirements notes must summarize verification, not include API tokens or raw runtime JSON dumps. |
</threat_model>

<verification>
1. Run `mvn -q -DskipTests compile -f packages/server/pom.xml`.
2. Run `mvn -q -Dtest=PublicRuntimeAssetServiceTest test -f packages/server/pom.xml`.
3. Run `mvn -q -Dtest=PublicExperienceEventServiceTest test -f packages/server/pom.xml`.
4. Run `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/local/smoke-phase-38-public-runtime-assets.ps1`.
5. Confirm `38-VERIFICATION.md` records actual outcomes.
6. Confirm `.planning/REQUIREMENTS.md` marks `RUN-01` through `RUN-04` complete only after passing evidence.
</verification>

<success_criteria>
- Phase 38 smoke verifies runtime assets, fallback/lifecycle filtering, banned-field absence, event idempotency, exploration, and session exit.
- Verification evidence is written and caveats are explicit.
- Requirements traceability for `RUN-01` through `RUN-04` is updated truthfully.
- No secrets are committed or printed in planning artifacts.
</success_criteria>

<output>
After completion, create `.planning/phases/38-public-runtime-asset-consumption/38-03-SUMMARY.md`.
</output>
