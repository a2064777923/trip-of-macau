---
phase: 30-storyline-mode-and-chapter-override-workbench
plan: 04
type: execute
wave: 4
depends_on:
  - 30-01
  - 30-02
  - 30-03
files_modified:
  - scripts/local/mysql/init/41-phase-30-storyline-mode-overrides.sql
  - scripts/local/smoke-phase-30-storyline-mode.ps1
  - .planning/phases/30-storyline-mode-and-chapter-override-workbench/30-HANDOFF.md
  - .planning/phases/30-storyline-mode-and-chapter-override-workbench/30-VERIFICATION.md
autonomous: true
requirements_addressed:
  - STORY-02
  - STORY-04
  - LINK-02
requirements:
  - STORY-02
  - STORY-04
  - LINK-02
must_haves:
  truths:
    - Phase 30 ships with meaningful seeded story-mode data that demonstrates A-Ma Temple inheritance and chapter override replacement/append behavior.
    - Live smoke verifies admin facade, public storyline runtime, and Phase 29 POI runtime regression.
    - Chinese seed content is written through UTF-8/utf8mb4 SQL files, not inline PowerShell.
    - Closeout docs clearly identify Phase 31-34 follow-on boundaries.
  artifacts:
    - scripts/local/mysql/init/41-phase-30-storyline-mode-overrides.sql
    - scripts/local/smoke-phase-30-storyline-mode.ps1
    - .planning/phases/30-storyline-mode-and-chapter-override-workbench/30-HANDOFF.md
    - .planning/phases/30-storyline-mode-and-chapter-override-workbench/30-VERIFICATION.md
  key_links:
    - SQL seed -> `東西方文明的戰火與共生` Chapter 1 story-mode override data
    - Smoke -> admin storyline mode facade and public storyline runtime endpoint
    - Handoff -> Phase 31 governance, Phase 32 progress, Phase 33 content package, Phase 34 mini-program baseline
---

<objective>
Seed and verify the first story-mode acceptance slice so Phase 30 ends with live evidence that a story chapter can inherit the A-Ma Temple default POI flow, replace the default arrival media, append story-specific interactions/rewards, and expose compiled public runtime DTOs.
</objective>

<threat_model>
| Risk ID | ASVS L1 risk | Surface | Mitigation |
| --- | --- | --- | --- |
| T30-04-01 | Encoding corruption in Chinese story seed | SQL seed | Use UTF-8 SQL file with `SET NAMES utf8mb4`; avoid inline PowerShell Chinese literals. |
| T30-04-02 | False-positive verification from source-only checks | Phase closeout | Run live admin and public HTTP smoke against 8081 and 8080. |
| T30-04-03 | Public draft leakage | Runtime smoke | Assert public runtime omits admin-only `status` fields and uses published data only. |
| T30-04-04 | Story seed conflicts with existing content | SQL idempotency | Use code-based lookups/upserts and delete/recreate only Phase 30 scoped override rows. |
| T30-04-05 | Secrets in smoke scripts | Auth handling | Read admin/public auth from env vars or ignored local login material only. |
</threat_model>

<tasks>

<task id="30-04-01" type="auto">
<read_first>
- .planning/phases/30-storyline-mode-and-chapter-override-workbench/30-CONTEXT.md
- scripts/local/mysql/init/40-phase-29-poi-default-experience.sql
- scripts/local/mysql/init/39-phase-28-experience-orchestration.sql
- scripts/local/mysql/init/38-phase-28-story-content-and-lottie.sql
</read_first>
<action>
Create `scripts/local/mysql/init/41-phase-30-storyline-mode-overrides.sql`.

The SQL file must:

- Begin with comments warning that all multilingual text must remain UTF-8/utf8mb4.
- Use `USE aoxiaoyou;` and `SET NAMES utf8mb4;`.
- Resolve `@poi_ama_id` from `pois.code = 'ama_temple'`.
- Resolve `@poi_ama_flow_id` from `experience_flows.code = 'poi_ama_default_walk_in'`.
- Ensure a published storyline exists with `code='east_west_war_and_coexistence'` and Traditional Chinese title `東西方文明的戰火與共生`.
- Ensure a published chapter exists with title `鏡海初戰：中葡首次海防對峙`, `chapter_order=1`, `anchor_type='poi'`, `anchor_target_id=@poi_ama_id`, and `status='published'`.
- Set the chapter `story_mode_config_json` to a `schemaVersion: 1` object containing:
  - `hideUnrelatedContent: true`
  - `nearbyRevealEnabled: true`
  - `nearbyRevealRadiusMeters: 120`
  - `nearbyRevealMeters: 120`
  - `currentRouteHighlight: "copper_flame"`
  - `currentRouteStyle: "copper_flame"`
  - `inactiveRouteStyle: "muted_ink"`
  - `clearTemporaryProgressOnExit: true`
  - `exitResetsSessionProgress: true`
  - `preservePermanentEvents: true`
  - `branchSourceType: "nearby_poi"`
  - `branchInsertPosition: "between_chapters"`
  - `branchSkippable: true`
  - `branchAffectsStoryProgress: false`
- Set the chapter `override_policy_json` to `schemaVersion: 1`, `inheritDefaultFlow: true`, `disableDefaultArrivalMedia: false`, and `appendStorySpecificRewards: true`.
- Create a published chapter-specific experience flow with `code='story_east_west_ch01_flow'`, `flow_type='story_chapter'`, and `mode='storyline'`.
- Create published replacement/append steps in that flow:
  - `story_ch01_arrival_immersive_media`
  - `story_ch01_mainline_overlays`
  - `story_ch01_side_pickups`
  - `story_ch01_hidden_challenge`
  - `story_ch01_reward_titles`
- Create published overrides for the chapter:
  - `target_step_code='arrival_intro_media'`, `override_mode='replace'`, replacement step `story_ch01_arrival_immersive_media`.
  - append override for `story_ch01_mainline_overlays`.
  - append override for `story_ch01_side_pickups`.
  - append override for `story_ch01_hidden_challenge`.
  - append override for `story_ch01_reward_titles`.
- Seed exploration elements for:
  - `story_ch01_arrival_immersive_media`
  - `story_ch01_mainline_overlays`
  - `story_ch01_side_pickups`
  - `story_ch01_hidden_challenge`
  - `story_ch01_reward_titles`

Use idempotent code-based upserts/deletes scoped to these Phase 30 codes. Do not use inline PowerShell to write Chinese content.
</action>
<verify>
<grep>powershell -NoProfile -Command "Select-String -Path 'scripts/local/mysql/init/41-phase-30-storyline-mode-overrides.sql' -Pattern 'SET NAMES utf8mb4|east_west_war_and_coexistence|東西方文明的戰火與共生|鏡海初戰：中葡首次海防對峙|ama_temple|poi_ama_default_walk_in|story_east_west_ch01_flow|story_ch01_arrival_immersive_media|story_ch01_mainline_overlays|story_ch01_side_pickups|story_ch01_hidden_challenge|story_ch01_reward_titles|arrival_intro_media|replace|append|schemaVersion|nearbyRevealRadiusMeters|clearTemporaryProgressOnExit|preservePermanentEvents'"</grep>
</verify>
<acceptance_criteria>
- `41-phase-30-storyline-mode-overrides.sql` exists and contains `SET NAMES utf8mb4`.
- The seed contains `東西方文明的戰火與共生` and `鏡海初戰：中葡首次海防對峙`.
- The seed references `poi_ama_default_walk_in` and `arrival_intro_media`.
- The seed creates or updates published replacement/append story steps.
- The seed includes `replace`, `append`, `schemaVersion`, `nearbyRevealRadiusMeters`, `clearTemporaryProgressOnExit`, and `preservePermanentEvents`.
</acceptance_criteria>
</task>

<task id="30-04-02" type="auto">
<read_first>
- scripts/local/smoke-phase-29-poi-experience.ps1
- scripts/local/smoke-phase-28-experience.ps1
- packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/controller/AdminStorylineModeController.java
- packages/server/src/main/java/com/aoxiaoyou/tripofmacau/controller/ExperienceController.java
</read_first>
<action>
Create `scripts/local/smoke-phase-30-storyline-mode.ps1` using the same explicit UTF-8 `HttpClient` pattern as Phase 29 smoke.

The script must:

- Accept optional parameters `AdminBaseUrl`, `PublicBaseUrl`, `Locale`, and `StorylineCode`, defaulting to `http://127.0.0.1:8081`, `http://127.0.0.1:8080`, `zh-Hant`, and `east_west_war_and_coexistence`.
- Read admin auth from `PHASE30_ADMIN_BEARER`, `PHASE30_ADMIN_USERNAME`/`PHASE30_ADMIN_PASSWORD`, `PHASE29_ADMIN_BEARER`, or local `tmp-admin-login.json`.
- Read public auth from `PHASE30_PUBLIC_BEARER`, `PHASE29_PUBLIC_BEARER`, or dev-bypass only when available.
- Resolve the storyline ID by admin page query or direct lookup using the story code.
- Call `GET /api/admin/v1/storylines/{storylineId}/mode-workbench` and assert:
  - `publicRuntimePath` contains `/api/v1/storylines/{storylineId}/runtime`.
  - `availableAnchorTypes` contains `poi`, `indoor_building`, `indoor_floor`, `indoor_node`, `task`, `overlay`, `manual`.
  - `availableOverrideModes` contains `inherit`, `disable`, `replace`, `append`.
  - Chapter 1 title contains `鏡海初戰`.
  - Chapter 1 anchor type is `poi`.
  - Chapter 1 inherited flow contains `arrival_intro_media`.
  - Overrides contain `targetStepCode='arrival_intro_media'` and `overrideMode='replace'`.
- Call `GET /api/v1/storylines/{storylineId}/runtime?locale={Locale}` and assert:
  - `storyModeConfig.hideUnrelatedContent` is true.
  - `storyModeConfig.nearbyRevealRadiusMeters` or compatible alias is `120`.
  - `storyModeConfig.preservePermanentEvents` is true.
  - compiled steps include `story_ch01_arrival_immersive_media`.
  - compiled steps include `story_ch01_mainline_overlays`, `story_ch01_side_pickups`, `story_ch01_hidden_challenge`, and `story_ch01_reward_titles`.
  - public runtime flow, steps, and overrides do not expose admin-only `status`.
- Run or call the Phase 29 POI runtime regression path for `GET /api/v1/experience/poi/{PoiId}` when the A-Ma POI is resolvable.
- Print `Phase 30 storyline mode smoke passed` on success.

Do not hardcode secrets or credentials.
</action>
<verify>
<automated>powershell -NoProfile -ExecutionPolicy Bypass -File scripts/local/smoke-phase-30-storyline-mode.ps1</automated>
<grep>powershell -NoProfile -Command "Select-String -Path 'scripts/local/smoke-phase-30-storyline-mode.ps1' -Pattern 'PHASE30_ADMIN_BEARER|PHASE30_PUBLIC_BEARER|HttpClient|UTF8|mode-workbench|/api/v1/storylines|runtime|availableAnchorTypes|availableOverrideModes|arrival_intro_media|story_ch01_arrival_immersive_media|story_ch01_mainline_overlays|story_ch01_side_pickups|story_ch01_hidden_challenge|story_ch01_reward_titles|Assert-NoStatusField|Phase 30 storyline mode smoke passed'"</grep>
</verify>
<acceptance_criteria>
- The smoke script exists and uses explicit UTF-8 HTTP handling.
- The smoke script reads credentials only from environment variables or ignored local login material.
- The smoke script verifies admin workbench snapshot, public runtime, override replacement, append steps, story-mode config, and no public `status` leakage.
- `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/local/smoke-phase-30-storyline-mode.ps1` succeeds against the local stack after seed import and service restart.
</acceptance_criteria>
</task>

<task id="30-04-03" type="auto">
<read_first>
- .planning/phases/30-storyline-mode-and-chapter-override-workbench/30-CONTEXT.md
- .planning/phases/30-storyline-mode-and-chapter-override-workbench/30-RESEARCH.md
- scripts/local/smoke-phase-30-storyline-mode.ps1
- .planning/phases/29-poi-default-experience-workbench/29-HANDOFF.md
- .planning/phases/29-poi-default-experience-workbench/29-VERIFICATION.md
</read_first>
<action>
After implementation and verification, write Phase 30 closeout evidence:

- Create `30-HANDOFF.md` with status `foundation-handoff`, delivered artifacts, carried-forward contracts, and explicit non-claims.
- Create `30-VERIFICATION.md` with status `passed` only if backend compiles, public backend compiles, admin UI build, seed/source checks, Phase 30 smoke, and Phase 29 regression smoke pass.

`30-HANDOFF.md` must include a follow-on table:

- `Storyline mode config` -> Phase 34 mini-program route/runtime consumption.
- `Chapter inherited flow and overrides` -> Phase 31 governance can inspect conflicts and usage.
- `Story-specific pickups/challenges/rewards as steps` -> Phase 33 flagship content package can expand.
- `Semantic exploration weights on story steps` -> Phase 32 dynamic progress denominator.
- `Public runtime preview` -> Phase 34 mini-program baseline.

`30-HANDOFF.md` must explicitly state:

- Phase 30 does not build global interaction/task governance; Phase 31 owns it.
- Phase 30 does not build traveler progress inspection; Phase 32 owns it.
- Phase 30 does not generate the complete five-chapter assets; Phase 33 owns it.
- Phase 30 does not claim full mini-program experiential acceptance; Phase 34 or later owns it.

`30-VERIFICATION.md` must list exact commands and results.
</action>
<verify>
<grep>powershell -NoProfile -Command "Select-String -Path '.planning/phases/30-storyline-mode-and-chapter-override-workbench/30-HANDOFF.md','.planning/phases/30-storyline-mode-and-chapter-override-workbench/30-VERIFICATION.md' -Pattern 'Storyline mode config|Chapter inherited flow and overrides|Phase 31|Phase 32|Phase 33|Phase 34|smoke-phase-30-storyline-mode.ps1|STORY-02|STORY-04|LINK-02|passed'"</grep>
</verify>
<acceptance_criteria>
- `30-HANDOFF.md` exists and names follow-on consumers for Phase 31, 32, 33, and 34.
- `30-HANDOFF.md` explicitly states Phase 30 non-claims for governance, progress, complete assets, and full mini-program acceptance.
- `30-VERIFICATION.md` exists and lists backend compile, public backend compile, admin UI build, source checks, Phase 30 smoke, and Phase 29 regression smoke.
- `Select-String` finds `STORY-02`, `STORY-04`, `LINK-02`, `Phase 31`, `Phase 32`, `Phase 33`, and `Phase 34` in closeout docs.
</acceptance_criteria>
</task>

</tasks>

<verification>
- Run `mvn -q -DskipTests compile -f packages/admin/aoxiaoyou-admin-backend/pom.xml`.
- Run `mvn -q -DskipTests compile -f packages/server/pom.xml`.
- Run `npm run build --prefix packages/admin/aoxiaoyou-admin-ui`.
- Run source checks for `41-phase-30-storyline-mode-overrides.sql` and `smoke-phase-30-storyline-mode.ps1`.
- Run `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/local/smoke-phase-30-storyline-mode.ps1`.
- Run `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/local/smoke-phase-29-poi-experience.ps1` as regression.
</verification>

<must_haves>
- Seeded Phase 30 story data demonstrates real POI inheritance and chapter override behavior.
- Live smoke proves admin/public alignment for storyline runtime.
- Phase 29 POI runtime still passes.
- Handoff and verification docs preserve the Phase 31-34 boundaries.
</must_haves>

<success_criteria>
- Seed/source checks pass.
- Backend compile, public backend compile, and admin UI build pass.
- Phase 30 smoke passes.
- Phase 29 smoke still passes.
- Handoff and verification docs are ready for `/gsd-verify-work`.
</success_criteria>
