---
phase: 17-indoor-runtime-evaluation-and-mini-program-alignment
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - packages/client/src/services/api.ts
  - packages/client/src/services/indoorRuntime.ts
  - packages/client/src/services/gameService.ts
  - packages/client/src/pages/map/indoor/index.tsx
  - packages/client/src/pages/map/indoor/index.scss
  - packages/client/src/types/**/*.ts
autonomous: true
requirements: [RULE-03]
user_setup: []
must_haves:
  truths:
    - "The mini-program must consume live authored indoor runtime data from the public backend and must not recreate the rule model from mock payloads or local fixtures."
    - "Anonymous users may browse indoor maps and authored overlays, but guarded or stateful indoor interactions must route through the existing auth-wall flow before mutation."
    - "Phase 17 supports only the explicit runtime subset from research; unsupported behaviors must degrade safely instead of crashing the indoor page."
  artifacts:
    - path: "packages/client/src/pages/map/indoor/index.tsx"
      provides: "Current indoor page that still renders tiles and static markers only"
      contains: "parseTileManifest"
    - path: "packages/client/src/services/api.ts"
      provides: "Current public indoor DTO and request wrapper baseline"
      contains: "PublicIndoorFloorDto"
    - path: "packages/client/src/services/gameService.ts"
      provides: "Existing auth gating contract for anonymous versus authenticated mini-program usage"
      contains: "requireAuth"
    - path: "packages/server/src/main/java/com/aoxiaoyou/tripofmacau/controller/IndoorController.java"
      provides: "Public indoor runtime endpoints produced by Plan 01"
      contains: "/api/v1/indoor"
---

<objective>
Integrate the mini-program indoor runtime with the new public backend contract for Phase 17.

Purpose: refactor the indoor page from a static tile and marker viewer into a live runtime consumer that evaluates the supported authored behavior subset, preserves anonymous read-only browsing, and routes guarded mutations through the real-auth flow.
Output: expanded public API types, a dedicated indoor runtime client service, indoor page runtime orchestration, effect rendering support, and auth-safe behavior handling.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-indoor-runtime-evaluation-and-mini-program-alignment/17-RESEARCH.md
@.planning/phases/17-indoor-runtime-evaluation-and-mini-program-alignment/17-VALIDATION.md
@packages/client/src/pages/map/indoor/index.tsx
@packages/client/src/services/api.ts
@packages/client/src/services/gameService.ts
@packages/server/src/main/java/com/aoxiaoyou/tripofmacau/controller/IndoorController.java
</context>

<threat_model>
<trust_boundaries>
  <boundary>
    <name>Public indoor runtime payloads into mini-program page state</name>
    <description>The client will load live authored runtime payloads and transform them into visible overlays, markers, timers, and active effects on the indoor page.</description>
    <data>runtime nodes, behavior profiles, overlay geometry, localized labels, popup payloads, path motion metadata, supported flags</data>
  </boundary>
  <boundary>
    <name>Client-side interaction events into auth-gated mutation flows</name>
    <description>The indoor page will convert tap, proximity, and dwell interactions into server submissions or auth-wall prompts.</description>
    <data>eventType, nodeId, behaviorId, dwellMs, client session state, auth status, blocked reasons, effect responses</data>
  </boundary>
</trust_boundaries>
<threats>
  <threat>
    <threat_id>T-17-02</threat_id>
    <category>Client Runtime Drift</category>
    <component>indoor page orchestration</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Centralize runtime fetch, normalization, and interaction submission in a dedicated `indoorRuntime.ts` service module instead of spreading DTO assumptions across the page tree.</mitigation_plan>
  </threat>
  <threat>
    <threat_id>T-17-03</threat_id>
    <category>Anonymous Mutation</category>
    <component>auth gating</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Use the existing `requireAuth(...)` flow before sending guarded interaction writes and keep anonymous users on a read-only runtime path.</mitigation_plan>
  </threat>
  <threat>
    <threat_id>T-17-04</threat_id>
    <category>Unsupported Runtime Crash</category>
    <component>runtime effect renderer</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Render only the supported subset, surface unsupported behaviors as non-executable cards or hints, and guard every effect dispatcher against missing typed segments.</mitigation_plan>
  </threat>
</threats>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Extend public indoor DTOs and add a dedicated mini-program runtime service</name>
  <files>packages/client/src/services/api.ts, packages/client/src/services/indoorRuntime.ts, packages/client/src/types/**/*.ts</files>
  <read_first>packages/client/src/services/api.ts, .planning/phases/17-indoor-runtime-evaluation-and-mini-program-alignment/17-RESEARCH.md, packages/server/src/main/java/com/aoxiaoyou/tripofmacau/controller/IndoorController.java</read_first>
  <action>Extend `packages/client/src/services/api.ts` with typed DTOs for the new runtime snapshot and interaction endpoints instead of overloading the old `PublicIndoorFloorDto` and `PublicIndoorMarkerDto` only. Add a new `packages/client/src/services/indoorRuntime.ts` module that owns runtime snapshot fetching, interaction submission, client-session identifiers, supported-subset normalization, and safe helpers for tap, proximity, and dwell dispatch. The runtime service must preserve locale handling, must not create mock indoor identities, and must expose a shape that the indoor page can consume without re-parsing raw JSON in many places.</action>
  <verify>powershell -Command "Test-Path 'packages/client/src/services/indoorRuntime.ts'; Select-String -Path 'packages/client/src/services/api.ts','packages/client/src/services/indoorRuntime.ts' -Pattern 'IndoorRuntime|interactionAccepted|blockedReason|requiresAuth|appearanceRules|triggerRules|effectRules|schedule_window|path_motion|clientSessionId'"</verify>
  <acceptance_criteria>
    - `api.ts` exposes typed DTOs for runtime snapshots and runtime interaction responses
    - `packages/client/src/services/indoorRuntime.ts` exists and centralizes indoor runtime fetch and submit logic
    - the new client runtime service preserves locale-aware live requests and does not recreate mock identity behavior
  </acceptance_criteria>
  <done>The mini-program has one stable client-side integration layer for the public indoor runtime contract.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor the indoor page into a live runtime viewer with supported effect rendering</name>
  <files>packages/client/src/pages/map/indoor/index.tsx, packages/client/src/pages/map/indoor/index.scss</files>
  <read_first>packages/client/src/pages/map/indoor/index.tsx, packages/client/src/services/indoorRuntime.ts, packages/client/src/services/api.ts</read_first>
  <action>Refactor `packages/client/src/pages/map/indoor/index.tsx` so the page loads `GET /api/v1/indoor/floors/{floorId}/runtime`, renders authored runtime nodes and overlays, and evaluates the supported subset locally for responsiveness. The page must keep tile rendering and static floor fallback, but it now also needs loading-state guards to prevent visible flicker while switching floors, runtime-aware marker or overlay rendering, path-motion playback for the guided trail showcase, popup or bubble display for supported effects, and dwell or proximity timing state that does not leak across floor switches. Update `index.scss` to support runtime badges, active path cues, and effect containers without destabilizing the existing indoor viewport layout.</action>
  <verify>powershell -Command "Select-String -Path 'packages/client/src/pages/map/indoor/index.tsx','packages/client/src/pages/map/indoor/index.scss' -Pattern 'indoorRuntime|loading|flicker|path|popup|bubble|dwell|proximity|supported|overlay|activeBehavior'"</verify>
  <acceptance_criteria>
    - the indoor page loads the runtime snapshot instead of relying only on static floor and marker reads
    - supported authored effects such as popup, bubble, media, and path motion have explicit render paths on the page
    - floor switching and runtime loading include guards that prevent the visible jitter or flicker reported in earlier indoor work
  </acceptance_criteria>
  <done>The mini-program indoor page becomes a live runtime viewer for authored indoor behaviors instead of a static marker-only demo.</done>
</task>

<task type="auto">
  <name>Task 3: Align auth gating and failure-safe behavior execution with the runtime page</name>
  <files>packages/client/src/pages/map/indoor/index.tsx, packages/client/src/services/gameService.ts, packages/client/src/services/indoorRuntime.ts</files>
  <read_first>packages/client/src/services/gameService.ts, packages/client/src/pages/map/indoor/index.tsx, .planning/phases/17-indoor-runtime-evaluation-and-mini-program-alignment/17-VALIDATION.md</read_first>
  <action>Integrate the runtime page with the existing auth contract from Phase 7. Anonymous users must still be able to open indoor maps, switch floors, and view authored passive overlays, but any guarded interaction that would post to `/api/v1/indoor/runtime/interactions` for a mutation or personalized outcome must first route through `requireAuth(...)` and the profile auth wall. For supported passive interactions, the page should show the effect or hint immediately. For unsupported or blocked behaviors, show a deterministic non-crashing explanation based on the returned `blockedReason`. Ensure the Lisboa showcase floor remains the canonical proof case and that no local mock fallback bypasses live runtime results.</action>
  <verify>powershell -Command "Select-String -Path 'packages/client/src/pages/map/indoor/index.tsx','packages/client/src/services/gameService.ts','packages/client/src/services/indoorRuntime.ts' -Pattern 'requireAuth|blockedReason|anonymous|authenticated|dev-bypass|/api/v1/indoor/runtime/interactions|lisboeta|profile'"</verify>
  <acceptance_criteria>
    - anonymous browsing remains available for indoor runtime reads
    - guarded or stateful indoor interactions are routed through the existing auth-wall flow before mutation
    - blocked or unsupported runtime behaviors degrade safely instead of freezing or crashing the indoor page
  </acceptance_criteria>
  <done>The indoor runtime aligns with the app-wide auth contract and failure-safe behavior expected by the user-facing mini-program.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] the client consumes the new runtime snapshot and interaction DTOs through one dedicated service module
- [ ] the indoor page renders supported runtime effects without relying on mock-only behavior
- [ ] anonymous browsing remains read-only while guarded indoor writes require login
- [ ] unsupported or blocked runtime items degrade safely and visibly instead of crashing the page
</verification>

<success_criteria>

- All tasks completed
- the mini-program indoor page consumes live authored runtime data from `packages/server`
- supported authored appearance, trigger, and effect subsets execute predictably on the indoor page
- auth and failure handling for indoor runtime flows stay aligned with the existing mini-program contract
</success_criteria>

<output>
After completion, create `.planning/phases/17-indoor-runtime-evaluation-and-mini-program-alignment/17-02-SUMMARY.md`
</output>
