---
phase: 17-indoor-runtime-evaluation-and-mini-program-alignment
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - packages/server/src/test/java/com/aoxiaoyou/tripofmacau/PublicIndoorRuntimeServiceTest.java
  - packages/server/src/test/java/com/aoxiaoyou/tripofmacau/PublicIndoorRuntimeInteractionServiceTest.java
  - scripts/local/smoke-phase-17-indoor-runtime.ps1
  - packages/client/package.json
  - .planning/phases/17-indoor-runtime-evaluation-and-mini-program-alignment/17-UAT.md
autonomous: true
requirements: [RULE-03]
user_setup: []
must_haves:
  truths:
    - "Phase 17 is not complete without live proof on the local public stack at port 8080 plus a real mini-program build path."
    - "The Lisboa indoor showcase remains the canonical verification fixture unless execution intentionally replaces it with a newer deterministic fixture."
    - "Smoke must fail fast when runtime fixtures or endpoints are missing instead of passing on partial static fallbacks."
  artifacts:
    - path: ".planning/phases/17-indoor-runtime-evaluation-and-mini-program-alignment/17-VALIDATION.md"
      provides: "Per-task verification contract and expected test cadence"
      contains: "PublicIndoorRuntimeServiceTest"
    - path: "scripts/local/smoke-phase-12-indoor.ps1"
      provides: "Existing public indoor smoke pattern that Phase 17 can extend"
      contains: "8080"
    - path: "packages/client/package.json"
      provides: "Mini-program build and script aliases"
      contains: "build:weapp"
    - path: "scripts/local/mysql/init/25-phase-15-indoor-rule-showcase-seed.sql"
      provides: "Deterministic Lisboa authored indoor behavior fixture"
      contains: "zipcity-guiding-path"
---

<objective>
Leave executable proof for Phase 17 on the live local stack.

Purpose: verify the new public indoor runtime contract, the mini-program build path, and the deterministic Lisboa runtime fixtures through repeatable tests, a smoke harness, and a resumable UAT checklist.
Output: backend tests, a PowerShell smoke script, optional package script glue, and a manual UAT document for runtime behavior checks in WeChat DevTools.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-indoor-runtime-evaluation-and-mini-program-alignment/17-RESEARCH.md
@.planning/phases/17-indoor-runtime-evaluation-and-mini-program-alignment/17-VALIDATION.md
@packages/server/src/main/java/com/aoxiaoyou/tripofmacau/controller/IndoorController.java
@packages/client/src/pages/map/indoor/index.tsx
@packages/client/package.json
@scripts/local/smoke-phase-12-indoor.ps1
@scripts/local/smoke-phase-15-indoor-authoring.ps1
@scripts/local/smoke-phase-16-indoor-rule-governance.ps1
</context>

<threat_model>
<trust_boundaries>
  <boundary>
    <name>Live public-stack smoke execution</name>
    <description>The smoke harness will authenticate where needed, call the public indoor runtime endpoints on port 8080, and assert deterministic authored behavior data.</description>
    <data>public runtime payloads, blocked reasons, effect responses, authored behavior codes, local service readiness, build results</data>
  </boundary>
  <boundary>
    <name>Manual mini-program runtime evaluation</name>
    <description>Operators or developers must validate the experiential parts of tap, dwell, popup, and path motion in WeChat DevTools.</description>
    <data>runtime marker visibility, popup timing, dwell timing, path motion clarity, auth-wall routing, localized labels</data>
  </boundary>
</trust_boundaries>
<threats>
  <threat>
    <threat_id>T-17-05</threat_id>
    <category>False Proof</category>
    <component>phase verification</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Smoke must hit the live public backend on port 8080, assert runtime-specific fields, and run the mini-program build path instead of relying on page load alone.</mitigation_plan>
  </threat>
  <threat>
    <threat_id>T-17-06</threat_id>
    <category>Fixture Drift</category>
    <component>runtime showcase selection</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Use deterministic Lisboa indoor fixture records and fail fast with an explicit missing-fixture error if they cannot be found.</mitigation_plan>
  </threat>
  <threat>
    <threat_id>T-17-07</threat_id>
    <category>Regression Blind Spot</category>
    <component>manual acceptance</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Create a resumable UAT checklist that covers runtime-specific behavior understanding, auth-gated failure paths, and fallback behavior for unsupported authored rules.</mitigation_plan>
  </threat>
</threats>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add backend tests for runtime projection and interaction evaluation</name>
  <files>packages/server/src/test/java/com/aoxiaoyou/tripofmacau/PublicIndoorRuntimeServiceTest.java, packages/server/src/test/java/com/aoxiaoyou/tripofmacau/PublicIndoorRuntimeInteractionServiceTest.java</files>
  <read_first>packages/server/src/main/java/com/aoxiaoyou/tripofmacau/controller/IndoorController.java, .planning/phases/17-indoor-runtime-evaluation-and-mini-program-alignment/17-VALIDATION.md, .planning/phases/17-indoor-runtime-evaluation-and-mini-program-alignment/17-01-PLAN.md</read_first>
  <action>Create or extend Spring Boot tests under `packages/server/src/test/java/com/aoxiaoyou/tripofmacau` so they prove the Phase 17 backend runtime contract. `PublicIndoorRuntimeServiceTest` must assert that the floor runtime snapshot exposes `runtimeVersion`, behavior arrays, `supported`, and typed effect or trigger segments while reading the deterministic Lisboa fixture. `PublicIndoorRuntimeInteractionServiceTest` must assert that invalid or expired interactions are rejected, anonymous guarded mutations return `requiresAuth = true` with a deterministic `blockedReason`, and supported accepted interactions return effect payloads plus a logged interaction identifier. Keep the tests runnable through `mvn -q "-Dtest=PublicIndoorRuntimeServiceTest,PublicIndoorRuntimeInteractionServiceTest" test` in `packages/server`.</action>
  <verify>powershell -Command "Test-Path 'packages/server/src/test/java/com/aoxiaoyou/tripofmacau/PublicIndoorRuntimeServiceTest.java'; Test-Path 'packages/server/src/test/java/com/aoxiaoyou/tripofmacau/PublicIndoorRuntimeInteractionServiceTest.java'; Select-String -Path 'packages/server/src/test/java/com/aoxiaoyou/tripofmacau/PublicIndoorRuntimeServiceTest.java','packages/server/src/test/java/com/aoxiaoyou/tripofmacau/PublicIndoorRuntimeInteractionServiceTest.java' -Pattern 'runtimeVersion|supported|requiresAuth|blockedReason|interactionLogId|night-market-schedule-overlay|royal-palace-dwell-reveal|zipcity-guiding-path'"</verify>
  <acceptance_criteria>
    - backend tests exist for runtime snapshot projection and authoritative interaction handling
    - the runtime tests assert deterministic fields such as `runtimeVersion`, `supported`, `requiresAuth`, and `blockedReason`
    - the tests target the deterministic Lisboa authored runtime fixture instead of ad hoc seed data
  </acceptance_criteria>
  <done>The Phase 17 backend contract has repeatable automated proof before cross-stack smoke starts.</done>
</task>

<task type="auto">
  <name>Task 2: Build a Phase 17 smoke harness for the live public runtime and mini-program build path</name>
  <files>scripts/local/smoke-phase-17-indoor-runtime.ps1, packages/client/package.json</files>
  <read_first>scripts/local/smoke-phase-12-indoor.ps1, scripts/local/smoke-phase-15-indoor-authoring.ps1, scripts/local/smoke-phase-16-indoor-rule-governance.ps1, packages/client/package.json</read_first>
  <action>Create `scripts/local/smoke-phase-17-indoor-runtime.ps1` that verifies the live public backend on `http://localhost:8080`, locates the deterministic Lisboa building and one target floor, calls `GET /api/v1/indoor/floors/{floorId}/runtime`, asserts the presence of the showcase behaviors `night-market-schedule-overlay`, `royal-palace-dwell-reveal`, and `zipcity-guiding-path`, posts at least one supported runtime interaction and one anonymous-blocked interaction to `POST /api/v1/indoor/runtime/interactions`, and fails fast if the required fixture or runtime fields are missing. Finish the smoke run with `npm run build:weapp` in `packages/client`. If useful, add a package alias such as `smoke:phase17:indoor-runtime` in `packages/client/package.json`.</action>
  <verify>powershell -Command "Test-Path 'scripts/local/smoke-phase-17-indoor-runtime.ps1'; Select-String -Path 'scripts/local/smoke-phase-17-indoor-runtime.ps1','packages/client/package.json' -Pattern '8080|/floors/.*/runtime|/runtime/interactions|night-market-schedule-overlay|royal-palace-dwell-reveal|zipcity-guiding-path|build:weapp|smoke:phase17:indoor-runtime'"</verify>
  <acceptance_criteria>
    - a new Phase 17 smoke harness exists under `scripts/local`
    - the smoke harness asserts runtime-specific fields and authored Lisboa behavior codes against the live public stack on port 8080
    - the Phase 17 proof path includes the mini-program `build:weapp` build
  </acceptance_criteria>
  <done>The project gains a repeatable cross-stack smoke path for the new indoor runtime behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Create a resumable UAT checklist for runtime behavior understanding in DevTools</name>
  <files>.planning/phases/17-indoor-runtime-evaluation-and-mini-program-alignment/17-UAT.md</files>
  <read_first>.planning/phases/17-indoor-runtime-evaluation-and-mini-program-alignment/17-VALIDATION.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</read_first>
  <action>Create `.planning/phases/17-indoor-runtime-evaluation-and-mini-program-alignment/17-UAT.md` with pending checkpoints for these exact manual flows: opening the Lisboa indoor page against the live backend, confirming the runtime snapshot loads without static-marker-only fallback, seeing the schedule-based overlay appear when expected, triggering the dwell-based reveal, triggering the path-motion guidance, observing one popup or bubble effect, attempting one guarded interaction while anonymous and confirming auth-wall routing, retrying the same guarded flow after login, and confirming unsupported authored categories degrade with a visible explanation rather than a crash. Include one explicit checkpoint for multilingual labels still appearing coherent in the runtime UI.</action>
  <verify>powershell -Command "Test-Path '.planning/phases/17-indoor-runtime-evaluation-and-mini-program-alignment/17-UAT.md'; Select-String -Path '.planning/phases/17-indoor-runtime-evaluation-and-mini-program-alignment/17-UAT.md' -Pattern 'Lisboa indoor page|runtime snapshot|schedule-based overlay|dwell-based reveal|path-motion guidance|popup|anonymous|auth-wall|after login|unsupported|multilingual'"</verify>
  <acceptance_criteria>
    - `17-UAT.md` exists in the phase directory
    - the UAT scaffold covers runtime-specific behavior understanding, auth-wall routing, and unsupported fallback handling
    - the UAT scaffold explicitly includes a multilingual runtime-label checkpoint
  </acceptance_criteria>
  <done>Phase 17 leaves a clear manual verification path for runtime behavior quality in WeChat DevTools.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] backend tests cover runtime snapshot projection and interaction gating
- [ ] the Phase 17 smoke harness exercises the live public stack on port 8080 and runs `build:weapp`
- [ ] smoke fails fast when the Lisboa runtime fixture or runtime-specific fields are missing
- [ ] the phase directory contains a resumable `17-UAT.md` for manual runtime validation
</verification>

<success_criteria>

- All tasks completed
- Phase 17 has executable proof for the public runtime contract and the mini-program build path
- deterministic Lisboa runtime fixtures can be used repeatedly for smoke and UAT
- the project can close Rule 03 with real local evidence instead of plan-only confidence
</success_criteria>

<output>
After completion, create `.planning/phases/17-indoor-runtime-evaluation-and-mini-program-alignment/17-03-SUMMARY.md`
</output>
