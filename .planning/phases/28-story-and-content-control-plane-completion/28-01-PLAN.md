---
phase: 28-story-and-content-control-plane-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/local/mysql/init/39-phase-28-experience-orchestration.sql
  - packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/controller/AdminExperienceOrchestrationController.java
  - packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/service/impl/AdminExperienceOrchestrationServiceImpl.java
  - packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/dto/request/AdminExperienceRequest.java
  - packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/dto/response/AdminExperienceResponse.java
  - packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/common/config/AdminAuthInterceptor.java
  - packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/common/config/WebConfig.java
  - packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/common/config/SecurityConfig.java
  - packages/server/src/main/java/com/aoxiaoyou/tripofmacau/controller/ExperienceController.java
  - packages/server/src/main/java/com/aoxiaoyou/tripofmacau/service/impl/PublicExperienceServiceImpl.java
  - packages/server/src/main/java/com/aoxiaoyou/tripofmacau/dto/response/ExperienceRuntimeResponse.java
autonomous: true
requirements_addressed:
  - STORY-01
  - STORY-02
  - STORY-03
  - LINK-01
  - LINK-02
requirements:
  - STORY-01
  - STORY-02
  - STORY-03
  - LINK-01
  - LINK-02
must_haves:
  truths:
    - Admin experience saves reject unversioned JSON and preserve canonical override and exploration semantics.
    - Public experience runtime returns only published compiled flows, storyline chapters, and exploration math.
    - Admin and public contracts use the same owner, flow, override, trigger, and weight vocabulary.
    - Admin experience endpoints stay protected by the existing admin auth boundary and reject anonymous requests while allowing authenticated admin access.
  artifacts:
    - scripts/local/mysql/init/39-phase-28-experience-orchestration.sql
    - packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/service/impl/AdminExperienceOrchestrationServiceImpl.java
    - packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/common/config/AdminAuthInterceptor.java
    - packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/common/config/WebConfig.java
    - packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/common/config/SecurityConfig.java
    - packages/server/src/main/java/com/aoxiaoyou/tripofmacau/service/impl/PublicExperienceServiceImpl.java
    - packages/server/src/main/java/com/aoxiaoyou/tripofmacau/dto/response/ExperienceRuntimeResponse.java
  key_links:
    - AdminExperienceOrchestrationServiceImpl.validateVersionedJson -> persisted experience JSON columns
    - WebConfig.addInterceptors -> AdminAuthInterceptor.preHandle -> AdminExperienceOrchestrationController `/api/admin/v1/experience/**`
    - PublicExperienceServiceImpl.compileSteps -> ExperienceRuntimeResponse.StoryChapterRuntime.compiledSteps
    - ExperienceController.recordEvent -> user_exploration_events client_event_id idempotency
---

<objective>
Harden the MySQL, admin-backend, and public-backend experience foundation so later Phase 29-34 work extends one contract instead of reinterpreting behavior per surface.

Purpose: Lock D-01 through D-12 into schema, DTO, validation, and runtime behavior while covering the contract-sharing intent of LINK-01 and LINK-02.
Output: Versioned admin save contracts, protected admin experience routes, and published-only public runtime DTOs with dynamic exploration/event semantics aligned to the seeded experience model.
</objective>

<threat_model>
| Risk ID | ASVS L1 risk | Surface | Mitigation |
| --- | --- | --- | --- |
| T28-01-01 | Input tampering through arbitrary JSON payloads | Admin experience save endpoints | Enforce `schemaVersion` object validation for all versioned JSON fields and reject malformed payloads before persistence. |
| T28-01-02 | Broken access or data exposure through draft runtime leakage | Public experience runtime endpoints | Filter flows, steps, overrides, and exploration elements to runtime-eligible published rows only before DTO compilation. |
| T28-01-03 | Event replay or duplicate progress writes | `/api/v1/experience/events` and storyline session events | Preserve unique `client_event_id` behavior and keep event ingestion idempotent against `user_exploration_events`. |
| T28-01-04 | Cross-surface contract drift | SQL, admin DTOs, public DTOs | Normalize owner types, override modes, trigger types, and weight levels in SQL plus Java DTO/service code from one vocabulary. |
| T28-01-05 | Broken access control on admin experience endpoints | `AdminAuthInterceptor`, `WebConfig`, `SecurityConfig`, `/api/admin/v1/experience/**` | Preserve MVC interceptor coverage for `/api/admin/v1/**` and prove anonymous 401/403 plus authenticated success against the experience controller. |
</threat_model>

<tasks>

<task id="28-01-01" type="auto">
<read_first>
- .planning/phases/28-story-and-content-control-plane-completion/28-CONTEXT.md
- .planning/phases/28-story-and-content-control-plane-completion/28-RESEARCH.md
- .planning/phases/28-story-and-content-control-plane-completion/28-VALIDATION.md
- scripts/local/mysql/init/39-phase-28-experience-orchestration.sql
- packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/controller/AdminExperienceOrchestrationController.java
- packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/service/impl/AdminExperienceOrchestrationServiceImpl.java
- packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/dto/request/AdminExperienceRequest.java
- packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/dto/response/AdminExperienceResponse.java
</read_first>
<action>
Update the admin schema and save contract per D-01, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11, and D-12. Keep the existing `/api/admin/v1/experience/**` surface, but normalize `template_type`, `flow_type`, `owner_type`, `binding_role`, `override_mode`, `trigger_type`, `weight_level`, and `status` across `39-phase-28-experience-orchestration.sql`, `AdminExperienceRequest.java`, `AdminExperienceResponse.java`, and `AdminExperienceOrchestrationServiceImpl.java`. Enforce versioned-object validation for `configJson`, `schemaJson`, `mapPolicyJson`, `advancedConfigJson`, `triggerConfigJson`, `conditionConfigJson`, `effectConfigJson`, `overrideConfigJson`, and `metadataJson`, while keeping `rewardRuleIdsJson` as explicitly non-versioned JSON when needed. Preserve `inherit|disable|replace|append` override semantics, anchor-default binding behavior, semantic exploration weights, and governance findings so the workbench can render operator guidance instead of forcing raw JSON-only authoring. Do not hardcode secrets.
</action>
<verify>
<automated>mvn -q -DskipTests compile -f packages/admin/aoxiaoyou-admin-backend/pom.xml</automated>
<grep>powershell -NoProfile -Command "Select-String -Path 'scripts/local/mysql/init/39-phase-28-experience-orchestration.sql','packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/service/impl/AdminExperienceOrchestrationServiceImpl.java','packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/dto/request/AdminExperienceRequest.java' -Pattern 'schemaVersion|overrideMode|default_experience_flow|weightLevel|story_mode_config_json'"</grep>
</verify>
<acceptance_criteria>
- `Select-String` finds schema-version enforcement for each versioned JSON save field and the allowed override semantics `inherit`, `disable`, `replace`, and `append`.
- `Select-String` finds semantic exploration weight handling in the admin contract and seed vocabulary rather than fixed increment text.
- `mvn -q -DskipTests compile -f packages/admin/aoxiaoyou-admin-backend/pom.xml` succeeds with the updated admin controller, DTO, and service wiring.
</acceptance_criteria>
</task>

<task id="28-01-02" type="auto">
<read_first>
- .planning/phases/28-story-and-content-control-plane-completion/28-CONTEXT.md
- .planning/phases/28-story-and-content-control-plane-completion/28-RESEARCH.md
- .planning/phases/28-story-and-content-control-plane-completion/28-VALIDATION.md
- packages/server/src/main/java/com/aoxiaoyou/tripofmacau/controller/ExperienceController.java
- packages/server/src/main/java/com/aoxiaoyou/tripofmacau/service/impl/PublicExperienceServiceImpl.java
- packages/server/src/main/java/com/aoxiaoyou/tripofmacau/dto/response/ExperienceRuntimeResponse.java
- scripts/local/mysql/init/39-phase-28-experience-orchestration.sql
</read_first>
<action>
Update the public runtime contract per D-02, D-03, D-04, D-10, D-11, and D-12 plus LINK-02. Keep `ExperienceController.java` route shapes unchanged, but make `PublicExperienceServiceImpl.java` the canonical compiler for published POI default flows, story-chapter inherited flows, story-mode config, override-applied compiled steps, and dynamic exploration state derived from `exploration_elements` plus `user_exploration_events`. Ensure runtime DTOs expose parsed maps and typed chapter runtime structure in `ExperienceRuntimeResponse.java` instead of raw admin form JSON, keep storyline-session progress separate from permanent exploration events, and preserve `client_event_id`-based idempotency for event ingestion. Do not pull Phase 34 mini-program-only behavior into this plan.
</action>
<verify>
<automated>mvn -q -DskipTests compile -f packages/server/pom.xml</automated>
<grep>powershell -NoProfile -Command "Select-String -Path 'packages/server/src/main/java/com/aoxiaoyou/tripofmacau/service/impl/PublicExperienceServiceImpl.java','packages/server/src/main/java/com/aoxiaoyou/tripofmacau/dto/response/ExperienceRuntimeResponse.java' -Pattern 'published|compileSteps|storyModeConfig|client_event_id|explorationWeightLevel'"</grep>
</verify>
<acceptance_criteria>
- `Select-String` shows published-only filtering in the public service for flows, steps, overrides, and exploration elements.
- `Select-String` shows compiled storyline runtime output with `storyModeConfig`, chapter-level overrides, and ordered compiled steps.
- `mvn -q -DskipTests compile -f packages/server/pom.xml` succeeds with the updated runtime DTO and service wiring.
</acceptance_criteria>
</task>

<task id="28-01-03" type="auto">
<read_first>
- .planning/phases/28-story-and-content-control-plane-completion/28-CONTEXT.md
- .planning/phases/28-story-and-content-control-plane-completion/28-RESEARCH.md
- .planning/phases/28-story-and-content-control-plane-completion/28-VALIDATION.md
- packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/common/config/AdminAuthInterceptor.java
- packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/common/config/WebConfig.java
- packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/common/config/SecurityConfig.java
- packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/controller/AdminExperienceOrchestrationController.java
</read_first>
<action>
Audit the actual admin auth boundary before closing backend verification. Preserve `/api/admin/v1/experience/**` protection by keeping `WebConfig.java` interceptor coverage and `AdminAuthInterceptor.java` bearer-token enforcement in front of `AdminExperienceOrchestrationController.java`; only patch `AdminAuthInterceptor.java`, `WebConfig.java`, or `SecurityConfig.java` if the current boundary allows anonymous access or bypasses the experience routes. Do not "fix" failing smoke checks by weakening `addPathPatterns("/api/admin/v1/**")`, changing the controller base path, or bypassing `Authorization` handling. Add a live smoke path that hits an experience endpoint anonymously and then with an authenticated admin token so execution proves anonymous requests return 401 or 403 while authenticated requests succeed.
</action>
<verify>
<automated>powershell -NoProfile -Command "$base='http://127.0.0.1:8081'; $anonStatus=$null; try { Invoke-WebRequest -Uri \"$base/api/admin/v1/experience/templates?pageNum=1&amp;pageSize=1\" -UseBasicParsing | Out-Null; throw 'Anonymous experience request unexpectedly succeeded.' } catch { if ($_.Exception.Response) { $anonStatus=[int]$_.Exception.Response.StatusCode } }; if ($anonStatus -notin 401,403) { throw \"Expected anonymous 401/403 but got $anonStatus\" }; if (-not $env:PHASE28_ADMIN_BEARER) { throw 'Set PHASE28_ADMIN_BEARER before running the authenticated admin smoke check.' }; $authHeaders=@{ Authorization = \"Bearer $($env:PHASE28_ADMIN_BEARER)\" }; $authResponse = Invoke-RestMethod -Uri \"$base/api/admin/v1/experience/templates?pageNum=1&amp;pageSize=1\" -Headers $authHeaders -Method Get; if ($null -eq $authResponse) { throw 'Authenticated experience request returned no body.' }"</automated>
<grep>powershell -NoProfile -Command "Select-String -Path 'packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/common/config/AdminAuthInterceptor.java','packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/common/config/WebConfig.java','packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/common/config/SecurityConfig.java','packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/controller/AdminExperienceOrchestrationController.java' -Pattern '/api/admin/v1/experience|addPathPatterns|Bearer |401|permitAll|RequestMapping'"</grep>
</verify>
<acceptance_criteria>
- Anonymous `GET /api/admin/v1/experience/templates?pageNum=1&pageSize=1` returns 401 or 403, and the same request succeeds when `PHASE28_ADMIN_BEARER` supplies a valid admin token.
- `Select-String` confirms `WebConfig.java` still registers `AdminAuthInterceptor` for `/api/admin/v1/**`, `AdminAuthInterceptor.java` still enforces bearer auth, and `AdminExperienceOrchestrationController.java` remains under `/api/admin/v1/experience`.
- No implementation weakens the admin auth boundary or moves experience endpoints outside the protected admin namespace.
</acceptance_criteria>
</task>

</tasks>

<verification>
- Run `mvn -q -DskipTests compile -f packages/admin/aoxiaoyou-admin-backend/pom.xml`.
- Run `mvn -q -DskipTests compile -f packages/server/pom.xml`.
- Run the task-level anonymous/authenticated admin smoke command for `/api/admin/v1/experience/templates` after setting `PHASE28_ADMIN_BEARER`.
- Run the task-level `Select-String` checks to confirm schema-version enforcement, published filtering, and compiled-step wiring are present in source.
</verification>

<must_haves>
- Admin experience save endpoints preserve D-01 through D-12 semantics with versioned JSON validation and canonical enums.
- Public runtime endpoints return compiled published data only, with storyline session state separate from permanent exploration history.
- SQL, admin DTOs, and public DTOs describe the same default-flow, override, trigger, and exploration vocabulary required for LINK-01 and LINK-02.
</must_haves>

<success_criteria>
- The admin backend compiles after schema/DTO/service hardening.
- The public backend compiles after runtime DTO/service hardening.
- Anonymous admin experience requests are rejected while authenticated admin requests succeed against the live `/api/admin/v1/experience/**` surface.
- Source checks confirm versioned JSON validation, protected admin routing, published-only runtime filtering, and idempotent event plumbing are all present in the implemented files.
</success_criteria>
