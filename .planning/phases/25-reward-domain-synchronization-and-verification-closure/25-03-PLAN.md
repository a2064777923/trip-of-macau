---
phase: 25-reward-domain-synchronization-and-verification-closure
plan: 03
type: execute
wave: 2
depends_on:
  - 25-01
  - 25-02
files_modified:
  - scripts/local/smoke-phase-23-reward-domain.ps1
  - .planning/phases/23-reward-domain-split-and-acquisition-presentation-system/23-UAT.md
  - .planning/phases/23-reward-domain-split-and-acquisition-presentation-system/23-VERIFICATION.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [REWARD-01, REWARD-02, REWARD-03, REWARD-04, REWARD-05]
user_setup: []
must_haves:
  truths:
    - "Phase 25 is not complete until the old diagnosed Phase 23 UAT trail is superseded by a formal verification artifact."
    - "Formal closure must use the live local stack on admin `8081` and public `8080`, not only source inspection."
    - "REQUIREMENTS.md may mark the reward requirements complete only after current live evidence exists for the repaired reward domain."
  artifacts:
    - path: ".planning/v2.1-MILESTONE-AUDIT.md"
      provides: "The authoritative statement that Phase 23 still lacks formal verification and that REWARD-04 is unsatisfied"
      contains: "23-VERIFICATION.md"
    - path: ".planning/phases/23-reward-domain-split-and-acquisition-presentation-system/23-UAT.md"
      provides: "Diagnosed artifact that must be superseded"
      contains: "status: diagnosed"
    - path: "scripts/local/smoke-phase-23-reward-domain.ps1"
      provides: "Existing live admin/public reward-domain smoke baseline"
      contains: "Phase 23 reward domain smoke passed"
    - path: "packages/server/src/main/java/com/aoxiaoyou/tripofmacau/controller/RedeemablePrizeController.java"
      provides: "Current public split reward endpoint"
      contains: "/api/v1/redeemable-prizes"
---

<objective>
Produce the formal reward-domain closure evidence and update traceability.

Purpose: turn the repaired reward-domain implementation into milestone-grade proof by extending live smoke, writing `23-VERIFICATION.md`, superseding the diagnosed UAT trail, and updating reward requirement traceability to closed.
Output: current live reward-domain verification on admin `8081` and public `8080`, a formal verification artifact for Phase 23, and `REQUIREMENTS.md` updated to mark `REWARD-01` through `REWARD-05` complete.
</objective>

<execution_context>
@D:/Archive/trip-of-macau/.codex/get-shit-done/workflows/execute-plan.md
@D:/Archive/trip-of-macau/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v2.1-MILESTONE-AUDIT.md
@.planning/phases/25-reward-domain-synchronization-and-verification-closure/25-CONTEXT.md
@.planning/phases/25-reward-domain-synchronization-and-verification-closure/25-RESEARCH.md
@.planning/phases/23-reward-domain-split-and-acquisition-presentation-system/23-UAT.md
@scripts/local/smoke-phase-23-reward-domain.ps1
@packages/server/src/main/java/com/aoxiaoyou/tripofmacau/controller/RedeemablePrizeController.java
@packages/server/src/main/java/com/aoxiaoyou/tripofmacau/controller/GameRewardController.java
@packages/server/src/main/java/com/aoxiaoyou/tripofmacau/controller/RewardPresentationController.java
</context>

<threat_model>
<trust_boundaries>
  <boundary>
    <name>Live reward-domain stack into milestone verification truth</name>
    <description>Formal closure depends on the running admin and public services returning current split reward-domain behavior, not stale or source-only assumptions.</description>
    <data>admin 8081 responses, public 8080 responses, smoke outputs, verification notes, requirements status</data>
  </boundary>
  <boundary>
    <name>Planning and audit artifacts into milestone archival decisions</name>
    <description>`23-VERIFICATION.md` and `REQUIREMENTS.md` must reflect the current live truth so later milestone audit and archival steps do not overclaim closure.</description>
    <data>verification statuses, superseded UAT state, requirement completion flags, evidence citations</data>
  </boundary>
</trust_boundaries>
<threats>
  <threat>
    <threat_id>T-25-21</threat_id>
    <category>False Positive</category>
    <component>reward verification</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Run live admin and public smoke against the repaired reward-domain stack and record exact endpoints and outcomes in `23-VERIFICATION.md`.</mitigation_plan>
  </threat>
  <threat>
    <threat_id>T-25-22</threat_id>
    <category>Traceability Drift</category>
    <component>requirements closure</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Update `REQUIREMENTS.md` only after the live verification artifact exists and explicitly covers `REWARD-01` through `REWARD-05`.</mitigation_plan>
  </threat>
  <threat>
    <threat_id>T-25-23</threat_id>
    <category>Audit Ambiguity</category>
    <component>Phase 23 closure trail</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Mark the old diagnosed `23-UAT.md` trail as superseded by the formal verification artifact, rather than leaving both artifacts to conflict silently.</mitigation_plan>
  </threat>
</threats>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Extend the live reward-domain smoke to cover shared-owner closure</name>
  <files>scripts/local/smoke-phase-23-reward-domain.ps1</files>
  <read_first>.planning/phases/25-reward-domain-synchronization-and-verification-closure/25-VALIDATION.md, scripts/local/smoke-phase-23-reward-domain.ps1, packages/server/src/main/java/com/aoxiaoyou/tripofmacau/controller/RedeemablePrizeController.java, packages/server/src/main/java/com/aoxiaoyou/tripofmacau/controller/GameRewardController.java, packages/server/src/main/java/com/aoxiaoyou/tripofmacau/controller/RewardPresentationController.java</read_first>
  <action>Extend the Phase 23 smoke harness so it proves the repaired shared-rule closure, not only split reward data presence. The script must still authenticate against admin `8081` and query public `8080`, but now it also needs explicit assertions that a shared reward rule can be observed with linked indoor-behavior ownership on the admin side and that public reward endpoints `/api/v1/redeemable-prizes`, `/api/v1/game-rewards`, and `/api/v1/reward-presentations/{id}` still return current split reward-domain data and rule or presentation summaries. Keep the UTF-8-safe payload decoding pattern from `23-04-SUMMARY.md`.</action>
  <verify>powershell -Command "Select-String -Path 'scripts/local/smoke-phase-23-reward-domain.ps1' -Pattern '/api/v1/redeemable-prizes|/api/v1/game-rewards|/api/v1/reward-presentations|indoor_behavior|linkedRewardRule|UTF-8|Encoding|HttpClient' | Measure-Object"</verify>
  <acceptance_criteria>
    - `smoke-phase-23-reward-domain.ps1` contains the exact endpoint strings `/api/v1/redeemable-prizes`, `/api/v1/game-rewards`, and `/api/v1/reward-presentations`
    - the smoke script contains an assertion path for indoor shared-rule ownership using the exact string `indoor_behavior`
    - the smoke script still uses explicit UTF-8-safe response handling rather than implicit Windows decoding
  </acceptance_criteria>
  <done>The reward smoke proves the Phase 25 closure condition on the live stack.</done>
</task>

<task type="auto">
  <name>Task 2: Write the formal Phase 23 verification artifact and supersede the diagnosed UAT trail</name>
  <files>.planning/phases/23-reward-domain-split-and-acquisition-presentation-system/23-UAT.md, .planning/phases/23-reward-domain-split-and-acquisition-presentation-system/23-VERIFICATION.md</files>
  <read_first>.planning/v2.1-MILESTONE-AUDIT.md, .planning/phases/23-reward-domain-split-and-acquisition-presentation-system/23-UAT.md, .planning/phases/23-reward-domain-split-and-acquisition-presentation-system/23-04-SUMMARY.md</read_first>
  <action>Create a formal `23-VERIFICATION.md` that cites the current live Phase 25 closure evidence, including admin `8081`, public `8080`, shared reward-rule synchronization, and current smoke or test outcomes. Update `23-UAT.md` so it no longer presents the diagnosed state as the active closure artifact; mark it as superseded by `23-VERIFICATION.md` while preserving the original diagnosis history. Keep the verification artifact explicit about what was exercised and when.</action>
  <verify>powershell -Command "Select-String -Path '.planning/phases/23-reward-domain-split-and-acquisition-presentation-system/23-UAT.md','.planning/phases/23-reward-domain-split-and-acquisition-presentation-system/23-VERIFICATION.md' -Pattern 'superseded|23-VERIFICATION.md|8081|8080|REWARD-04|shared reward-rule|indoor' | Measure-Object"</verify>
  <acceptance_criteria>
    - `.planning/phases/23-reward-domain-split-and-acquisition-presentation-system/23-VERIFICATION.md` exists
    - `23-VERIFICATION.md` contains the exact strings `8081`, `8080`, and `REWARD-04`
    - `23-UAT.md` contains the exact string `superseded`
    - the diagnosed UAT trail is preserved historically but no longer acts as the current closure artifact
  </acceptance_criteria>
  <done>Phase 23 has a formal verification artifact and the old diagnosed UAT trail is clearly superseded.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh reward requirement traceability to closed using current evidence</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/23-reward-domain-split-and-acquisition-presentation-system/23-VERIFICATION.md, .planning/v2.1-MILESTONE-AUDIT.md</read_first>
  <action>Update `REQUIREMENTS.md` so `REWARD-01`, `REWARD-02`, `REWARD-03`, `REWARD-04`, and `REWARD-05` are marked complete, and update the traceability table so those five requirements point to Phase 25 as complete rather than pending. Do this only after `23-VERIFICATION.md` is present and explicitly covers the repaired shared-rule synchronization and live reward-domain verification chain.</action>
  <verify>powershell -Command "Select-String -Path '.planning/REQUIREMENTS.md' -Pattern 'REWARD-01|REWARD-02|REWARD-03|REWARD-04|REWARD-05|Phase 25|Complete' | Measure-Object"</verify>
  <acceptance_criteria>
    - `REQUIREMENTS.md` marks `REWARD-01` through `REWARD-05` as checked or complete
    - the traceability table in `REQUIREMENTS.md` maps `REWARD-01` through `REWARD-05` to `Phase 25 | Complete`
    - no reward requirement remains `Pending` in `REQUIREMENTS.md` after the verification artifact is created
  </acceptance_criteria>
  <done>The reward requirements are traceably closed using current Phase 25 evidence.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] the live smoke proves shared reward-rule closure on admin and public surfaces
- [ ] `23-VERIFICATION.md` exists and supersedes the diagnosed UAT trail
- [ ] `REQUIREMENTS.md` closes `REWARD-01` through `REWARD-05` honestly
</verification>

<success_criteria>

- All tasks completed
- Phase 23 has a formal closure artifact instead of a diagnosed-only trail
- `REWARD-01` through `REWARD-05` can be marked complete without stale partial states
</success_criteria>

<output>
After completion, create `.planning/phases/25-reward-domain-synchronization-and-verification-closure/25-03-SUMMARY.md`
</output>
