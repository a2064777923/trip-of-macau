---
phase: 09-spatial-model-rebuild
plan: 03
type: execute
wave: 3
depends_on: [1, 2]
files_modified:
  - packages/server/src/main/java/com/aoxiaoyou/tripofmacau/controller/**/*.java
  - packages/server/src/main/java/com/aoxiaoyou/tripofmacau/service/**/*.java
  - packages/server/src/main/java/com/aoxiaoyou/tripofmacau/dto/**/*.java
  - packages/server/src/test/java/**/*.java
  - packages/client/src/services/api.ts
  - packages/client/src/services/gameService.ts
  - packages/client/src/pages/map/index.tsx
  - packages/client/src/types/game.ts
  - packages/client/src/styles/**/*.scss
  - scripts/local/mysql/init/06-live-backend-mock-migration.sql
  - scripts/local/smoke-phase-09-spatial.ps1
autonomous: true
requirements: [MAP-01, MAP-02, MAP-03, MAP-04]
user_setup: []
must_haves:
  truths:
    - "Public APIs expose the rebuilt city/sub-map/POI hierarchy without breaking the current normalized coordinate semantics used by the mini-program."
    - "The mini-program runtime stops treating Taipa and Coloane as top-level cities and consumes the new city/sub-map hierarchy from live public data."
    - "Seed and smoke proof cover the required initial city set and end-to-end admin/public/runtime spatial flow."
  artifacts:
    - path: "packages/server/src/main/java/com/aoxiaoyou/tripofmacau/controller"
      provides: "Public sub-map and spatially extended city/POI endpoints"
      contains: "SubMapController"
    - path: "packages/client/src/services/gameService.ts"
      provides: "Mini-program spatial hierarchy alignment"
      contains: "subMap"
    - path: "scripts/local/smoke-phase-09-spatial.ps1"
      provides: "Phase 9 end-to-end spatial smoke proof"
      contains: "macau-peninsula"
---

<objective>
Publish the rebuilt spatial model through the public backend, adapt the mini-program runtime, and prove the hierarchy migration end to end.

Purpose: expose cities, sub-maps, and POIs with normalized coordinate metadata to the live client, replace pseudo-city runtime assumptions with the canonical hierarchy, and verify the seed/backfill path for the initial regions.
Output: public spatial endpoints and DTOs, mini-program runtime alignment, seed migration updates, and a reusable Phase 9 smoke script.
</objective>

<execution_context>
@D:/Archive/trip-of-macau/.codex/get-shit-done/workflows/execute-plan.md
@D:/Archive/trip-of-macau/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-spatial-model-rebuild/09-CONTEXT.md
@.planning/phases/09-spatial-model-rebuild/09-RESEARCH.md
@.planning/phases/09-spatial-model-rebuild/09-VALIDATION.md
@.planning/phases/09-spatial-model-rebuild/09-01-PLAN.md
@.planning/phases/09-spatial-model-rebuild/09-02-PLAN.md
@packages/server/src/main/java/com/aoxiaoyou/tripofmacau/service/impl/PublicCatalogServiceImpl.java
@packages/client/src/services/api.ts
@packages/client/src/services/gameService.ts
@packages/client/src/pages/map/index.tsx
</context>

<threat_model>
<trust_boundaries>
  <boundary>
    <name>Published spatial catalog to mini-program runtime</name>
    <description>The public backend serves the rebuilt hierarchy to a traveler-facing client that already assumes GCJ-02-compatible coordinates.</description>
    <data>city codes, sub-map codes, normalized coordinates, popup/display metadata, cover and icon asset URLs</data>
  </boundary>
</trust_boundaries>
<threats>
  <threat>
    <threat_id>T-09-07</threat_id>
    <category>Information Disclosure</category>
    <component>public spatial reads</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Keep published-status filtering and ownership checks across cities, sub-maps, POIs, and linked assets so the public client can never read unpublished spatial records through the new endpoints.</mitigation_plan>
  </threat>
  <threat>
    <threat_id>T-09-08</threat_id>
    <category>Tampering</category>
    <component>legacy hierarchy migration</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Map legacy pseudo-city codes to the new canonical hierarchy explicitly during seed/backfill and add smoke proof so client state does not silently point at missing top-level regions.</mitigation_plan>
  </threat>
</threats>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Extend public spatial endpoints and DTOs for the rebuilt hierarchy</name>
  <files>packages/server/src/main/java/com/aoxiaoyou/tripofmacau/controller/**/*.java, packages/server/src/main/java/com/aoxiaoyou/tripofmacau/service/**/*.java, packages/server/src/main/java/com/aoxiaoyou/tripofmacau/dto/**/*.java, packages/server/src/test/java/**/*.java</files>
  <read_first>packages/server/src/main/java/com/aoxiaoyou/tripofmacau/controller/CityController.java, packages/server/src/main/java/com/aoxiaoyou/tripofmacau/controller/PoiController.java, packages/server/src/main/java/com/aoxiaoyou/tripofmacau/service/impl/PublicCatalogServiceImpl.java</read_first>
  <action>Add a public `SubMapController` and sub-map response DTOs, add service/foundation methods for published sub-map reads, extend public city responses with sub-map awareness as needed, and extend public POI responses with `subMapId`, `subMapCode`, `subMapName`, `mapIconUrl`, `sourceCoordinateSystem`, `sourceLatitude`, and `sourceLongitude` while keeping `latitude` / `longitude` as normalized GCJ-02 values. Add public filtering so `GET /api/v1/pois` supports `subMapCode` and new tests that prove unpublished cities/sub-maps/POIs do not leak through the new hierarchy.</action>
  <verify>powershell -Command "Select-String -Path 'packages/server/src/main/java/com/aoxiaoyou/tripofmacau/**/*.java','packages/server/src/test/java/**/*.java' -Pattern 'SubMapController|subMapCode|subMapName|mapIconUrl|sourceCoordinateSystem|sourceLatitude|sourceLongitude'"</verify>
  <acceptance_criteria>
    - `packages/server/src/main/java/com/aoxiaoyou/tripofmacau/controller/SubMapController.java` exists
    - public server code contains `subMapCode`, `subMapName`, `mapIconUrl`, `sourceCoordinateSystem`, `sourceLatitude`, and `sourceLongitude`
    - `PoiController` or its service path supports `subMapCode`
    - public backend tests cover published sub-map or POI filtering behavior
  </acceptance_criteria>
  <done>The public backend exposes the rebuilt spatial hierarchy and richer coordinate metadata without breaking normalized coordinate compatibility.</done>
</task>

<task type="auto">
  <name>Task 2: Align the mini-program runtime with top-level cities and sub-maps</name>
  <files>packages/client/src/services/api.ts, packages/client/src/services/gameService.ts, packages/client/src/pages/map/index.tsx, packages/client/src/types/game.ts, packages/client/src/styles/**/*.scss</files>
  <read_first>packages/client/src/services/api.ts, packages/client/src/services/gameService.ts, packages/client/src/pages/map/index.tsx, packages/client/src/types/game.ts</read_first>
  <action>Extend the client public DTOs and runtime types with sub-map fields and coordinate-source metadata, then update `gameService.ts` and `pages/map/index.tsx` so the top-level city switcher uses the canonical city set (`macau`, `hengqin`, `hong-kong`, `ecnu`) and Macau sub-maps are handled as a second spatial layer instead of fake cities. Keep the authenticated public-data flow from Phases 7 and 8 intact, keep map calculations using normalized GCJ-02 coordinates, and add a compatibility migration that maps legacy local `currentCityId` values of `taipa` or `coloane` back to `macau` plus a derived current sub-map selection.</action>
  <verify>powershell -Command "Select-String -Path 'packages/client/src/services/api.ts','packages/client/src/services/gameService.ts','packages/client/src/pages/map/index.tsx','packages/client/src/types/game.ts' -Pattern 'subMap|hong-kong|hengqin|macau-peninsula|sourceCoordinateSystem|mapIconUrl'"</verify>
  <acceptance_criteria>
    - `packages/client/src/services/api.ts` contains public sub-map DTO or endpoint bindings
    - `packages/client/src/types/game.ts` contains `subMap`-related runtime types
    - `packages/client/src/services/gameService.ts` contains `subMap` logic and no longer relies on Taipa/Coloane being top-level cities
    - `packages/client/src/pages/map/index.tsx` contains code for the rebuilt city and sub-map hierarchy
  </acceptance_criteria>
  <done>The mini-program consumes the rebuilt spatial hierarchy from live public data instead of pseudo-city assumptions.</done>
</task>

<task type="auto">
  <name>Task 3: Reseed the initial hierarchy and add end-to-end spatial smoke proof</name>
  <files>scripts/local/mysql/init/06-live-backend-mock-migration.sql, scripts/local/smoke-phase-09-spatial.ps1, packages/server/src/test/java/**/*.java, packages/admin/aoxiaoyou-admin-backend/src/test/java/**/*.java</files>
  <read_first>scripts/local/mysql/init/06-live-backend-mock-migration.sql, .planning/phases/09-spatial-model-rebuild/09-VALIDATION.md, packages/client/src/services/gameService.ts</read_first>
  <action>Update the seed/backfill script so the initial top-level cities are `macau`, `hengqin`, `hong-kong`, and `ecnu`, and Macau gets sub-maps `macau-peninsula`, `taipa`, and `coloane`. Reassign or backfill existing POIs to their canonical city/sub-map rows where deterministic, preserve the corrected ECNU naming, and add `scripts/local/smoke-phase-09-spatial.ps1` that proves admin spatial write, MySQL persistence, public city/sub-map/POI read, and client build compatibility in one repeatable path.</action>
  <verify>powershell -Command "Select-String -Path 'scripts/local/mysql/init/06-live-backend-mock-migration.sql','scripts/local/smoke-phase-09-spatial.ps1' -Pattern 'macau-peninsula|taipa|coloane|hengqin|hong-kong|ecnu'; Test-Path 'scripts/local/smoke-phase-09-spatial.ps1'"</verify>
  <acceptance_criteria>
    - `scripts/local/mysql/init/06-live-backend-mock-migration.sql` contains `hengqin`, `hong-kong`, `ecnu`, `macau-peninsula`, `taipa`, and `coloane`
    - `scripts/local/smoke-phase-09-spatial.ps1` exists
    - the smoke script contains checks for city, sub-map, and POI public reads
    - backend tests and the smoke script cover at least one legacy pseudo-city compatibility migration path
  </acceptance_criteria>
  <done>The rebuilt hierarchy is seeded, migrated, and proven end to end.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] public APIs expose city/sub-map/POI hierarchy and richer coordinate metadata without breaking normalized coordinate semantics
- [ ] mini-program runtime uses the canonical top-level cities and sub-maps
- [ ] initial seed data matches the required city/sub-map set
- [ ] build/test/smoke proof covers the end-to-end spatial chain
</verification>

<success_criteria>

- All tasks completed
- The live public backend and mini-program consume the rebuilt spatial hierarchy correctly
- Legacy pseudo-city assumptions are removed or migrated safely
- The initial city/sub-map seed is canonical and verified end to end
</success_criteria>

<output>
After completion, create `.planning/phases/09-spatial-model-rebuild/09-03-SUMMARY.md`
</output>
