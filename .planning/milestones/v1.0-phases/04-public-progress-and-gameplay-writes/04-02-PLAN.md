---
phase: 04-public-progress-and-gameplay-writes
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - packages/server/src/main/java/com/aoxiaoyou/tripofmacau/controller/UserController.java
  - scripts/local/smoke-phase-04-user-writes.ps1
  - packages/server/src/main/resources/application.yml
autonomous: true
requirements: [PUB-03]
user_setup: []
must_haves:
  truths:
    - "The public backend exposes the live `/api/v1/user/*` read/write endpoints needed by the mini-program for gameplay mutations."
    - "Phase 4 write flows can be smoke-tested locally against the running service and MySQL."
  artifacts:
    - path: "packages/server/src/main/java/com/aoxiaoyou/tripofmacau/controller/UserController.java"
      provides: "Contract-aligned public user endpoints"
      contains: "@RequestMapping(\"/api/v1/user\")"
    - path: "scripts/local/smoke-phase-04-user-writes.ps1"
      provides: "Repeatable local HTTP smoke coverage for Phase 4 writes"
      contains: "Invoke-RestMethod"
---

<objective>
Expose the Phase 4 user endpoints and add runnable verification coverage for live write flows.
</objective>

<tasks>

<task type="auto">
  <name>Add public user controllers</name>
  <action>Wire the authenticated read/write controller surface for login, state reads, preferences, check-ins, current-city updates, and reward redemption.</action>
</task>

<task type="auto">
  <name>Add local smoke coverage</name>
  <action>Create a repeatable Phase 4 smoke script or equivalent runnable checks that prove the public backend accepts and persists live user mutations.</action>
</task>

</tasks>

<output>
After completion, create `.planning/phases/04-public-progress-and-gameplay-writes/04-02-SUMMARY.md`
</output>
