---
phase: 05-cos-media-pipeline
plan: 03
type: execute
wave: 3
depends_on: [05-01, 05-02]
files_modified:
  - scripts/local/smoke-phase-05-cos-media.ps1
  - .planning/phases/05-cos-media-pipeline/*
autonomous: true
requirements: [MED-01, MED-02]
user_setup: []
must_haves:
  truths:
    - "Phase 5 can be verified locally against the running admin backend, local MySQL, and the real Tencent COS bucket."
    - "Admin and public asset consumers both rely on the same canonical URL semantics after upload."
  artifacts:
    - path: "scripts/local/smoke-phase-05-cos-media.ps1"
      provides: "Repeatable live COS + MySQL smoke verification"
      contains: "Invoke-RestMethod"
    - path: ".planning/phases/05-cos-media-pipeline/05-VERIFICATION.md"
      provides: "Recorded Phase 5 truth/artifact verification"
      contains: "Phase 5: COS Media Pipeline Verification Report"
---

<objective>
Prove the COS media pipeline works end to end and capture the verification/reporting artifacts for the phase.
</objective>

<tasks>

<task type="auto">
  <name>Build repeatable Phase 5 smoke coverage</name>
  <action>Add a local script that authenticates to the admin backend, uploads a real test file, verifies the stored MySQL metadata, and checks the returned canonical URL behavior.</action>
</task>

<task type="auto">
  <name>Close the phase with evidence</name>
  <action>Run compile/build/smoke verification, confirm that existing public asset resolution still matches uploaded canonical URLs, and write the required summaries/review/verification artifacts.</action>
</task>

</tasks>

<output>
After completion, create `.planning/phases/05-cos-media-pipeline/05-03-SUMMARY.md`
</output>
