---
phase: 06-migration-cutover-and-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/local/mysql/init/*
  - scripts/local/*
  - packages/server/src/main/java/com/aoxiaoyou/tripofmacau/service/impl/PublicCatalogServiceImpl.java
  - packages/server/src/main/java/com/aoxiaoyou/tripofmacau/service/impl/CatalogFoundationServiceImpl.java
autonomous: true
requirements: [DATA-02, ADM-03]
user_setup: []
must_haves:
  truths:
    - "The current mock-driven mini-program content is representable in canonical MySQL tables or runtime settings and can be loaded repeatably without manual edits."
    - "Published public catalog endpoints return seeded cities, POIs, storylines, chapters, tips, stamps, rewards, notifications, and runtime settings that match the live mini-program domains."
    - "Any mock-only data inconsistency discovered during migration is normalized into a coherent canonical representation instead of being copied forward blindly."
  artifacts:
    - path: "scripts/local/mysql/init/06-live-backend-mock-migration.sql"
      provides: "Deterministic Phase 6 seed/import script"
      contains: "phase6-mock-dataset-migration"
    - path: "scripts/local/smoke-phase-06-live-cutover.ps1"
      provides: "Repeatable local verification entrypoint that can seed and assert the migrated content"
      contains: "phase-06"
---

<objective>
Convert the remaining mock dataset into repeatable canonical MySQL seed data and prove the public read model matches it.
</objective>

<tasks>

<task type="auto">
  <name>Migrate mock content into canonical tables</name>
  <action>Seed or upsert cities, storylines, chapters, POIs, stamps, rewards, tips, notifications, and runtime settings so the live public/admin stack has the same effective content coverage as the existing mock dataset.</action>
</task>

<task type="auto">
  <name>Close schema-to-runtime gaps discovered during migration</name>
  <action>Where the mock dataset does not fit the current public/admin read model cleanly, extend the canonical runtime mapping instead of leaving client-only fallback data behind.</action>
</task>

</tasks>

<output>
After completion, create `.planning/phases/06-migration-cutover-and-hardening/06-01-SUMMARY.md`
</output>
