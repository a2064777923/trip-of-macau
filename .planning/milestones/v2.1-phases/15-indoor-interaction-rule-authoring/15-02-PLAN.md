---
phase: 15-indoor-interaction-rule-authoring
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - packages/admin/aoxiaoyou-admin-ui/src/pages/MapSpace/MapTileManagement.tsx
  - packages/admin/aoxiaoyou-admin-ui/src/components/indoor/IndoorRuleAppearanceEditor.tsx
  - packages/admin/aoxiaoyou-admin-ui/src/components/indoor/IndoorRuleTriggerChainEditor.tsx
  - packages/admin/aoxiaoyou-admin-ui/src/components/indoor/IndoorRuleEffectEditor.tsx
  - packages/admin/aoxiaoyou-admin-ui/src/components/indoor/IndoorPathEditor.tsx
  - packages/admin/aoxiaoyou-admin-ui/src/components/indoor/IndoorOverlayGeometryEditor.tsx
  - packages/admin/aoxiaoyou-admin-ui/src/services/api.ts
  - packages/admin/aoxiaoyou-admin-ui/src/types/admin.ts
autonomous: true
requirements: [RULE-01, RULE-02]
user_setup: []
must_haves:
  truths:
    - "Operators must author indoor interaction behavior through structured forms and visual editors, not raw JSON-first inputs."
    - "The floor canvas remains the main workspace and must support both marker placement and richer path or overlay editing."
    - "Phase 15 should preserve drafts, CSV preview, media selection, and same-city POI filtering from the existing indoor workspace."
  artifacts:
    - path: "packages/admin/aoxiaoyou-admin-ui/src/pages/MapSpace/MapTileManagement.tsx"
      provides: "Current indoor authoring workspace"
      contains: "SpatialPopupDisplayField"
    - path: "packages/admin/aoxiaoyou-admin-ui/src/services/api.ts"
      provides: "Current admin indoor API client"
      contains: "createIndoorMarker"
    - path: "packages/admin/aoxiaoyou-admin-ui/src/types/admin.ts"
      provides: "Current indoor admin DTO typings"
      contains: "AdminIndoorMarkerPayload"
---

<objective>
Turn the current indoor map workspace into a structured interaction-rule authoring UI.

Purpose: give operators dedicated editors for appearance conditions, trigger chains, effects, overlays, and motion paths while preserving the useful Phase 12 authoring primitives already in `MapTileManagement.tsx`.
Output: a rebuilt indoor authoring workspace, reusable rule-editor components, updated admin API bindings, and visual geometry/path tooling.
</objective>

<execution_context>
@D:/Archive/trip-of-macau/.codex/get-shit-done/workflows/execute-plan.md
@D:/Archive/trip-of-macau/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-indoor-interaction-rule-authoring/15-CONTEXT.md
@.planning/phases/15-indoor-interaction-rule-authoring/15-RESEARCH.md
@packages/admin/aoxiaoyou-admin-ui/src/pages/MapSpace/MapTileManagement.tsx
@packages/admin/aoxiaoyou-admin-ui/src/services/api.ts
@packages/admin/aoxiaoyou-admin-ui/src/types/admin.ts
</context>

<threat_model>
<trust_boundaries>
  <boundary>
    <name>Operator-authored indoor rule graphs inside the admin workspace</name>
    <description>The UI collects complex rule state, path points, overlay geometry, and linked-entity inheritance choices before sending them to the admin backend.</description>
    <data>typed rule fields, preset selections, draft state, canvas coordinates, CSV preview rows</data>
  </boundary>
  <boundary>
    <name>Floor-canvas interactions into persisted geometry</name>
    <description>Mouse clicks and editing gestures turn into normalized path or overlay geometry used by later runtime phases.</description>
    <data>relative x/y points, polyline paths, polygons, durations, loop settings, editor mode state</data>
  </boundary>
</trust_boundaries>
<threats>
  <threat>
    <threat_id>T-15-04</threat_id>
    <category>Usability Failure</category>
    <component>structured rule authoring UI</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Use dedicated appearance, trigger, effect, and path editors with presets and helper text so operators are not dropped back into JSON-only editing.</mitigation_plan>
  </threat>
  <threat>
    <threat_id>T-15-05</threat_id>
    <category>State Loss</category>
    <component>canvas and draft workflows</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Preserve draft saving, floor-loading guards, and deterministic selection state so richer editing does not reintroduce flicker or accidental data loss.</mitigation_plan>
  </threat>
</threats>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild the indoor node editor around structured rule sections</name>
  <files>packages/admin/aoxiaoyou-admin-ui/src/pages/MapSpace/MapTileManagement.tsx, packages/admin/aoxiaoyou-admin-ui/src/components/indoor/IndoorRuleAppearanceEditor.tsx, packages/admin/aoxiaoyou-admin-ui/src/components/indoor/IndoorRuleTriggerChainEditor.tsx, packages/admin/aoxiaoyou-admin-ui/src/components/indoor/IndoorRuleEffectEditor.tsx, packages/admin/aoxiaoyou-admin-ui/src/services/api.ts, packages/admin/aoxiaoyou-admin-ui/src/types/admin.ts</files>
  <read_first>packages/admin/aoxiaoyou-admin-ui/src/pages/MapSpace/MapTileManagement.tsx, packages/admin/aoxiaoyou-admin-ui/src/services/api.ts, packages/admin/aoxiaoyou-admin-ui/src/types/admin.ts</read_first>
  <action>Refactor `MapTileManagement.tsx` so the right-side editor stops being a marker-only form. Add structured sections or tabs for `node basics`, `appearance`, `triggers`, `effects`, and `advanced`. The basics section must expose `presentationMode`, `overlayType`, `inheritMode`, `runtimeSupportLevel`, `linkedEntityType`, and `linkedEntityId`. The appearance editor must expose preset-driven builders for `schedule_window`, `recurring_calendar`, `user_progress`, `scene_dwell`, `proximity`, `always_on`, and `manual`. The trigger-chain editor must let operators add multiple trigger steps, set one trigger step to depend on another, and choose categories such as `tap`, `proximity`, `dwell`, `drag`, `voice_placeholder`, and `custom`. The effect editor must expose typed effect cards for `popup`, `bubble`, `media`, `path_motion`, `reward_grant`, `collectible_grant`, `badge_grant`, `task_update`, and `account_adjustment`. Keep raw JSON only behind an explicit advanced toggle or drawer.</action>
  <verify>powershell -Command "Select-String -Path 'packages/admin/aoxiaoyou-admin-ui/src/pages/MapSpace/MapTileManagement.tsx','packages/admin/aoxiaoyou-admin-ui/src/components/indoor/*.tsx','packages/admin/aoxiaoyou-admin-ui/src/services/api.ts','packages/admin/aoxiaoyou-admin-ui/src/types/admin.ts' -Pattern 'presentationMode|overlayType|inheritMode|runtimeSupportLevel|schedule_window|recurring_calendar|voice_placeholder|path_motion|account_adjustment|advanced'"</verify>
  <acceptance_criteria>
    - the indoor workspace exposes structured sections for basics, appearance, triggers, and effects
    - the UI contains selectors or builders for the exact condition, trigger, and effect categories listed in the action
    - raw JSON editing is clearly secondary and not the only authoring path
  </acceptance_criteria>
  <done>Operators can compose indoor rule graphs through structured UI instead of raw JSON-first forms.</done>
</task>

<task type="auto">
  <name>Task 2: Add visual path and overlay geometry editing on the floor canvas</name>
  <files>packages/admin/aoxiaoyou-admin-ui/src/pages/MapSpace/MapTileManagement.tsx, packages/admin/aoxiaoyou-admin-ui/src/components/indoor/IndoorPathEditor.tsx, packages/admin/aoxiaoyou-admin-ui/src/components/indoor/IndoorOverlayGeometryEditor.tsx</files>
  <read_first>packages/admin/aoxiaoyou-admin-ui/src/pages/MapSpace/MapTileManagement.tsx</read_first>
  <action>Extend the floor workspace with visual editing tools for `path_motion` and overlay geometry. Add a dedicated editor mode that lets operators click the floor canvas to create normalized points, reorder them, delete them, and persist a `pathGraph` containing at least `points`, `durationMs`, `holdMs`, `loop`, and `easing`. Add overlay geometry tools that support `point`, `polyline`, and `polygon` previews and persist normalized geometry into `overlayGeometryJson` or the typed geometry field returned by the backend. Keep the existing loading guard behavior and show an explicit loading state while switching floors so the richer editor does not flicker or continuously remount.</action>
  <verify>powershell -Command "Select-String -Path 'packages/admin/aoxiaoyou-admin-ui/src/pages/MapSpace/MapTileManagement.tsx','packages/admin/aoxiaoyou-admin-ui/src/components/indoor/*.tsx' -Pattern 'pathGraph|durationMs|holdMs|loop|easing|overlayGeometry|polygon|polyline|loading'"</verify>
  <acceptance_criteria>
    - the indoor workspace exposes visual path editing controls
    - the UI persists path fields including `points`, `durationMs`, `holdMs`, `loop`, and `easing`
    - the UI exposes overlay geometry editing for at least point, polyline, and polygon
  </acceptance_criteria>
  <done>The admin workspace can author path-based behaviors and overlay geometry visually.</done>
</task>

<task type="auto">
  <name>Task 3: Extend drafts, search bindings, and CSV preview for the richer node model</name>
  <files>packages/admin/aoxiaoyou-admin-ui/src/pages/MapSpace/MapTileManagement.tsx, packages/admin/aoxiaoyou-admin-ui/src/services/api.ts, packages/admin/aoxiaoyou-admin-ui/src/types/admin.ts</files>
  <read_first>packages/admin/aoxiaoyou-admin-ui/src/pages/MapSpace/MapTileManagement.tsx, packages/admin/aoxiaoyou-admin-ui/src/services/api.ts, packages/admin/aoxiaoyou-admin-ui/src/types/admin.ts</read_first>
  <action>Preserve and extend the current authoring productivity flows. Update draft records so they store the structured rule fields, `presentationMode`, `overlayType`, `inheritMode`, and `pathGraph` instead of only the old marker fields. Keep same-city POI filtering, but make linked-entity lookup searchable for tasks, activities, collectibles, badges, chapters, and events. Extend CSV preview and confirm support with optional columns `presentationMode`, `appearancePresetCode`, `triggerTemplateCode`, `effectTemplateCode`, and `inheritMode`, while keeping path geometry visual-only. Make the currently selected icon, animation asset, and linked files visibly previewable in the editor state after selection.</action>
  <verify>powershell -Command "Select-String -Path 'packages/admin/aoxiaoyou-admin-ui/src/pages/MapSpace/MapTileManagement.tsx','packages/admin/aoxiaoyou-admin-ui/src/services/api.ts','packages/admin/aoxiaoyou-admin-ui/src/types/admin.ts' -Pattern 'appearancePresetCode|triggerTemplateCode|effectTemplateCode|inheritMode|pathGraph|draft|showSearch|selectedAsset|csv'"</verify>
  <acceptance_criteria>
    - draft persistence includes the new structured node and rule fields
    - linked-entity selectors remain searchable for the major bound entity types
    - CSV preview supports the additional preset and presentation columns listed in the action
  </acceptance_criteria>
  <done>The richer node model stays practical to author at scale instead of becoming a fragile one-off editor.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] the indoor editor no longer depends on raw JSON as the primary behavior authoring path
- [ ] operators can visually author both path motion and overlay geometry
- [ ] draft, asset-preview, and searchable binding workflows still function with the richer node model
- [ ] floor switching remains guarded by loading state instead of flickering through repeated remounts
</verification>

<success_criteria>

- All tasks completed
- The indoor admin workspace supports structured interaction-rule authoring
- Visual path and overlay editing are available on top of the existing floor canvas
- Phase 12 productivity primitives survive the richer rule-authoring upgrade
</success_criteria>

<output>
After completion, create `.planning/phases/15-indoor-interaction-rule-authoring/15-02-SUMMARY.md`
</output>
