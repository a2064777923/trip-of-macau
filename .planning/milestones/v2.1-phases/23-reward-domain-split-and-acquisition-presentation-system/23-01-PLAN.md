---
phase: 23-reward-domain-split-and-acquisition-presentation-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/local/mysql/init/23-phase-23-reward-domain.sql
  - packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/entity/**/*.java
  - packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/dto/**/*.java
  - packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/controller/**/*.java
  - packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/service/**/*.java
  - packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/mapper/**/*.java
  - packages/admin/aoxiaoyou-admin-backend/src/test/java/**/*.java
autonomous: true
requirements: [REWARD-01, REWARD-02, REWARD-03, REWARD-04, REWARD-05]
user_setup: []
must_haves:
  truths:
    - "Phase 23 is not allowed to keep one overloaded reward table and one overloaded reward page as the write path."
    - "Badges and titles must become canonical in-game rewards rather than a parallel root subsystem."
    - "Shared reward rules must be reference-based so reward authoring and indoor interaction authoring cannot drift."
  artifacts:
    - path: ".planning/phases/23-reward-domain-split-and-acquisition-presentation-system/23-CONTEXT.md"
      provides: "Locked reward-domain decisions"
      contains: "兌換獎勵物品管理"
    - path: ".planning/phases/23-reward-domain-split-and-acquisition-presentation-system/23-DESIGN.md"
      provides: "Canonical model and IA target"
      contains: "reward_presentation"
    - path: "packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/entity/Reward.java"
      provides: "Current overloaded reward baseline"
      contains: "stamp_cost"
---

<objective>
Create the canonical backend foundation for the split reward domain.

Purpose: replace the current single reward write model with explicit `redeemable_prize`, `game_reward`, `reward_rule`, and `reward_presentation` contracts that the admin UI and public runtime can build on safely.
Output: new MySQL schema, admin backend entities / DTOs / services / controllers, legacy migration path, and machine-checkable tests for the split reward domain.
</objective>

<execution_context>
@D:/Archive/trip-of-macau/.codex/get-shit-done/workflows/execute-plan.md
@D:/Archive/trip-of-macau/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-reward-domain-split-and-acquisition-presentation-system/23-CONTEXT.md
@.planning/phases/23-reward-domain-split-and-acquisition-presentation-system/23-DESIGN.md
@.planning/phases/23-reward-domain-split-and-acquisition-presentation-system/23-RESEARCH.md
@packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/entity/Reward.java
@packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/entity/Badge.java
@packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/service/impl/AdminCollectibleServiceImpl.java
@packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/controller/AdminCollectibleController.java
</context>

<threat_model>
<trust_boundaries>
  <boundary>
    <name>Operator reward authoring into canonical reward tables</name>
    <description>The admin backend becomes the only write path for split reward families, shared rule definitions, and acquisition presentation definitions.</description>
    <data>reward family, reward type, fulfillment mode, rule references, presentation references, scope bindings, stock data, code-pool metadata</data>
  </boundary>
  <boundary>
    <name>Shared rule references into indoor interaction behaviors</name>
    <description>Reward-trigger logic will later be consumed by both reward pages and indoor interaction authoring, so persistence must prevent duplicate rule copies.</description>
    <data>reward_rule identifiers, rule summaries, linked reward IDs, linked behavior IDs, condition groups, effect bindings</data>
  </boundary>
</trust_boundaries>
<threats>
  <threat>
    <threat_id>T-23-01</threat_id>
    <category>Integrity</category>
    <component>split reward persistence</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Introduce explicit reward family tables and typed DTOs rather than extending the current reward entity with ambiguous mixed-purpose fields.</mitigation_plan>
  </threat>
  <threat>
    <threat_id>T-23-02</threat_id>
    <category>Configuration Drift</category>
    <component>reward-trigger rule reuse</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Persist reward rules as first-class records plus bindings so later reward and interaction editors can reference the same rule IDs and summaries.</mitigation_plan>
  </threat>
  <threat>
    <threat_id>T-23-03</threat_id>
    <category>Migration</category>
    <component>legacy reward and badge data</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Provide a deterministic migration path from legacy `rewards` and `badges` into the new tables, preserving relation bindings and traceability.</mitigation_plan>
  </threat>
</threats>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Introduce canonical reward-domain schema and entity contracts</name>
  <files>scripts/local/mysql/init/23-phase-23-reward-domain.sql, packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/entity/**/*.java, packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/mapper/**/*.java</files>
  <read_first>.planning/phases/23-reward-domain-split-and-acquisition-presentation-system/23-DESIGN.md, packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/entity/Reward.java, packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/entity/Badge.java</read_first>
  <action>Create a new MySQL migration script and matching MyBatis-Plus entities for the canonical reward-domain tables. The schema must include `redeemable_prizes`, `game_rewards`, `reward_rules`, `reward_condition_groups`, `reward_conditions`, `reward_rule_bindings`, `reward_presentations`, and `reward_presentation_steps`. `redeemable_prizes` must include structured fields for `prize_type`, `fulfillment_mode`, `stock_policy_json`, and `operator_notes`. `game_rewards` must include `reward_type`, `rarity`, `stackable`, `max_owned`, `can_equip`, `can_consume`, and `presentation_id`. `reward_rules` must support `rule_type`, `status`, `summary_text`, and a root condition-group reference. `reward_presentations` must support `presentation_type`, `first_time_only`, `skippable`, `minimum_display_ms`, `interrupt_policy`, and `queue_policy`.</action>
  <verify>powershell -Command "Select-String -Path 'scripts/local/mysql/init/23-phase-23-reward-domain.sql','packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/entity/**/*.java' -Pattern 'redeemable_prizes|game_rewards|reward_rules|reward_condition_groups|reward_conditions|reward_rule_bindings|reward_presentations|reward_presentation_steps|fulfillment_mode|reward_type|presentation_id|first_time_only|minimum_display_ms'"</verify>
  <acceptance_criteria>
    - `23-phase-23-reward-domain.sql` contains `CREATE TABLE IF NOT EXISTS redeemable_prizes`
    - `23-phase-23-reward-domain.sql` contains `CREATE TABLE IF NOT EXISTS game_rewards`
    - `23-phase-23-reward-domain.sql` contains `CREATE TABLE IF NOT EXISTS reward_rules`
    - `23-phase-23-reward-domain.sql` contains `CREATE TABLE IF NOT EXISTS reward_presentations`
    - backend entity definitions contain `fulfillmentMode`, `rewardType`, `summaryText`, and `minimumDisplayMs`
  </acceptance_criteria>
  <done>The canonical split reward schema exists and is no longer constrained by the old `Reward` and `Badge` root models.</done>
</task>

<task type="auto">
  <name>Task 2: Add admin backend CRUD and query contracts for split rewards, rules, and presentations</name>
  <files>packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/dto/**/*.java, packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/controller/**/*.java, packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/service/**/*.java</files>
  <read_first>packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/controller/AdminCollectibleController.java, packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/service/impl/AdminCollectibleServiceImpl.java, .planning/phases/23-reward-domain-split-and-acquisition-presentation-system/23-DESIGN.md</read_first>
  <action>Add explicit admin APIs for the new domains instead of keeping them under `/api/admin/v1/collectibles/rewards`. The backend must expose `@RequestMapping("/api/admin/v1/redeemable-prizes")`, `@RequestMapping("/api/admin/v1/game-rewards")`, `@RequestMapping("/api/admin/v1/reward-rules")`, `@RequestMapping("/api/admin/v1/reward-presentations")`, and `@RequestMapping("/api/admin/v1/reward-governance")`. Upsert DTOs must carry `ruleIds`, `presentationId`, `cityBindings`, `subMapBindings`, `indoorBuildingBindings`, `indoorFloorBindings`, and `attachmentAssetIds`, plus structured fields for fulfillment or reward-type semantics instead of raw custom JSON only.</action>
  <verify>powershell -Command "Select-String -Path 'packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/controller/**/*.java','packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/dto/**/*.java','packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/service/**/*.java' -Pattern '/api/admin/v1/redeemable-prizes|/api/admin/v1/game-rewards|/api/admin/v1/reward-rules|/api/admin/v1/reward-presentations|/api/admin/v1/reward-governance|ruleIds|presentationId|fulfillmentMode|rewardType|stackable|maxOwned|postal'"</verify>
  <acceptance_criteria>
    - controller code contains `@RequestMapping("/api/admin/v1/redeemable-prizes")`
    - controller code contains `@RequestMapping("/api/admin/v1/game-rewards")`
    - controller code contains `@RequestMapping("/api/admin/v1/reward-rules")`
    - DTOs expose `ruleIds` and `presentationId`
    - DTOs expose either `fulfillmentMode` or `rewardType` depending on reward family
  </acceptance_criteria>
  <done>The admin backend exposes a clean split reward API surface that the new admin IA can own directly.</done>
</task>

<task type="auto">
  <name>Task 3: Provide deterministic legacy migration and bounded backend proof</name>
  <files>scripts/local/mysql/init/23-phase-23-reward-domain.sql, packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/service/**/*.java, packages/admin/aoxiaoyou-admin-backend/src/test/java/**/*.java</files>
  <read_first>.planning/phases/23-reward-domain-split-and-acquisition-presentation-system/23-RESEARCH.md, packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/service/impl/AdminCollectibleServiceImpl.java</read_first>
  <action>Implement a deterministic migration path that maps legacy `rewards` rows into `redeemable_prize` records and legacy `badges` rows into `game_reward` records with `reward_type = badge`, while preserving storyline, city, sub-map, indoor, and attachment bindings through existing relation services. Add targeted backend tests that prove split-family creation, rule binding persistence, and at least one legacy migration path round-trip successfully.</action>
  <verify>powershell -Command "Select-String -Path 'scripts/local/mysql/init/23-phase-23-reward-domain.sql','packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/service/**/*.java','packages/admin/aoxiaoyou-admin-backend/src/test/java/**/*.java' -Pattern 'INSERT INTO redeemable_prizes|INSERT INTO game_rewards|reward_type = ''badge''|ruleIds|presentationId|legacy|migration|reward_rule_bindings'"</verify>
  <acceptance_criteria>
    - migration logic or seed SQL references both `redeemable_prizes` and `game_rewards`
    - migration logic maps legacy badge data into `reward_type = 'badge'`
    - backend tests reference split reward creation or migration behavior
  </acceptance_criteria>
  <done>The split reward backend can absorb current legacy data without losing relation ownership or type meaning.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] canonical split reward tables exist with structured fulfillment, rule, and presentation fields
- [ ] admin backend routes exist for redeemable prizes, game rewards, rules, presentations, and governance
- [ ] deterministic migration or seed logic exists for current rewards and badges
</verification>

<success_criteria>

- All tasks completed
- the old single reward write model is no longer the canonical path
- Phase 23 has a durable backend foundation for `REWARD-01` through `REWARD-05`
</success_criteria>

<output>
After completion, create `.planning/phases/23-reward-domain-split-and-acquisition-presentation-system/23-01-SUMMARY.md`
</output>
