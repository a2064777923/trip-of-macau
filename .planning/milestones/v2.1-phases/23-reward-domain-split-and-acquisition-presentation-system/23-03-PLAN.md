---
phase: 23-reward-domain-split-and-acquisition-presentation-system
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/service/**/*.java
  - packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/controller/**/*.java
  - packages/server/src/main/java/com/aoxiaoyou/tripofmacau/controller/**/*.java
  - packages/server/src/main/java/com/aoxiaoyou/tripofmacau/service/**/*.java
  - packages/server/src/main/java/com/aoxiaoyou/tripofmacau/dto/**/*.java
  - packages/client/src/services/**/*.ts
  - packages/client/src/types/**/*.ts
  - scripts/local/mysql/init/24-phase-23-reward-seed.sql
  - scripts/local/smoke-phase-23-reward-domain.ps1
  - packages/admin/aoxiaoyou-admin-backend/src/test/java/**/*.java
  - packages/server/src/test/java/**/*.java
autonomous: true
requirements: [REWARD-02, REWARD-03, REWARD-04, REWARD-05]
user_setup: []
must_haves:
  truths:
    - "Shared reward rules must synchronize with indoor interaction authoring instead of being copied into two different JSON stores."
    - "Public reward payloads must distinguish redeemable prizes from owned or grantable in-game rewards."
    - "Acquisition ceremony design is not complete until the public/backend contract can carry fullscreen and audio playback policy safely."
  artifacts:
    - path: ".planning/phases/23-reward-domain-split-and-acquisition-presentation-system/23-DESIGN.md"
      provides: "Runtime-facing reward presentation and rule model"
      contains: "fullscreen_video"
    - path: "packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/entity/IndoorNodeBehavior.java"
      provides: "Current JSON-based indoor behavior baseline"
      contains: "trigger_rules_json"
    - path: "packages/server/src/main/java/com/aoxiaoyou/tripofmacau/controller/RewardController.java"
      provides: "Current public reward read surface"
      contains: "/api/v1/rewards"
---

<objective>
Align the split reward domain across indoor rule governance, public backend payloads, and runtime verification.

Purpose: make the new reward model consumable outside the admin UI by synchronizing shared rule references with interaction behaviors, exposing safe public reward contracts, and proving the phase with seeded data and smoke coverage.
Output: indoor-rule linkage to shared reward rules, public reward and presentation endpoints, client-facing contract updates, deterministic reward seed data, and live verification artifacts.
</objective>

<execution_context>
@D:/Archive/trip-of-macau/.codex/get-shit-done/workflows/execute-plan.md
@D:/Archive/trip-of-macau/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-reward-domain-split-and-acquisition-presentation-system/23-CONTEXT.md
@.planning/phases/23-reward-domain-split-and-acquisition-presentation-system/23-DESIGN.md
@.planning/phases/23-reward-domain-split-and-acquisition-presentation-system/23-RESEARCH.md
@packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/entity/IndoorNodeBehavior.java
@packages/admin/aoxiaoyou-admin-ui/src/pages/MapSpace/IndoorRuleCenter.tsx
@packages/server/src/main/java/com/aoxiaoyou/tripofmacau/controller/RewardController.java
@packages/server/src/main/java/com/aoxiaoyou/tripofmacau/service/impl/PublicCatalogServiceImpl.java
@packages/client/src/services/api.ts
@packages/client/src/services/gameService.ts
</context>

<threat_model>
<trust_boundaries>
  <boundary>
    <name>Shared reward rules into indoor interaction runtime</name>
    <description>Indoor behaviors and reward definitions will reference the same saved rule records, so runtime snapshots and governance reads must stay consistent.</description>
    <data>reward_rule IDs, linked behavior IDs, rule summaries, enabled status, presentation references</data>
  </boundary>
  <boundary>
    <name>Public reward payloads into mini-program runtime</name>
    <description>The public backend becomes the safe projection layer for reward eligibility, ownership-safe summaries, and acquisition presentation payloads.</description>
    <data>redeemable prize summaries, owned game rewards, presentation policy, media URLs, scope bindings, public-safe rule summaries</data>
  </boundary>
</trust_boundaries>
<threats>
  <threat>
    <threat_id>T-23-07</threat_id>
    <category>Integrity</category>
    <component>shared rule synchronization</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Replace or augment JSON-only reward trigger storage with explicit rule references in indoor behavior services and governance reads.</mitigation_plan>
  </threat>
  <threat>
    <threat_id>T-23-08</threat_id>
    <category>Information Disclosure</category>
    <component>public reward payloads</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Project only public-safe summaries, media, and playback fields; do not leak admin-only notes, internal governance metadata, or raw rule authoring structures.</mitigation_plan>
  </threat>
  <threat>
    <threat_id>T-23-09</threat_id>
    <category>False Confidence</category>
    <component>phase verification</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Seed meaningful examples for both reward families and verify admin `8081` plus public `8080` using a deterministic smoke harness.</mitigation_plan>
  </threat>
</threats>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Synchronize shared reward rules with indoor interaction governance</name>
  <files>packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/service/**/*.java, packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/controller/**/*.java, packages/admin/aoxiaoyou-admin-backend/src/test/java/**/*.java</files>
  <read_first>packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/entity/IndoorNodeBehavior.java, packages/admin/aoxiaoyou-admin-ui/src/pages/MapSpace/IndoorRuleCenter.tsx, .planning/phases/23-reward-domain-split-and-acquisition-presentation-system/23-DESIGN.md</read_first>
  <action>Extend the indoor rule governance and authoring backend so reward-trigger logic can reference shared `reward_rule` records. The backend must persist and return linked reward-rule IDs, linked reward IDs, and human-readable summaries for indoor behaviors that grant or depend on rewards. Destructive operations on shared reward rules must surface affected indoor behaviors before deletion or disablement succeeds.</action>
  <verify>powershell -Command "Select-String -Path 'packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/service/**/*.java','packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/controller/**/*.java','packages/admin/aoxiaoyou-admin-backend/src/test/java/**/*.java' -Pattern 'rewardRule|linkedReward|affectedBehavior|impactSummary|delete.*reward rule|disable.*reward rule'"</verify>
  <acceptance_criteria>
    - indoor governance or authoring backend code contains `rewardRule` references
    - delete or disable paths mention affected behaviors or linked rewards
    - tests reference shared reward rule linkage with indoor behaviors
  </acceptance_criteria>
  <done>Indoor interaction governance can stay synchronized with reward-trigger rules instead of duplicating them.</done>
</task>

<task type="auto">
  <name>Task 2: Expose public reward and acquisition-presentation contracts</name>
  <files>packages/server/src/main/java/com/aoxiaoyou/tripofmacau/controller/**/*.java, packages/server/src/main/java/com/aoxiaoyou/tripofmacau/service/**/*.java, packages/server/src/main/java/com/aoxiaoyou/tripofmacau/dto/**/*.java, packages/server/src/test/java/**/*.java, packages/client/src/services/**/*.ts, packages/client/src/types/**/*.ts</files>
  <read_first>packages/server/src/main/java/com/aoxiaoyou/tripofmacau/controller/RewardController.java, packages/server/src/main/java/com/aoxiaoyou/tripofmacau/service/impl/PublicCatalogServiceImpl.java, packages/client/src/services/api.ts, packages/client/src/services/gameService.ts</read_first>
  <action>Add public runtime contracts that distinguish redeemable prizes from in-game rewards and can project acquisition ceremony data safely. The public backend must expose explicit payloads or endpoints for redeemable prize listing, game reward listing or inventory summary, and reward presentation projection with fields such as `presentationType`, `firstTimeOnly`, `skippable`, `minimumDisplayMs`, `interruptPolicy`, `queuePolicy`, and public-safe asset URLs. Update mini-program service and type contracts so future runtime work no longer assumes the old reward / badge split as the canonical interface.</action>
  <verify>powershell -Command "Select-String -Path 'packages/server/src/main/java/com/aoxiaoyou/tripofmacau/controller/**/*.java','packages/server/src/main/java/com/aoxiaoyou/tripofmacau/service/**/*.java','packages/server/src/main/java/com/aoxiaoyou/tripofmacau/dto/**/*.java','packages/server/src/test/java/**/*.java','packages/client/src/services/**/*.ts','packages/client/src/types/**/*.ts' -Pattern 'redeemable-prize|game-reward|presentationType|firstTimeOnly|skippable|minimumDisplayMs|interruptPolicy|queuePolicy|inventorySummary|ownedRewards'"</verify>
  <acceptance_criteria>
    - public backend code contains reward payload fields `presentationType`, `firstTimeOnly`, and `queuePolicy`
    - public backend exposes distinct redeemable prize or game reward contracts instead of one old mixed reward contract only
    - client service or type code references the split reward families
  </acceptance_criteria>
  <done>The split reward domain has public-facing contracts that the mini-program can consume safely.</done>
</task>

<task type="auto">
  <name>Task 3: Seed meaningful reward fixtures and add milestone-grade smoke coverage</name>
  <files>scripts/local/mysql/init/24-phase-23-reward-seed.sql, scripts/local/smoke-phase-23-reward-domain.ps1, packages/admin/aoxiaoyou-admin-backend/src/test/java/**/*.java, packages/server/src/test/java/**/*.java</files>
  <read_first>.planning/phases/23-reward-domain-split-and-acquisition-presentation-system/23-CONTEXT.md, .planning/phases/23-reward-domain-split-and-acquisition-presentation-system/23-DESIGN.md</read_first>
  <action>Create deterministic seed data and smoke coverage for both reward families. The seed set must include at least one offline pickup prize, one voucher or code prize, one badge, one title, one city fragment, and one reward presentation that uses fullscreen media plus audio policy fields. Add a smoke script that verifies admin `8081` and public `8080` can read the split reward records and their presentation metadata through real endpoints.</action>
  <verify>powershell -Command "Select-String -Path 'scripts/local/mysql/init/24-phase-23-reward-seed.sql','scripts/local/smoke-phase-23-reward-domain.ps1','packages/admin/aoxiaoyou-admin-backend/src/test/java/**/*.java','packages/server/src/test/java/**/*.java' -Pattern 'offline_pickup|voucher_code|reward_type|badge|title|city_fragment|fullscreen_video|voice_over|8081|8080'"</verify>
  <acceptance_criteria>
    - seed SQL contains `offline_pickup`
    - seed SQL contains `voucher_code`
    - seed SQL contains reward types `badge`, `title`, and `city_fragment`
    - seed SQL or smoke coverage contains `fullscreen_video`
    - smoke script contains both `8081` and `8080`
  </acceptance_criteria>
  <done>Phase 23 has meaningful test data and live verification hooks instead of a schema-only reward redesign.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] indoor rule governance can reference shared reward rules
- [ ] public reward contracts distinguish reward families and expose safe presentation payloads
- [ ] deterministic seed and smoke coverage exist for split reward examples
</verification>

<success_criteria>

- All tasks completed
- reward rules, presentations, and split reward families stay aligned beyond the admin UI
- Phase 23 can be verified on the live local stack instead of remaining a design-only refactor
</success_criteria>

<output>
After completion, create `.planning/phases/23-reward-domain-split-and-acquisition-presentation-system/23-03-SUMMARY.md`
</output>
