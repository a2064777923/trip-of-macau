---
phase: 16-indoor-rule-workbench-and-governance
plan: 04
type: execute
wave: 3
depends_on: [01, 02, 03]
files_modified:
  - packages/admin/aoxiaoyou-admin-backend/src/test/java/**/*.java
  - scripts/local/smoke-phase-16-indoor-rule-governance.ps1
  - packages/admin/aoxiaoyou-admin-ui/package.json
  - .planning/phases/16-indoor-rule-workbench-and-governance/16-UAT.md
autonomous: true
requirements: [RULE-04, RULE-05]
user_setup: []
must_haves:
  truths:
    - "Phase 16 needs live proof on the local admin stack at port 8081 instead of plan-only claims."
    - "Verification must cover both the staged workbench flow and the cross-entity governance flow."
    - "The Lisboa indoor demo remains the canonical verification fixture unless the phase execution intentionally creates a newer deterministic fixture."
  artifacts:
    - path: "packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/controller/AdminIndoorController.java"
      provides: "Indoor admin endpoints that Phase 16 verification must exercise"
      contains: "/api/admin/v1/map/indoor"
    - path: "packages/admin/aoxiaoyou-admin-ui/src/pages/MapSpace/MapTileManagement.tsx"
      provides: "Indoor authoring surface that launches the staged workbench"
      contains: "workbench"
    - path: "packages/admin/aoxiaoyou-admin-ui/src/pages/MapSpace/IndoorRuleCenter.tsx"
      provides: "Governance center route to be smoke-tested"
      contains: "space/indoor-rules"
---

<objective>
Prove the Phase 16 indoor rule workbench and governance center on the live local stack.

Purpose: leave behind executable evidence that the new workbench and governance center work against the canonical backend, not only in isolated code review.
Output: targeted backend tests, a PowerShell smoke harness against the admin stack on port 8081, optional package script glue for repeatable execution, and a resumable UAT checklist for manual workbench and governance checks.
</objective>

<execution_context>
@D:/Archive/trip-of-macau/.codex/get-shit-done/workflows/execute-plan.md
@D:/Archive/trip-of-macau/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-indoor-rule-workbench-and-governance/16-CONTEXT.md
@.planning/phases/16-indoor-rule-workbench-and-governance/16-RESEARCH.md
@.planning/phases/16-indoor-rule-workbench-and-governance/16-VALIDATION.md
@packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/controller/AdminIndoorController.java
@packages/admin/aoxiaoyou-admin-ui/src/pages/MapSpace/MapTileManagement.tsx
@packages/admin/aoxiaoyou-admin-ui/src/pages/MapSpace/IndoorRuleCenter.tsx
</context>

<threat_model>
<trust_boundaries>
  <boundary>
    <name>Live smoke execution against the local admin runtime</name>
    <description>The smoke harness will authenticate to the admin backend on port 8081, exercise governance and workbench-related endpoints, and assert deterministic readback.</description>
    <data>admin auth token, building and floor identifiers, behavior payloads, governance overview rows, conflict detail, status mutations</data>
  </boundary>
  <boundary>
    <name>Manual operator validation of staged workbench behavior</name>
    <description>The UAT path must prove that staged apply/cancel, behavior naming, map-assisted editing, and governance triage remain understandable for operators.</description>
    <data>behavior names, sort order, staged edits, apply and cancel transitions, conflict summaries, deep-link navigation</data>
  </boundary>
</trust_boundaries>
<threats>
  <threat>
    <threat_id>T-16-06</threat_id>
    <category>False Proof</category>
    <component>phase verification</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Smoke must hit the live admin stack on port 8081 and assert exact behavior fields, conflict data, and status changes instead of checking page load only.</mitigation_plan>
  </threat>
  <threat>
    <threat_id>T-16-07</threat_id>
    <category>Fixture Drift</category>
    <component>smoke data selection</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Use deterministic Lisboa indoor records or fail fast with an explicit missing-fixture error so verification does not silently pass on the wrong floor or city.</mitigation_plan>
  </threat>
  <threat>
    <threat_id>T-16-08</threat_id>
    <category>Usability Blind Spot</category>
    <component>manual acceptance</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Create a resumable UAT checklist that forces human checks for staged editing clarity, naming, ordering, minimap editing, conflict inspection, and governance deep-linking.</mitigation_plan>
  </threat>
</threats>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add backend verification coverage for governance projection, conflict classification, and status mutation</name>
  <files>packages/admin/aoxiaoyou-admin-backend/src/test/java/**/*.java</files>
  <read_first>packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/controller/AdminIndoorController.java, packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/service/impl/IndoorRuleGovernanceService.java, .planning/phases/16-indoor-rule-workbench-and-governance/16-VALIDATION.md</read_first>
  <action>Add or extend Spring Boot tests under `packages/admin/aoxiaoyou-admin-backend/src/test/java` to cover the Phase 16 governance contract. Include one test that asserts the overview endpoint returns `behaviorId`, `behaviorCode`, `runtimeSupportLevel`, and `conflictCount`; one test that asserts the conflict endpoint emits the exact codes `MISSING_PREREQUISITE`, `SCHEDULE_OVERLAP`, `ENTITY_COLLISION`, and `STATUS_MISMATCH` when fixture conditions match; and one test that asserts `PATCH /api/admin/v1/map/indoor/rules/behaviors/{behaviorId}/status` returns `behaviorId`, `status`, `parentNodeStatus`, and non-empty `warnings` for an enable request blocked by unpublished or storage-only parent state. Keep the tests runnable through `mvn test -q` in `packages/admin/aoxiaoyou-admin-backend`.</action>
  <verify>powershell -Command "Select-String -Path 'packages/admin/aoxiaoyou-admin-backend/src/test/java/**/*.java' -Pattern 'MISSING_PREREQUISITE|SCHEDULE_OVERLAP|ENTITY_COLLISION|STATUS_MISMATCH|conflictCount|runtimeSupportLevel|parentNodeStatus|warnings|/rules/overview|/rules/conflicts|/status'"</verify>
  <acceptance_criteria>
    - backend tests exist that cover the overview endpoint fields `behaviorId`, `behaviorCode`, `runtimeSupportLevel`, and `conflictCount`
    - backend tests assert the four exact conflict codes `MISSING_PREREQUISITE`, `SCHEDULE_OVERLAP`, `ENTITY_COLLISION`, and `STATUS_MISMATCH`
    - backend tests assert guarded status updates return `parentNodeStatus` and `warnings`
  </acceptance_criteria>
  <done>Phase 16 backend verification proves the governance contract and guarded status behavior before UI smoke begins.</done>
</task>

<task type="auto">
  <name>Task 2: Build a Phase 16 admin smoke harness for workbench and governance flows on port 8081</name>
  <files>scripts/local/smoke-phase-16-indoor-rule-governance.ps1, packages/admin/aoxiaoyou-admin-ui/package.json</files>
  <read_first>scripts/local/smoke-phase-15-indoor-authoring.ps1, .planning/phases/16-indoor-rule-workbench-and-governance/16-VALIDATION.md, packages/admin/aoxiaoyou-admin-ui/package.json</read_first>
  <action>Create `scripts/local/smoke-phase-16-indoor-rule-governance.ps1` that logs into the admin backend on `http://localhost:8081`, locates the deterministic Lisboa indoor building and one target floor, reads at least one behavior-bearing node, calls `GET /api/admin/v1/map/indoor/rules/overview`, `GET /api/admin/v1/map/indoor/rules/conflicts`, `GET /api/admin/v1/map/indoor/rules/behaviors/{behaviorId}`, and `PATCH /api/admin/v1/map/indoor/rules/behaviors/{behaviorId}/status`, then posts or updates one staged-behavior payload through the canonical indoor authoring endpoint and asserts that subsequent governance reads return the updated `behaviorNameZht`, `sortOrder`, `status`, and `conflictCount`. Finish the smoke flow with `npm run build` in `packages/admin/aoxiaoyou-admin-ui`. If useful, add a package script alias such as `smoke:phase16:indoor-rules` that calls the PowerShell script.</action>
  <verify>powershell -Command "Test-Path 'scripts/local/smoke-phase-16-indoor-rule-governance.ps1'; Select-String -Path 'scripts/local/smoke-phase-16-indoor-rule-governance.ps1','packages/admin/aoxiaoyou-admin-ui/package.json' -Pattern '8081|/rules/overview|/rules/conflicts|/rules/behaviors/|behaviorNameZht|sortOrder|conflictCount|npm run build|smoke:phase16:indoor-rules'"</verify>
  <acceptance_criteria>
    - `scripts/local/smoke-phase-16-indoor-rule-governance.ps1` exists
    - the smoke script authenticates against port `8081` and calls the governance overview, conflicts, detail, and status endpoints
    - the smoke script asserts exact fields including `behaviorNameZht`, `sortOrder`, `status`, and `conflictCount`
    - the Phase 16 proof path includes `npm run build` for `packages/admin/aoxiaoyou-admin-ui`
  </acceptance_criteria>
  <done>Phase 16 has live repeatable proof on the local admin stack rather than relying on screenshots or manual claims.</done>
</task>

<task type="auto">
  <name>Task 3: Leave a resumable UAT scaffold for workbench ergonomics and governance triage</name>
  <files>.planning/phases/16-indoor-rule-workbench-and-governance/16-UAT.md</files>
  <read_first>.planning/phases/16-indoor-rule-workbench-and-governance/16-VALIDATION.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</read_first>
  <action>Create `.planning/phases/16-indoor-rule-workbench-and-governance/16-UAT.md` with pending checkpoints for these exact manual flows: opening the workbench from the indoor page, renaming at least three behaviors, reordering behaviors, canceling one staged change, applying one staged change, using minimap point picking or path authoring, opening the governance center, filtering to one Lisboa floor, inspecting one conflict row, toggling one behavior status, and using the governance deep link back to the authoring context. Include one explicit checkpoint that confirms operators no longer need the crowded inline rule editor as the primary authoring path.</action>
  <verify>powershell -Command "Test-Path '.planning/phases/16-indoor-rule-workbench-and-governance/16-UAT.md'; Select-String -Path '.planning/phases/16-indoor-rule-workbench-and-governance/16-UAT.md' -Pattern 'renaming at least three behaviors|reordering behaviors|canceling one staged change|applying one staged change|minimap point picking|governance center|filtering to one Lisboa floor|inspecting one conflict row|toggling one behavior status|deep link|crowded inline rule editor'"</verify>
  <acceptance_criteria>
    - `16-UAT.md` exists in the phase directory
    - the UAT scaffold covers staged apply and cancel, behavior naming and ordering, minimap editing, governance filtering, conflict inspection, status toggling, and deep linking
    - the UAT scaffold explicitly checks that the crowded inline rule editor is no longer the primary authoring path
  </acceptance_criteria>
  <done>Phase 16 leaves a concrete manual-verification path for the new workbench and governance center.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] backend tests cover governance overview, conflict codes, and guarded status mutation
- [ ] the Phase 16 smoke harness exercises the live admin stack on port 8081
- [ ] the smoke path proves workbench-originated data is visible through the governance center
- [ ] the phase directory contains a resumable `16-UAT.md` for operator verification
</verification>

<success_criteria>

- All tasks completed
- Phase 16 has executable proof for both the indoor rule workbench and the governance center
- workbench-authored behavior changes are visible through the governance contract on the local admin stack
- the project is ready to execute Phase 16 without relying on plan-only confidence
</success_criteria>

<output>
After completion, create `.planning/phases/16-indoor-rule-workbench-and-governance/16-04-SUMMARY.md`
</output>
