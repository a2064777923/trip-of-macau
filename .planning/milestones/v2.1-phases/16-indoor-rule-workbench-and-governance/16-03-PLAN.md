---
phase: 16-indoor-rule-workbench-and-governance
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - packages/admin/aoxiaoyou-admin-ui/src/App.tsx
  - packages/admin/aoxiaoyou-admin-ui/src/layouts/DefaultLayout.tsx
  - packages/admin/aoxiaoyou-admin-ui/src/pages/MapSpace/IndoorRuleCenter.tsx
  - packages/admin/aoxiaoyou-admin-ui/src/components/indoor/IndoorRuleConflictPanel.tsx
  - packages/admin/aoxiaoyou-admin-ui/src/services/api.ts
  - packages/admin/aoxiaoyou-admin-ui/src/types/admin.ts
  - packages/admin/aoxiaoyou-admin-ui/src/pages/MapSpace/IndoorSpaceManagement.tsx
autonomous: true
requirements: [RULE-05]
user_setup: []
must_haves:
  truths:
    - "The governance center must be a dedicated page, not just another table embedded in the indoor authoring screen."
    - "Operators need cross-entity visibility, filterable conflicts, and direct enable/disable actions."
    - "The governance center must deep-link back to the indoor page so rule triage leads back into editing instead of becoming a dead-end dashboard."
  artifacts:
    - path: "packages/admin/aoxiaoyou-admin-ui/src/App.tsx"
      provides: "Current admin route map"
      contains: "space/indoor-buildings"
    - path: "packages/admin/aoxiaoyou-admin-ui/src/layouts/DefaultLayout.tsx"
      provides: "Current admin sidebar and IA"
      contains: "室內建築與小地圖"
    - path: "packages/admin/aoxiaoyou-admin-ui/src/services/api.ts"
      provides: "Current admin indoor API client"
      contains: "validateIndoorRuleGraph"
---

<objective>
Create the operator-facing indoor rule governance center.

Purpose: let operators search, filter, inspect, and triage rule-bearing behaviors and their conflicts without opening each node editor one by one.
Output: a dedicated governance route, filters, overview table, conflict/detail side panel, and deep links back into the indoor authoring surface.
</objective>

<execution_context>
@D:/Archive/trip-of-macau/.codex/get-shit-done/workflows/execute-plan.md
@D:/Archive/trip-of-macau/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-indoor-rule-workbench-and-governance/16-CONTEXT.md
@.planning/phases/16-indoor-rule-workbench-and-governance/16-RESEARCH.md
@.planning/phases/16-indoor-rule-workbench-and-governance/16-UI-SPEC.md
@packages/admin/aoxiaoyou-admin-ui/src/App.tsx
@packages/admin/aoxiaoyou-admin-ui/src/layouts/DefaultLayout.tsx
@packages/admin/aoxiaoyou-admin-ui/src/services/api.ts
@packages/admin/aoxiaoyou-admin-ui/src/types/admin.ts
</context>

<threat_model>
<trust_boundaries>
  <boundary>
    <name>Governance center filters into backend rule projections</name>
    <description>The page must send filter state to the backend and render only the scoped result set, especially for conflict-only views.</description>
    <data>keyword, cityId, buildingId, floorId, POI, linked entity type/id, status, runtime support, conflictOnly, enabledOnly</data>
  </boundary>
  <boundary>
    <name>Governance actions back into authoring routes</name>
    <description>Operators need to move from a governance row into the exact building and floor editor that owns that rule.</description>
    <data>behaviorId, nodeId, buildingId, floorId, route query params, selected behavior context</data>
  </boundary>
</trust_boundaries>
<threats>
  <threat>
    <threat_id>T-16-01</threat_id>
    <category>Projection Drift</category>
    <component>governance page data rendering</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Use typed API clients and explicit DTOs for overview and detail rows instead of ad hoc table shaping.</mitigation_plan>
  </threat>
  <threat>
    <threat_id>T-16-02</threat_id>
    <category>Operator Blind Spot</category>
    <component>conflict triage</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Expose conflict counts in the table and a dedicated side panel that lists classified issues rather than hiding them inside row tooltips only.</mitigation_plan>
  </threat>
</threats>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add a dedicated governance route, sidebar entry, and typed API client</name>
  <files>packages/admin/aoxiaoyou-admin-ui/src/App.tsx, packages/admin/aoxiaoyou-admin-ui/src/layouts/DefaultLayout.tsx, packages/admin/aoxiaoyou-admin-ui/src/services/api.ts, packages/admin/aoxiaoyou-admin-ui/src/types/admin.ts</files>
  <read_first>packages/admin/aoxiaoyou-admin-ui/src/App.tsx, packages/admin/aoxiaoyou-admin-ui/src/layouts/DefaultLayout.tsx, packages/admin/aoxiaoyou-admin-ui/src/services/api.ts, packages/admin/aoxiaoyou-admin-ui/src/types/admin.ts</read_first>
  <action>Add a new admin route `path=\"space/indoor-rules\"` in `App.tsx` and a new sidebar item labeled `互動規則中心` under `地圖與空間管理` in `DefaultLayout.tsx`. Extend the admin API client and types with exact methods and DTOs for `getIndoorRuleOverview`, `getIndoorRuleConflicts`, `getIndoorRuleBehaviorDetail`, and `updateIndoorRuleBehaviorStatus`, matching the governance endpoints from Plan 01. The overview request shape must include `keyword`, `cityId`, `buildingId`, `floorId`, `relatedPoiId`, `linkedEntityType`, `linkedEntityId`, `status`, `runtimeSupportLevel`, `conflictOnly`, and `enabledOnly`.</action>
  <verify>powershell -Command "Select-String -Path 'packages/admin/aoxiaoyou-admin-ui/src/App.tsx','packages/admin/aoxiaoyou-admin-ui/src/layouts/DefaultLayout.tsx','packages/admin/aoxiaoyou-admin-ui/src/services/api.ts','packages/admin/aoxiaoyou-admin-ui/src/types/admin.ts' -Pattern 'space/indoor-rules|互動規則中心|getIndoorRuleOverview|getIndoorRuleConflicts|getIndoorRuleBehaviorDetail|updateIndoorRuleBehaviorStatus|conflictOnly|enabledOnly'"</verify>
  <acceptance_criteria>
    - `App.tsx` contains the exact route `space/indoor-rules`
    - `DefaultLayout.tsx` contains a sidebar label `互動規則中心`
    - the API client and types contain the four governance methods named in the action
  </acceptance_criteria>
  <done>The governance center is a real admin destination with typed data access instead of an unlinked concept.</done>
</task>

<task type="auto">
  <name>Task 2: Build the governance overview page with filters, table, and conflict counts</name>
  <files>packages/admin/aoxiaoyou-admin-ui/src/pages/MapSpace/IndoorRuleCenter.tsx, packages/admin/aoxiaoyou-admin-ui/src/services/api.ts, packages/admin/aoxiaoyou-admin-ui/src/types/admin.ts</files>
  <read_first>packages/admin/aoxiaoyou-admin-ui/src/pages/MapSpace/IndoorSpaceManagement.tsx, packages/admin/aoxiaoyou-admin-ui/src/pages/MapSpace/MapTileManagement.tsx, .planning/phases/16-indoor-rule-workbench-and-governance/16-UI-SPEC.md</read_first>
  <action>Create `IndoorRuleCenter.tsx` as the governance page. The page must expose a sticky filter bar with the exact filters `keyword`, `city`, `building`, `floor`, `POI`, `linked entity type`, `status`, `runtime support`, `只看衝突`, and `只看啟用`. Render a results table with columns for `行為名稱`, `行為代碼`, `父節點 / 標記`, `所屬樓層`, `綁定實體`, `狀態`, and `衝突數`. Conflict counts must render as colored tags or badges using the backend `conflictCount` field instead of local heuristics.</action>
  <verify>powershell -Command "Select-String -Path 'packages/admin/aoxiaoyou-admin-ui/src/pages/MapSpace/IndoorRuleCenter.tsx' -Pattern '只看衝突|只看啟用|行為名稱|行為代碼|父節點 / 標記|所屬樓層|綁定實體|衝突數|conflictCount'"</verify>
  <acceptance_criteria>
    - a new page file exists named `IndoorRuleCenter.tsx`
    - the page exposes the exact filter and column labels listed in the action
    - conflict counts come from the typed backend overview field instead of frontend-only inference
  </acceptance_criteria>
  <done>Operators can scan and scope indoor rule-bearing entities from one dedicated governance page.</done>
</task>

<task type="auto">
  <name>Task 3: Add a conflict/detail side panel with status actions and deep links back to editing</name>
  <files>packages/admin/aoxiaoyou-admin-ui/src/pages/MapSpace/IndoorRuleCenter.tsx, packages/admin/aoxiaoyou-admin-ui/src/components/indoor/IndoorRuleConflictPanel.tsx, packages/admin/aoxiaoyou-admin-ui/src/pages/MapSpace/IndoorSpaceManagement.tsx</files>
  <read_first>packages/admin/aoxiaoyou-admin-ui/src/pages/MapSpace/IndoorRuleCenter.tsx, packages/admin/aoxiaoyou-admin-ui/src/pages/MapSpace/IndoorSpaceManagement.tsx, packages/admin/aoxiaoyou-admin-ui/src/pages/MapSpace/MapTileManagement.tsx</read_first>
  <action>Create `IndoorRuleConflictPanel.tsx` and use it as the governance side panel or detail drawer. The panel must render the selected behavior's `appearanceRules`, `triggerRules`, `effectRules`, `pathGraph` presence, conflict list, and current status. Add action buttons labeled `啟用規則`, `停用規則`, and `前往編輯`. `前往編輯` must navigate back into the indoor page using `buildingId`, `floorId`, and `tab=authoring` query parameters so the operator lands on the owning floor context instead of a generic page root.</action>
  <verify>powershell -Command "Select-String -Path 'packages/admin/aoxiaoyou-admin-ui/src/pages/MapSpace/IndoorRuleCenter.tsx','packages/admin/aoxiaoyou-admin-ui/src/components/indoor/IndoorRuleConflictPanel.tsx' -Pattern '啟用規則|停用規則|前往編輯|appearanceRules|triggerRules|effectRules|pathGraph|buildingId|floorId|tab=authoring'"</verify>
  <acceptance_criteria>
    - a new component file exists named `IndoorRuleConflictPanel.tsx`
    - the governance detail surface contains the exact actions `啟用規則`, `停用規則`, and `前往編輯`
    - the deep-link action navigates with `buildingId`, `floorId`, and `tab=authoring`
  </acceptance_criteria>
  <done>The governance page supports real triage and handoff back into editing instead of being a passive report only.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] the governance center is routed and discoverable from the admin sidebar
- [ ] server-side filters and conflict counts drive the page state
- [ ] selected rows open a detail surface with conflict information and status actions
- [ ] governance rows can deep-link back into the indoor editing context
</verification>

<success_criteria>

- All tasks completed
- Phase 16 ships a dedicated indoor rule governance center page
- operators can filter, inspect, enable/disable, and deep-link rule-bearing entities from one place
- the governance center remains aligned with backend projections and the indoor authoring route structure
</success_criteria>

<output>
After completion, create `.planning/phases/16-indoor-rule-workbench-and-governance/16-03-SUMMARY.md`
</output>
