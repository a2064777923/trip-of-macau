---
phase: 25-reward-domain-synchronization-and-verification-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/dto/request/AdminIndoorNodeBehaviorPayload.java
  - packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/service/impl/IndoorMarkerAuthoringService.java
  - packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/service/impl/IndoorRuleGovernanceService.java
  - packages/admin/aoxiaoyou-admin-backend/src/test/java/com/aoxiaoyou/admin/*.java
  - packages/admin/aoxiaoyou-admin-ui/src/pages/MapSpace/MapTileManagement.tsx
  - packages/admin/aoxiaoyou-admin-ui/src/types/admin.ts
autonomous: true
requirements: [REWARD-04]
user_setup: []
must_haves:
  truths:
    - "Phase 25 must close REWARD-04 by giving indoor interaction authoring a real write path for shared reward-rule bindings."
    - "The canonical persistence model is still `reward_rule_bindings`; this phase must not create a second JSON-only reward trigger representation for indoor behaviors."
    - "Indoor governance already reads linked reward rules, so the missing piece is synchronized authoring and deterministic persistence."
  artifacts:
    - path: ".planning/phases/25-reward-domain-synchronization-and-verification-closure/25-CONTEXT.md"
      provides: "Locked closure scope for shared reward-trigger synchronization"
      contains: "D-04"
    - path: ".planning/phases/25-reward-domain-synchronization-and-verification-closure/25-RESEARCH.md"
      provides: "Code-level evidence that indoor governance is read-only today"
      contains: "IndoorMarkerAuthoringService"
    - path: "packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/service/impl/AdminRewardDomainServiceImpl.java"
      provides: "Canonical reward-side binding sync logic"
      contains: "syncRuleBindings"
    - path: "packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/service/impl/IndoorRuleGovernanceService.java"
      provides: "Current indoor-side linked reward readback"
      contains: "linkedRewardRuleIds"
---

<objective>
Add the missing indoor-side shared reward-rule authoring contract.

Purpose: close the real technical gap behind `REWARD-04` by letting indoor interaction behaviors create, update, and remove canonical shared reward-rule bindings instead of only showing linked rules in governance readback.
Output: indoor behavior payloads and persistence can own `rewardRuleIds`, the indoor workbench can edit them, and reward-rule owner views remain synchronized across reward and indoor domains.
</objective>

<execution_context>
@D:/Archive/trip-of-macau/.codex/get-shit-done/workflows/execute-plan.md
@D:/Archive/trip-of-macau/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-reward-domain-synchronization-and-verification-closure/25-CONTEXT.md
@.planning/phases/25-reward-domain-synchronization-and-verification-closure/25-RESEARCH.md
@packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/dto/request/AdminIndoorNodeBehaviorPayload.java
@packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/service/impl/IndoorMarkerAuthoringService.java
@packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/service/impl/AdminRewardDomainServiceImpl.java
@packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/service/impl/IndoorRuleGovernanceService.java
@packages/admin/aoxiaoyou-admin-ui/src/pages/MapSpace/MapTileManagement.tsx
@packages/admin/aoxiaoyou-admin-ui/src/types/admin.ts
</context>

<threat_model>
<trust_boundaries>
  <boundary>
    <name>Indoor interaction authoring into canonical reward bindings</name>
    <description>The indoor workbench must write shared reward-rule relationships into the same persistence model already used by reward-domain authoring.</description>
    <data>behavior IDs, behavior codes, reward rule IDs, binding role, owner domain, owner code</data>
  </boundary>
  <boundary>
    <name>Cross-domain linked-owner visibility</name>
    <description>Reward and indoor surfaces must resolve the same owner graph so operators do not see contradictory binding state.</description>
    <data>linked rule summaries, linked owner lists, reward owner counts, indoor behavior counts</data>
  </boundary>
</trust_boundaries>
<threats>
  <threat>
    <threat_id>T-25-01</threat_id>
    <category>Integrity</category>
    <component>indoor behavior persistence</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Add `rewardRuleIds` to the indoor behavior authoring contract and persist those IDs through `reward_rule_bindings` with `owner_domain = indoor_behavior`, instead of embedding reward-trigger state into ad hoc JSON.</mitigation_plan>
  </threat>
  <threat>
    <threat_id>T-25-02</threat_id>
    <category>Configuration Drift</category>
    <component>shared rule ownership</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Ensure indoor workbench save and reward-domain save both resolve linked-owner state from the same binding table so delete guards and owner counts stay synchronized.</mitigation_plan>
  </threat>
  <threat>
    <threat_id>T-25-03</threat_id>
    <category>Operator Error</category>
    <component>indoor workbench UI</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Expose reward-rule selection in the behavior editor using the same canonical rule references the reward pages use, so operators choose shared rules directly rather than typing opaque IDs.</mitigation_plan>
  </threat>
</threats>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Extend the indoor behavior request contract with canonical reward rule IDs</name>
  <files>packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/dto/request/AdminIndoorNodeBehaviorPayload.java, packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/service/impl/IndoorMarkerAuthoringService.java</files>
  <read_first>.planning/phases/25-reward-domain-synchronization-and-verification-closure/25-RESEARCH.md, packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/dto/request/AdminIndoorNodeBehaviorPayload.java, packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/service/impl/IndoorMarkerAuthoringService.java, packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/service/impl/AdminRewardDomainServiceImpl.java</read_first>
  <action>Add `private List<Long> rewardRuleIds = new ArrayList<>();` to `AdminIndoorNodeBehaviorPayload` and thread that field through indoor node create or update flows in `IndoorMarkerAuthoringService`. The behavior save path must validate the referenced reward rules and synchronize them through `reward_rule_bindings` with `owner_domain = "indoor_behavior"`, `owner_id = behaviorId`, `owner_code = behaviorCode`, and `binding_role = "attached"`. Reuse the canonical binding table semantics already established in `AdminRewardDomainServiceImpl`; do not add a parallel JSON field to persist reward-trigger links.</action>
  <verify>powershell -Command "Select-String -Path 'packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/dto/request/AdminIndoorNodeBehaviorPayload.java','packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/service/impl/IndoorMarkerAuthoringService.java' -Pattern 'List<Long> rewardRuleIds|indoor_behavior|ownerCode|bindingRole|RewardRuleBinding|rewardRuleBindingMapper|attached' | Measure-Object"</verify>
  <acceptance_criteria>
    - `AdminIndoorNodeBehaviorPayload.java` contains the exact field `List<Long> rewardRuleIds`
    - `IndoorMarkerAuthoringService.java` contains logic that references `RewardRuleBinding` or `rewardRuleBindingMapper` for indoor behavior save or update
    - the indoor behavior persistence path uses the exact owner-domain string `indoor_behavior`
    - no new JSON field is introduced as the canonical storage for reward-rule links
  </acceptance_criteria>
  <done>Indoor behavior payloads can carry canonical shared reward-rule IDs into persistence.</done>
</task>

<task type="auto">
  <name>Task 2: Surface shared reward-rule editing in the indoor workbench and admin types</name>
  <files>packages/admin/aoxiaoyou-admin-ui/src/pages/MapSpace/MapTileManagement.tsx, packages/admin/aoxiaoyou-admin-ui/src/types/admin.ts</files>
  <read_first>.planning/phases/25-reward-domain-synchronization-and-verification-closure/25-RESEARCH.md, packages/admin/aoxiaoyou-admin-ui/src/pages/MapSpace/MapTileManagement.tsx, packages/admin/aoxiaoyou-admin-ui/src/types/admin.ts, packages/admin/aoxiaoyou-admin-ui/src/components/rewards/RewardDomainShared.tsx</read_first>
  <action>Extend the indoor authoring types so behavior records and payloads include `rewardRuleIds` and linked reward-rule summaries where appropriate. In `MapTileManagement.tsx`, add a behavior-editor control that lets the operator pick one or more shared reward rules from canonical admin rule options, using the same rule-reference source pattern already used in `RewardDomainShared.tsx`. The workbench must show currently selected rules and preserve them during edit, draft resume, and save flows so the operator can author or remove reward-trigger links from the indoor side directly.</action>
  <verify>powershell -Command "Select-String -Path 'packages/admin/aoxiaoyou-admin-ui/src/pages/MapSpace/MapTileManagement.tsx','packages/admin/aoxiaoyou-admin-ui/src/types/admin.ts' -Pattern 'rewardRuleIds|linkedRewardRules|name=\"rewardRuleIds\"|getAdminRewardRules|ruleOptions|共享規則' | Measure-Object"</verify>
  <acceptance_criteria>
    - `admin.ts` contains `rewardRuleIds?: number[]` for the indoor behavior payload or response shape
    - `MapTileManagement.tsx` contains a form control keyed by the exact name `rewardRuleIds`
    - `MapTileManagement.tsx` loads reward-rule options from canonical admin reward-rule references rather than freeform ID input
    - selected reward rules remain visible in the indoor behavior editor before save
  </acceptance_criteria>
  <done>The indoor workbench can edit shared reward-rule bindings directly and visibly.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression coverage for indoor and reward shared-rule synchronization</name>
  <files>packages/admin/aoxiaoyou-admin-backend/src/test/java/com/aoxiaoyou/admin/*.java, packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/service/impl/IndoorRuleGovernanceService.java</files>
  <read_first>.planning/phases/25-reward-domain-synchronization-and-verification-closure/25-VALIDATION.md, packages/admin/aoxiaoyou-admin-backend/src/test/java/com/aoxiaoyou/admin/AdminRewardDomainServiceImplTest.java, packages/admin/aoxiaoyou-admin-backend/src/test/java/com/aoxiaoyou/admin/IndoorRuleGovernanceServiceTest.java, packages/admin/aoxiaoyou-admin-backend/src/test/java/com/aoxiaoyou/admin/IndoorRuleAuthoringServiceTest.java</read_first>
  <action>Add or extend targeted JUnit coverage so one test proves indoor behavior save writes `reward_rule_bindings` rows with `owner_domain = indoor_behavior`, one test proves unbinding from indoor behavior removes only that binding, and one test proves reward-rule linked-owner views still resolve both reward owners and indoor behavior owners after mixed edits. Keep governance response fields `linkedRewardRuleIds` and `linkedRewardRules` current with the new authoring path.</action>
  <verify>powershell -Command "Select-String -Path 'packages/admin/aoxiaoyou-admin-backend/src/test/java/com/aoxiaoyou/admin/*.java','packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/service/impl/IndoorRuleGovernanceService.java' -Pattern 'indoor_behavior|rewardRuleIds|linkedRewardRuleIds|linkedRewardRules|RewardRuleBinding|unbind|binding' | Measure-Object"</verify>
  <acceptance_criteria>
    - at least one admin backend test file contains the exact string `indoor_behavior`
    - at least one admin backend test file contains the exact string `rewardRuleIds`
    - `IndoorRuleGovernanceService.java` still returns `linkedRewardRuleIds` and `linkedRewardRules`
    - the test suite covers create or update and unbind behavior for indoor-side reward-rule links
  </acceptance_criteria>
  <done>The shared reward-rule contract is protected by explicit indoor-side regression tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] indoor behavior request and save paths support canonical `rewardRuleIds`
- [ ] the indoor authoring UI can select and persist shared reward rules
- [ ] admin backend tests protect indoor-side shared-rule synchronization
</verification>

<success_criteria>

- All tasks completed
- indoor interaction authoring and reward management now share the same reward-rule binding model
- `REWARD-04` has a real implementation path instead of governance-only readback
</success_criteria>

<output>
After completion, create `.planning/phases/25-reward-domain-synchronization-and-verification-closure/25-01-SUMMARY.md`
</output>
