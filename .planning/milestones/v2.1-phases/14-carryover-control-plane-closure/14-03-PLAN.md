---
phase: 14-carryover-control-plane-closure
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - scripts/local/mysql/init/22-phase-14-carryover-verification-seed.sql
  - scripts/local/smoke-phase-14-carryover.ps1
  - packages/admin/aoxiaoyou-admin-backend/src/test/java/**/*.java
  - packages/server/src/test/java/**/*.java
  - packages/client/package.json
  - .planning/phases/14-carryover-control-plane-closure/14-UAT.md
autonomous: true
requirements: [CARRY-03]
user_setup: []
must_haves:
  truths:
    - "Phase 14 cannot close by assumption; it needs a repeatable smoke path against admin 8081 and public 8080."
    - "Carryover verification must cover both collection/reward carryover and the rebuilt progress/settings surfaces."
    - "The phase should leave a concrete UAT path instead of repeating the archive-time ambiguity from v2.0."
  artifacts:
    - path: ".planning/milestones/v2.0-phases/11-story-activity-and-collection-composition/11-UAT.md"
      provides: "Carryover issue history"
      contains: "result: issue"
    - path: "scripts/local/smoke-phase-11-composition.ps1"
      provides: "Existing smoke pattern for admin/public carryover domains"
      contains: "macau_fire_route"
    - path: "scripts/local/smoke-phase-12-indoor.ps1"
      provides: "Existing indoor smoke pattern"
      contains: "indoor"
---

<objective>
Close the `v2.0` carryover loop with explicit seed, smoke, and UAT artifacts.

Purpose: prevent another archive-time assumption by giving Phase 14 a deterministic showcase dataset, a real carryover smoke harness, and a concrete UAT starter that can be resumed later.
Output: carryover verification seed data, `smoke-phase-14-carryover.ps1`, targeted backend tests, and a Phase 14 UAT scaffold that points at the rebuilt domains.
</objective>

<execution_context>
@D:/Archive/trip-of-macau/.codex/get-shit-done/workflows/execute-plan.md
@D:/Archive/trip-of-macau/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-carryover-control-plane-closure/14-CONTEXT.md
@scripts/local/smoke-phase-11-composition.ps1
@scripts/local/smoke-phase-12-indoor.ps1
@.planning/milestones/v2.0-phases/11-story-activity-and-collection-composition/11-UAT.md
@packages/admin/aoxiaoyou-admin-ui/src/pages/Collectibles/RewardManagement.tsx
@packages/admin/aoxiaoyou-admin-ui/src/pages/UserManagement/index.tsx
@packages/admin/aoxiaoyou-admin-ui/src/pages/SystemManagement/index.tsx
</context>

<threat_model>
<trust_boundaries>
  <boundary>
    <name>Verification seed data into local canonical runtime</name>
    <description>Carryover seed records and test fixtures are loaded into the local MySQL-backed admin/public stack for smoke verification.</description>
    <data>showcase records, relation bindings, traveler-progress fixtures, settings defaults</data>
  </boundary>
  <boundary>
    <name>Smoke assertions across admin, public, and client build paths</name>
    <description>Verification scripts exercise multiple live surfaces and can produce false confidence if they only hit warm or partial state.</description>
    <data>admin auth, endpoint responses, public catalog reads, mini-program build results</data>
  </boundary>
</trust_boundaries>
<threats>
  <threat>
    <threat_id>T-14-07</threat_id>
    <category>Integrity</category>
    <component>verification fixtures</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Use deterministic carryover seed records and named fixture codes so smoke assertions prove the intended bindings and progress data, not arbitrary warm-state leftovers.</mitigation_plan>
  </threat>
  <threat>
    <threat_id>T-14-08</threat_id>
    <category>False Confidence</category>
    <component>smoke coverage</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Smoke must touch both admin and public endpoints and include at least one client-facing build or runtime-alignment assertion rather than stopping at backend compile success.</mitigation_plan>
  </threat>
  <threat>
    <threat_id>T-14-09</threat_id>
    <category>Process</category>
    <component>UAT continuity</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Create a concrete Phase 14 UAT scaffold so manual verification can resume from file state instead of disappearing into conversation history.</mitigation_plan>
  </threat>
</threats>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add deterministic carryover verification seed data</name>
  <files>scripts/local/mysql/init/22-phase-14-carryover-verification-seed.sql</files>
  <read_first>.planning/milestones/v2.0-phases/11-story-activity-and-collection-composition/11-UAT.md, scripts/local/mysql/init/20-phase-14-collection-showcase-seed.sql, scripts/local/mysql/init/18-phase-12-indoor-markers-and-runtime.sql</read_first>
  <action>Create `scripts/local/mysql/init/22-phase-14-carryover-verification-seed.sql` with deterministic fixture records for at least one collectible / badge / reward chain, one traveler-progress scenario, and one settings snapshot that the smoke script can assert by exact code or config key. Use named fixture codes and titles rather than anonymous rows so carryover smoke can prove the rebuilt admin forms, progress aggregates, and settings surfaces are reading the intended records.</action>
  <verify>powershell -Command "Select-String -Path 'scripts/local/mysql/init/22-phase-14-carryover-verification-seed.sql' -Pattern 'carryover|reward_|collectible_|badge_|progress_|translationDefaultLocale|indoorZoomDefault'"</verify>
  <acceptance_criteria>
    - carryover verification seed SQL exists
    - the seed SQL includes named fixture records for collection, progress, and settings assertions
  </acceptance_criteria>
  <done>The phase has deterministic fixture data for smoke and UAT checks.</done>
</task>

<task type="auto">
  <name>Task 2: Build the real Phase 14 smoke harness across admin, public, and client alignment</name>
  <files>scripts/local/smoke-phase-14-carryover.ps1, packages/admin/aoxiaoyou-admin-backend/src/test/java/**/*.java, packages/server/src/test/java/**/*.java, packages/client/package.json</files>
  <read_first>scripts/local/smoke-phase-11-composition.ps1, scripts/local/smoke-phase-12-indoor.ps1, packages/client/package.json</read_first>
  <action>Create `scripts/local/smoke-phase-14-carryover.ps1` that logs into the admin backend on `8081`, reapplies the Phase 14 verification seed if needed, round-trips at least one collectible or reward carryover record through the admin API, verifies public reward or collectible reads on `8080`, verifies at least one traveler-progress or settings endpoint on the admin side, and finishes with `npm run build:weapp` in `packages/client`. Add targeted backend tests where the smoke harness would otherwise rely on opaque service behavior alone.</action>
  <verify>powershell -Command "Test-Path 'scripts/local/smoke-phase-14-carryover.ps1'; Select-String -Path 'scripts/local/smoke-phase-14-carryover.ps1','packages/admin/aoxiaoyou-admin-backend/src/test/java/**/*.java','packages/server/src/test/java/**/*.java' -Pattern '8081|8080|collectible|reward|progress|translationDefaultLocale|build:weapp'"</verify>
  <acceptance_criteria>
    - `scripts/local/smoke-phase-14-carryover.ps1` exists
    - the smoke script touches admin `8081`, public `8080`, and a mini-program build assertion
    - targeted backend tests exist for critical carryover logic that smoke alone should not hide
  </acceptance_criteria>
  <done>The carryover phase has a real end-to-end smoke path instead of archive-time assumptions.</done>
</task>

<task type="auto">
  <name>Task 3: Leave a resumable UAT scaffold for the carryover domains</name>
  <files>.planning/phases/14-carryover-control-plane-closure/14-UAT.md</files>
  <read_first>.planning/milestones/v2.0-phases/11-story-activity-and-collection-composition/11-UAT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</read_first>
  <action>Create `.planning/phases/14-carryover-control-plane-closure/14-UAT.md` with checkpoint entries that cover collection or reward carryover authoring, traveler-progress visibility, system-settings ownership, and carryover smoke readiness. Mark every test as pending and set the file up for `/gsd-verify-work` style one-by-one continuation rather than leaving the phase without a resumable manual-verification artifact.</action>
  <verify>powershell -Command "Test-Path '.planning/phases/14-carryover-control-plane-closure/14-UAT.md'; Select-String -Path '.planning/phases/14-carryover-control-plane-closure/14-UAT.md' -Pattern 'collection|reward|progress|system settings|smoke'"</verify>
  <acceptance_criteria>
    - `14-UAT.md` exists in the phase directory
    - the UAT scaffold includes pending checkpoints for collection, progress, settings, and smoke readiness
  </acceptance_criteria>
  <done>The carryover phase leaves a resumable UAT path instead of another ambiguous closeout state.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] deterministic verification seed data exists for carryover domains
- [ ] Phase 14 smoke hits admin, public, and client-alignment checkpoints
- [ ] the phase directory contains a resumable `14-UAT.md`
- [ ] verification proves carryover closure instead of archive-time assumptions
</verification>

<success_criteria>

- All tasks completed
- Phase 14 can be proven end to end through repeatable smoke and resumable UAT
- Carryover closure no longer depends on conversation memory or archive-time assumptions
- The project enters Phases 15-17 with a clean verification baseline
</success_criteria>

<output>
After completion, create `.planning/phases/14-carryover-control-plane-closure/14-03-SUMMARY.md`
</output>
