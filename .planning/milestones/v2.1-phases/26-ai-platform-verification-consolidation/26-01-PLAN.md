---
phase: 26-ai-platform-verification-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/local/smoke-phase-22-ai-platform-verification.ps1
  - scripts/local/smoke-phase-26-ai-platform-closure.ps1
autonomous: true
requirements: [AI-05, AI-07, AI-08]
user_setup: []
must_haves:
  truths:
    - "Phase 26 must reuse the real local admin stack on 8081 and keep provider truth states honest instead of flattening everything into pass or fail."
    - "The canonical outcome labels remain LIVE_VERIFIED, TEMPLATE_ONLY, CREDENTIAL_MISSING, and ACCESS_DENIED."
    - "Creative finalize proof and the voice workbench proof must be collected in one current smoke chain instead of staying split between old phase scripts."
  artifacts:
    - path: ".planning/phases/26-ai-platform-verification-consolidation/26-CONTEXT.md"
      provides: "Locked Phase 26 closure scope and provider-truth decisions"
      contains: "D-04"
    - path: ".planning/phases/22-ai-platform-verification-and-provider-default-closure/22-VERIFICATION.md"
      provides: "Current witness-provider truth states and creative-finalize evidence"
      contains: "LIVE_VERIFIED"
    - path: ".planning/phases/24-ai-tts-voice-library-and-voice-cloning/24-UAT.md"
      provides: "Current voice workbench witness trail that still lacks a verification artifact"
      contains: "voices"
    - path: ".planning/phases/24-ai-tts-voice-library-and-voice-cloning/24-SECURITY.md"
      provides: "Verified security boundaries for voice preview, clone, and source URL handling"
      contains: "AI24-SSRF-01"
    - path: "scripts/local/smoke-phase-22-ai-platform-verification.ps1"
      provides: "Existing witness smoke for providers, creative finalize, and TTS generation"
      contains: "LIVE_VERIFIED"
---

<objective>
Create one canonical AI closure smoke chain for the live local stack.

Purpose: consolidate the current AI runtime proof for provider truth states, creative finalize, voice inventory, voice preview, and voice-clone visibility into a single current smoke entrypoint that Phase 26 can cite.
Output: the existing Phase 22 smoke becomes reusable and a new Phase 26 smoke script proves the AI platform closure chain on admin `8081` with explicit truth labels and voice-workbench evidence.
</objective>

<execution_context>
@D:/Archive/trip-of-macau/.codex/get-shit-done/workflows/execute-plan.md
@D:/Archive/trip-of-macau/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-ai-platform-verification-consolidation/26-CONTEXT.md
@.planning/phases/22-ai-platform-verification-and-provider-default-closure/22-VERIFICATION.md
@.planning/phases/24-ai-tts-voice-library-and-voice-cloning/24-UAT.md
@.planning/phases/24-ai-tts-voice-library-and-voice-cloning/24-SECURITY.md
@packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/controller/AdminAiController.java
@scripts/local/smoke-phase-22-ai-platform-verification.ps1
</context>

<threat_model>
<trust_boundaries>
  <boundary>
    <name>Live admin AI APIs into milestone-close smoke truth</name>
    <description>Phase 26 smoke must hit the real admin backend routes that power provider inventory, creative finalize, and the voice workbench.</description>
    <data>provider templates, provider rows, overview, voice inventory, preview URLs, generation jobs, finalized asset IDs, truth labels</data>
  </boundary>
  <boundary>
    <name>Structured smoke outputs into verification artifacts</name>
    <description>The consolidated verification docs need a machine-readable or at least deterministic smoke result so later artifacts do not depend on hand-copied console lines.</description>
    <data>LIVE_VERIFIED, TEMPLATE_ONLY, CREDENTIAL_MISSING, ACCESS_DENIED, finalizedAssetId, previewUrl, voiceId</data>
  </boundary>
</trust_boundaries>
<threats>
  <threat>
    <threat_id>T-26-01</threat_id>
    <category>False Positive</category>
    <component>AI closure smoke</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Refactor the existing Phase 22 smoke so Phase 26 can consume deterministic structured results instead of scraping brittle console output.</mitigation_plan>
  </threat>
  <threat>
    <threat_id>T-26-02</threat_id>
    <category>Coverage Gap</category>
    <component>voice workbench evidence</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Add explicit Phase 26 smoke calls for `/api/admin/v1/ai/voices`, `/api/admin/v1/ai/voices/preview`, and the latest TTS generation-job surfaces so voice-library proof is part of the same closure chain.</mitigation_plan>
  </threat>
  <threat>
    <threat_id>T-26-03</threat_id>
    <category>Traceability Drift</category>
    <component>provider truth states</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Keep the canonical labels `LIVE_VERIFIED`, `TEMPLATE_ONLY`, `CREDENTIAL_MISSING`, and `ACCESS_DENIED` in the Phase 26 smoke output so later docs cannot silently reinterpret provider status.</mitigation_plan>
  </threat>
</threats>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Refactor the Phase 22 smoke for structured reuse instead of console-only output</name>
  <files>scripts/local/smoke-phase-22-ai-platform-verification.ps1</files>
  <read_first>.planning/phases/22-ai-platform-verification-and-provider-default-closure/22-VERIFICATION.md, scripts/local/smoke-phase-22-ai-platform-verification.ps1</read_first>
  <action>Extend `scripts/local/smoke-phase-22-ai-platform-verification.ps1` so it can still print the current labeled console output, but also support a structured return path for Phase 26. Add an explicit parameter such as `-EmitJson` or `-Passthru` and include exact fields for witness labels, provider names, capability codes, `finalizedAssetId`, and current image or TTS witness outcomes. The script must preserve the exact truth labels `LIVE_VERIFIED`, `TEMPLATE_ONLY`, `CREDENTIAL_MISSING`, and `ACCESS_DENIED`; do not replace them with booleans or generic pass or fail text.</action>
  <verify>powershell -Command "Select-String -Path 'scripts/local/smoke-phase-22-ai-platform-verification.ps1' -Pattern 'EmitJson|Passthru|finalizedAssetId|LIVE_VERIFIED|TEMPLATE_ONLY|CREDENTIAL_MISSING|ACCESS_DENIED' | Measure-Object"</verify>
  <acceptance_criteria>
    - `smoke-phase-22-ai-platform-verification.ps1` contains the exact strings `LIVE_VERIFIED`, `TEMPLATE_ONLY`, `CREDENTIAL_MISSING`, and `ACCESS_DENIED`
    - the same script contains an explicit structured-output switch such as `EmitJson` or `Passthru`
    - the structured result includes the exact field name `finalizedAssetId`
  </acceptance_criteria>
  <done>The Phase 22 smoke can be reused as a deterministic evidence source instead of a console-only witness.</done>
</task>

<task type="auto">
  <name>Task 2: Create the canonical Phase 26 AI closure smoke entrypoint</name>
  <files>scripts/local/smoke-phase-26-ai-platform-closure.ps1</files>
  <read_first>.planning/phases/26-ai-platform-verification-consolidation/26-CONTEXT.md, packages/admin/aoxiaoyou-admin-backend/src/main/java/com/aoxiaoyou/admin/controller/AdminAiController.java, scripts/local/smoke-phase-22-ai-platform-verification.ps1, .planning/phases/24-ai-tts-voice-library-and-voice-cloning/24-UAT.md</read_first>
  <action>Create `scripts/local/smoke-phase-26-ai-platform-closure.ps1` as the canonical Phase 26 smoke script. The script must authenticate to `http://127.0.0.1:8081`, reuse the structured Phase 22 smoke output for provider truth and creative finalize, then additionally verify the live voice-workbench APIs by calling the exact routes `/api/admin/v1/ai/voices`, `/api/admin/v1/ai/voices/preview`, `/api/admin/v1/ai/generation-jobs`, `/api/admin/v1/ai/generation-jobs/{jobId}`, and `/api/admin/v1/ai/logs`. Use one real preview payload with `providerId`, `modelCode`, `voiceCode`, `languageCode`, and `scriptText`, and assert that voice-library or TTS evidence still resolves through the same truth-label matrix instead of a second custom status vocabulary.</action>
  <verify>powershell -Command "Test-Path 'scripts/local/smoke-phase-26-ai-platform-closure.ps1'; Select-String -Path 'scripts/local/smoke-phase-26-ai-platform-closure.ps1' -Pattern '/api/admin/v1/ai/voices|/api/admin/v1/ai/voices/preview|/api/admin/v1/ai/generation-jobs|/api/admin/v1/ai/logs|scriptText|languageCode|voiceCode|LIVE_VERIFIED|ACCESS_DENIED' | Measure-Object"</verify>
  <acceptance_criteria>
    - a new file exists at `scripts/local/smoke-phase-26-ai-platform-closure.ps1`
    - the script contains the exact route strings `/api/admin/v1/ai/voices`, `/api/admin/v1/ai/voices/preview`, `/api/admin/v1/ai/generation-jobs`, and `/api/admin/v1/ai/logs`
    - the script contains the exact request field names `providerId`, `modelCode`, `voiceCode`, `languageCode`, and `scriptText`
    - the script emits or preserves the exact truth labels `LIVE_VERIFIED`, `TEMPLATE_ONLY`, `CREDENTIAL_MISSING`, and `ACCESS_DENIED`
  </acceptance_criteria>
  <done>Phase 26 has one canonical smoke entrypoint covering providers, creative finalize, and the voice workbench on the live stack.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Phase 22 smoke supports structured output without losing the canonical truth labels
- [ ] Phase 26 smoke covers providers, creative finalize, and voice-workbench APIs on admin `8081`
- [ ] the consolidated smoke output remains honest about non-green provider states
</verification>

<success_criteria>

- All tasks completed
- Phase 26 has one current smoke entrypoint for AI closure
- provider truth and voice-workbench evidence are available in one deterministic proof chain
</success_criteria>

<output>
After completion, create `.planning/phases/26-ai-platform-verification-consolidation/26-01-SUMMARY.md`
</output>
