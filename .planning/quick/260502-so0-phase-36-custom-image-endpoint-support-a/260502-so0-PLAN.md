---
quick_id: 260502-so0
type: quick
description: Phase 36 custom image endpoint support and local asset generation evidence
status: planned
---

# Quick Task 260502-so0 - Phase 36 Custom Image Endpoint Support

## Goal

Unblock Phase 36 image generation by supporting an env-backed OpenAI-compatible image endpoint without changing existing package configuration or committing secrets.

## Constraints

- Do not write the provided API key to tracked files, planning docs, generated reports, or command output.
- Preserve existing `phase36-batch.json` provider/model values; custom endpoint support must be opt-in through runtime environment variables.
- Generated image files may be written under `local-content/phase33/east-west-war-and-coexistence`; source/config edits must remain small and reviewable.
- Board source generation is not currently represented by `phase36-batch.json`; if board sheets are still missing, report that truthfully.

## Tasks

1. Add env-backed custom image endpoint support to `phase36-batch-produce.py`.
   - Support `PHASE36_IMAGE_BASE_URL`, `PHASE36_IMAGE_API_KEY`, `PHASE36_IMAGE_MODEL`, and fallback to `OPENAI_API_KEY`.
   - Accept both base paths like `/v1/images` and full generation paths like `/v1/images/generations`.
   - Handle `b64_json`, `url`, and list/object response shapes.

2. Generate Phase 36 batch image work items using the custom endpoint.
   - Use the key only in the current process environment.
   - Write outputs to manifest localRoot paths.
   - Validate file headers as PNG/JPEG.

3. Update quick summary and Phase 36 evidence without claiming live COS/provider completion.
   - Record local files generated and remaining blockers.
   - Keep Phase 36 live smoke blocked until admin token/COS/upload/publish evidence exists.

## Verification

- Run a dry-run preflight.
- Run the image generation script with endpoint env vars and confirm output files exist.
- Run a header validation command for generated PNG/JPEG files.
- Do not run live upload/promotion unless admin token and COS readiness are explicitly available.
