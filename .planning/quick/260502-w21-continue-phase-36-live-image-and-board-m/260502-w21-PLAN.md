---
quick_id: 260502-w21
type: quick
description: Continue Phase 36 live image and board material import to COS without disturbing unrelated dirty changes
status: executed
---

# Quick Task 260502-w21 - Plan

## Scope

Continue Phase 36 material-production closure for still images and board-sliced icons:

- Use the supplied OpenAI-compatible image endpoint without persisting secrets.
- Import generated local story images and board-sliced child assets through the admin backend into COS.
- Keep existing unrelated admin UI and SQL dirty changes untouched.
- Update Phase 36 evidence honestly while keeping MAT-04 blocked until subtitle-capable ffmpeg is available.

## Execution Steps

1. Verify local admin backend is running on `8081` and obtain an admin bearer token through local login.
2. Probe the supplied compatible image endpoint. Record that model `image-2` is rejected while `gpt-image-1` is accepted on the same endpoint.
3. Add a tracked, secret-free `phase36-image2-redo-batch.json` describing the compatible endpoint rerun.
4. Expand title board slicing from 3 icons to all 15 planned title icons.
5. Import 8 still-image assets and 35 sliced pickup/title icons through backend production APIs, promoting them to `published`.
6. Merge the previously verified live audio rows into the docs production report so image, board, and audio evidence live in one report.
7. Remove secondary video blockers that do not require ffmpeg: add 5 chapter video manifest entries, create package items through admin API, and make the video builder able to backfill local audio/subtitle inputs.
8. Run the live Phase 36 smoke and update verification/state docs with exact pass/blocker status.

## Non-Goals

- Do not generate or publish chapter MP4s while `ffmpeg -filters` lacks `subtitles`.
- Do not touch unrelated dirty admin UI worktree files.
- Do not commit provider keys, bearer tokens, COS secrets, or generated binary assets.
